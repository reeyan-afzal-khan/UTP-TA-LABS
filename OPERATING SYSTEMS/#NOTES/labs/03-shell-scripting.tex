\labsection{3}{Bash scripting fundamentals}{sec:lab03}
\begin{labproject}{Use the shell as a lightweight programming environment}
\textbf{Coverage.} Chapter 3.\\
\textbf{Source files.} Four scripts in \texttt{source\_code/Lab03}.\\
\textbf{Goal.} Use input, arithmetic, selection, loops, and variables while improving edge-case handling.

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\color{MLNavy}}p{0.18\linewidth}X X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Task & Supplied idea & Improvement to make \\
\midrule
1 & Even/odd & Validate integer input and quote expansions \\
2 & Leap year & Implement 400/100/4 Gregorian rule \\
3 & Factorial & Reject negative input; handle $0!$ explicitly \\
4 & Swap & Print before/after values and use descriptive variable names \\
\bottomrule
\end{tabularx}
\end{mltable}

\begin{lstlisting}[style=osbash]
if (( y % 400 == 0 || (y % 4 == 0 && y % 100 != 0) )); then
    echo "$y is a leap year"
else
    echo "$y is not a leap year"
fi
\end{lstlisting}

\begin{tryit}
Test leap-year logic with 1900, 2000, 2024, and 2100. Test factorial with 0, 1, 5, and a negative number.
\end{tryit}
\end{labproject}

\begin{labdeliverable}
Submit four corrected scripts plus a small test table listing inputs, expected outputs, and actual outputs.
\end{labdeliverable}
