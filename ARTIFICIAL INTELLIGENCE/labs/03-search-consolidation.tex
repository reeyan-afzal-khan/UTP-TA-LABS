\labsection{3}{Search Engineering: Package, Tests, and Benchmark}{sec:lab03-search-code}

\begin{labproject}{Turn Labs 1--2 into a reproducible Python toolkit}
\textbf{Scenario.} Convert notebook logic into reusable, testable software.\\
\textbf{Goal.} Package BFS, DFS, UCS, GBFS, and A$^\star$ behind a common interface, verify them with unit tests, and benchmark them on real route queries.

\begin{labmode}
Use \texttt{src/search\_toolkit/}, \texttt{tests/}, and \path{all_experiments/lab03_search_engineering_worked.ipynb}. Unit tests must run without Kaggle data; only the benchmark uses the OpenFlights files.
\end{labmode}

\begin{mlsteps}
  \item \textbf{Standardize the interface.} All algorithms must return the same result fields: found flag, path, hops, path cost, expanded nodes, maximum frontier, and runtime.
  \item \textbf{Separate responsibilities.} Keep generic search logic, OpenFlights loading, plotting, and command-line code in separate modules.
  \item \textbf{Run automated tests.} Cover start=goal, unreachable goal, cycles, weighted shortest path, duplicate frontier updates, admissible A$^\star$, and negative-cost rejection.
  \item \textbf{Exercise the CLI.} Run a command such as \texttt{python -m search\_toolkit --algorithm astar --start KUL --goal KEF}.
  \item \textbf{Benchmark 10 route pairs.} Report success rate, route cost, expanded nodes, and runtime for BFS, UCS, GBFS, and A$^\star$.
  \item \textbf{Prepare CI.} Configure GitHub Actions to run \texttt{pytest -q} without dataset downloads or secrets.
\end{mlsteps}

\requiredoutput{Passing tests, working CLI, \texttt{benchmark.csv}, benchmark summary, and executed notebook.}
\end{labproject}

\begin{verification}
Run \texttt{PYTHONPATH=src pytest -q} before and after refactoring. The real-data benchmark is valid only when the unit-test suite passes.
\end{verification}

\begin{labdeliverable}
Push the package, tests, notebook, and results; create tag \texttt{lab03-final}; and submit the GitHub repository link with a concise architecture and benchmark summary.
\end{labdeliverable}
