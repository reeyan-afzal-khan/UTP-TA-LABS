\labsection{1}{Search Foundations on an Airline Network}{sec:lab01-search}

\begin{labproject}{Build and compare BFS, DFS, and Uniform-Cost Search}
\textbf{Scenario.} Build a route planner over real airline connections and compare route quality with search effort.\\
\textbf{Goal.} Implement and evaluate BFS, DFS, and UCS using one cleaned OpenFlights graph.

\begin{labmode}
Use \texttt{all\_datasets/lab01-03\_airports.csv} and \texttt{all\_datasets/lab01-03\_routes.csv}. Run the worked Python notebook \texttt{all\_experiments/lab01\_search\_foundations\_worked.ipynb}. Save generated evidence under \texttt{results/lab01/}.
\end{labmode}

\begin{mlsteps}
  \item \textbf{Load and validate the graph.} Assign the documented OpenFlights airport columns, retain valid IATA codes and coordinates, remove unusable routes, and collapse duplicate source--destination pairs.
  \item \textbf{Define edge cost.} Treat each direct route as one step for BFS/DFS. For UCS, use Haversine distance in kilometres.
  \item \textbf{Run the algorithms.} For KUL--KEF, SIN--EZE, and NRT--CPT, record path, hops, distance, expanded nodes, maximum frontier, and runtime.
  \item \textbf{Verify every path.} Confirm every consecutive airport pair exists in the cleaned route graph and recompute the path distance independently.
  \item \textbf{Compare objectives.} Explain why BFS can return fewer hops but a longer geographic route than UCS, and why DFS is traversal-order dependent.
  \item \textbf{Save evidence.} Export \texttt{metrics.csv} and one route-comparison figure.
\end{mlsteps}

\requiredoutput{Executed notebook, \texttt{results/lab01/metrics.csv}, route-comparison figure, and a concise interpretation.}
\end{labproject}

\begin{verification}
A clean notebook run must reproduce all three queries without hidden variables. Every returned edge must exist in the source data and every reported distance must match the sum of route-edge distances.
\end{verification}

\begin{labdeliverable}
Push the notebook, reusable Python source, and generated evidence to GitHub. Create tag \texttt{lab01-final}. Do not commit duplicate copies of the raw dataset.
\end{labdeliverable}
