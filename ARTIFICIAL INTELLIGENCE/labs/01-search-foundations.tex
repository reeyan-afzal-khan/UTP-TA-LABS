\labsection{1}{Search Foundations on an Airline Network}{sec:lab01-search}

\begin{labproject}{Build and compare BFS, DFS, and Uniform-Cost Search}
\textbf{Scenario.} You are building the first version of an airline route planner. A route must follow real airport connections; the planner should report both route quality and search effort.\\
\textbf{Goal.} Implement BFS, DFS, and UCS from scratch and run them on a real flight-route graph.


\begin{mlsteps}
  \item \textbf{Load and validate the graph.} Assign the documented OpenFlights column names, remove routes with missing source/destination IATA codes, and keep airports with valid latitude/longitude. Print the number of airports, directed routes, and removed records.
  \item \textbf{Define cost.} Use one edge per direct route. For UCS, calculate edge cost as great-circle distance between the two airports using the Haversine formula. BFS and DFS treat each direct route as one step.
  \item \textbf{Implement the algorithms.} Implement BFS with a queue, DFS with a stack, and UCS with a priority queue. Each algorithm must return \texttt{found}, \texttt{path}, \texttt{hops}, \texttt{path\_cost\_km}, \texttt{expanded}, and \texttt{max\_frontier}.
  \item \textbf{Run the standard queries.} Use the three query pairs supplied in \texttt{config/queries.csv}. The repository template provides KUL--KEF, SIN--EZE, and NRT--CPT; if one IATA code is absent in the downloaded vintage, use the listed alternate and record the substitution in the README.
  \item \textbf{Verify.} Confirm that every consecutive airport pair in each returned path exists in the route data and that reported distance equals the sum of edge distances within a small numerical tolerance.
  \item \textbf{Save evidence.} Write all runs to \texttt{results/metrics.csv} and save one route visualization as \texttt{results/route\_comparison.png}.
\end{mlsteps}

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.18\linewidth}XXXX@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Algorithm & Found & Hops & Cost (km) & Expanded \\
\midrule
BFS & & & & \\
DFS & & & & \\
UCS & & & & \\
\bottomrule
\end{tabularx}
\end{mltable}
\end{labproject}
