\chapter{NLP Methods: Entities, Summaries, Sentiment, and Keywords}
\label{ch:nlp-methods}

\chapterguide%
  {Understand the Lab 6 pipeline idea and basic MATLAB string operations.}
  {explain the nine Lab 7 examples, distinguish rule-based from feature-based NLP, interpret frequency and term frequency--inverse document frequency (TF--IDF) summarization, and identify limitations of simplified simulations.}

Lab 7 surveys nine compact NLP pipelines based on rules, text statistics, or a simple classifier.

\section{Code 1: Named Entity Recognition (NER) simulation}

The program tokenizes a sentence and matches each token against person, location, and organization dictionaries.

\begin{lstlisting}[style=mlmatlab]
clc; clear;
text = "Barack Obama was born in Hawaii and served as the 44th President of the United States.";

personList = ["Barack Obama"];
locationList = ["Hawaii", "United States"];
organizationList = ["United Nations", "Google", "Microsoft"];

tokens = split(text);
disp("Named Entities Identified:");

for i = 1:length(tokens)
    word = erase(tokens{i}, '.');
    if any(strcmp(word, personList))
        fprintf("Person: %s\n", word);
    elseif any(strcmp(word, locationList))
        fprintf("Location: %s\n", word);
    elseif any(strcmp(word, organizationList))
        fprintf("Organization: %s\n", word);
    end
end
\end{lstlisting}

\figref{fig:ner} shows the mismatch. Splitting on whitespace produces single tokens, but two
of the three dictionaries hold two-word entries. Those entries can never match, so the loop
finds only \texttt{Hawaii}.

\begin{aifig}[Why the NER dictionary misses two-word entities]{Tokenising on whitespace
yields one word per token, while the dictionaries store multi-word names. Only the
single-word entry \texttt{Hawaii} can ever match. The fix is to compare $n$-grams, not
tokens --- a representation problem, not a logic error.}{fig:ner}
\begin{tikzpicture}[font=\small]
\foreach \w [count=\i from 0] in {Barack, Obama, was, born, in, Hawaii}{
  \ifnum\i=5
    \node[aidone,minimum width=1.32cm,minimum height=0.62cm,font=\scriptsize\bfseries]
      (t\i) at (1.42*\i,0) {\w};
  \else
    \node[aicell,minimum width=1.32cm,minimum height=0.62cm,font=\scriptsize\bfseries]
      (t\i) at (1.42*\i,0) {\w};
  \fi
}
\node[ailabel,anchor=east] at (-0.85,0) {tokens};
% bracket over the two-word entity
\draw[decorate,decoration={brace,amplitude=4pt,raise=3pt},draw=MLRed,semithick]
  (-0.68,0.34) -- (2.08,0.34);
\node[ailabel,anchor=south,color=MLRed,align=center] at (0.7,0.78)
  {dictionary holds ``Barack Obama'' as one entry:\\no single token can match it};
\node[ailabel,anchor=north,color=MLGreen] at (7.1,-0.42) {matches: one word, one entry};
\draw[aiptr,draw=MLGreen] (7.1,-0.4) -- (t5.south);
\end{tikzpicture}
\end{aifig}

\begin{pitfall}
The original code splits the sentence into single tokens but stores multi-word entities such as ``Barack Obama'' and ``United States'' as whole dictionary entries. Those entries cannot match a single token in the loop. Consequently, the original output contains only \texttt{Location: Hawaii}. This mismatch shows why representation choices matter.
\end{pitfall}

A rule-based correction searches the normalized sentence for complete dictionary phrases:

\begin{lstlisting}[style=mlmatlab]
cleanText = erasePunctuation(text);
entityGroups = {personList, locationList, organizationList};
groupNames = ["Person", "Location", "Organization"];

for g = 1:numel(entityGroups)
    entries = entityGroups{g};
    for i = 1:numel(entries)
        if contains(lower(cleanText), lower(entries(i)))
            fprintf('%s: %s\n', ...
                char(groupNames(g)), char(entries(i)));
        end
    end
end
\end{lstlisting}

This detects \texttt{Barack Obama}, \texttt{Hawaii}, and \texttt{United States}, but cannot identify entities absent from the dictionaries.

\section{Code 2: summarization by keyword frequency}

Before any counting can happen the text must be reduced to comparable units
(\figref{fig:preprocess}). Each stage throws information away deliberately: case, punctuation,
common words, and finally word endings.

\begin{aifig}[The text preprocessing pipeline]{Preprocessing reduces raw text to countable
tokens. Every stage discards something on purpose --- which is also how it goes wrong: stemming
``organisation'' and ``organ'' to the same root makes them indistinguishable
downstream.}{fig:preprocess}
\begin{tikzpicture}[font=\small]
\node[aibox,minimum width=3.0cm,minimum height=0.8cm,font=\scriptsize\ttfamily\color{MLNavy}]
  (a) at (0,0) {"The Robots Are Running."};
\node[aibox,minimum width=2.6cm,minimum height=0.8cm,font=\scriptsize\ttfamily\color{MLNavy}]
  (b) at (3.7,0) {the robots are running};
\node[aibox,minimum width=2.3cm,minimum height=0.8cm,font=\scriptsize\ttfamily\color{MLNavy}]
  (c) at (7.1,0) {robots running};
\node[aibox,minimum width=2.1cm,minimum height=0.8cm,font=\scriptsize\ttfamily\color{MLNavy},
      fill=MLPaleTeal,draw=MLGreen] (d) at (10.2,0) {robot run};
\foreach \x/\y in {a/b, b/c, c/d}{\draw[aiarrow] (\x)--(\y);}
\node[ailabel,anchor=north,align=center] at (1.85,-0.5) {lowercase,\\strip punctuation};
\node[ailabel,anchor=north,align=center] at (5.4,-0.5) {remove\\stopwords};
\node[ailabel,anchor=north,align=center] at (8.65,-0.5) {stem to\\word roots};
\end{tikzpicture}
\end{aifig}

The method counts words across the paragraph, scores each sentence by those frequencies, and selects the highest score.

\begin{mlfigure}
\begin{tikzpicture}[
  b/.style={draw=MLBlue,fill=MLPaleBlue,rounded corners=2mm,minimum width=2.55cm,minimum height=0.9cm,align=center,font=\scriptsize\color{MLNavy}},
  arr/.style={-{Stealth[length=2mm]},thick,draw=MLTeal}
]
\node[b] (s) {Split into sentences};
\node[b,right=0.40cm of s] (t) {Tokenize words};
\node[b,right=0.40cm of t] (f) {Count frequencies};
\node[b,right=0.40cm of f] (sc) {Score sentences};
\node[b,right=0.40cm of sc] (m) {Select max score};
\draw[arr] (s)--(t); \draw[arr] (t)--(f); \draw[arr] (f)--(sc); \draw[arr] (sc)--(m);
\end{tikzpicture}
\end{mlfigure}

The method is \term{extractive}: the summary is one of the original sentences rather than newly generated prose.

\begin{lstlisting}[style=mlmatlab]
sentences = [
    "AI systems process language."
    "AI systems learn patterns from data."
    "Careful evaluation makes AI systems more reliable."
];

allWords = strings(0, 1);
for i = 1:numel(sentences)
    words = split(lower(erasePunctuation(sentences(i))));
    allWords = [allWords; words(words ~= "")];
end

vocabulary = unique(allWords);
frequency = zeros(size(vocabulary));
for j = 1:numel(vocabulary)
    frequency(j) = sum(allWords == vocabulary(j));
end

scores = zeros(numel(sentences), 1);
for i = 1:numel(sentences)
    words = split(lower(erasePunctuation(sentences(i))));
    for j = 1:numel(words)
        idx = find(vocabulary == words(j), 1);
        if ~isempty(idx)
            scores(i) = scores(i) + frequency(idx);
        end
    end
end

[~, best] = max(scores);
disp("Summary: " + sentences(best));
disp(table(sentences, scores));
\end{lstlisting}

Raw-frequency scoring may favor long sentences and function words; consider stop-word removal or length normalization.

\section{Code 3: grammar correction simulation}

The grammar-correction simulation handles one pattern through string replacement:

\begin{lstlisting}[style=mlmatlab]
inputSentence = "She go to school every day.";
patterns = ["go to school"];
corrections = ["goes to school"];

corrected = inputSentence;
for i = 1:length(patterns)
    corrected = strrep(corrected, patterns(i), corrections(i));
end
disp("Original:  " + inputSentence);
disp("Corrected: " + corrected);
\end{lstlisting}

The program applies a fixed replacement rule rather than inferring grammar.

\section{Code 4: dependency-parsing simulation as part-of-speech (POS) tags}

The example pairs each word in ``The dog chased the cat.'' with a manual tag. Here, \texttt{DET} means determiner, \texttt{NOUN} noun, and \texttt{VERB} verb:
\[
\texttt{DET, NOUN, VERB, DET, NOUN}.
\]

\begin{lstlisting}[style=mlmatlab]
sentence = "The dog chased the cat.";
words = split(erasePunctuation(sentence));
tags = ["DET", "NOUN", "VERB", "DET", "NOUN"];

for i = 1:length(words)
    fprintf("%-7s - %s\n", words{i}, tags{i});
end
\end{lstlisting}

\begin{intuition}
The programmer supplies the tags, so the code demonstrates tagged-text representation rather than automatic parsing.
\end{intuition}

\section{Code 5: simplified Naive Bayes sentiment classification}

Codes 1--4 encode knowledge the programmer already has. Code 5 is the first that does not:
it counts how often each word appeared in positive and negative training sentences, then
scores a new sentence by multiplying those counts together (\figref{fig:naive-bayes}). The
``naive'' assumption is that the words are independent given the label --- which is plainly
false for language, and works anyway.

\begin{aifig}[How Naive Bayes scores a sentence]{Naive Bayes on a two-class problem. Each
word contributes an independent likelihood, the products are compared, and the larger one
wins. Nothing here is a rule the programmer wrote: every number comes from counting the
training data.}{fig:naive-bayes}
\begin{tikzpicture}[font=\small]
\node[ailabel,anchor=east,color=MLNavy] at (-0.8,1.5) {\textbf{sentence}};
\foreach \w [count=\i from 0] in {this, movie, was, great}{
  \node[aicell,minimum width=1.25cm,minimum height=0.58cm,font=\scriptsize\bfseries]
    at (1.35*\i,1.5) {\w};}
% two class rows
\foreach \cls/\yy/\col/\vals in {%
  positive/0.35/MLGreen/{0.12,0.20,0.15,0.30},
  negative/-0.55/MLRed/{0.11,0.18,0.14,0.03}}{
  \node[ailabel,anchor=east,color=\col] at (-0.8,\yy) {\textbf{\cls}};
  \foreach \v [count=\j from 0] in \vals{
    \node[minimum width=1.25cm,minimum height=0.5cm,draw=\col!55,fill=\col!10,
          font=\scriptsize\color{MLNavy},inner sep=0pt] at (1.35*\j,\yy) {\v};}
}
\foreach \i in {0,...,3}{\draw[aiptr,draw=MLMuted!60] (1.35*\i,1.2) -- (1.35*\i,0.62);}
\node[ailabel,anchor=west,align=left,color=MLGreen] at (5.5,0.35)
  {product $=1.08\times10^{-3}$};
\node[ailabel,anchor=west,align=left,color=MLRed] at (5.5,-0.55)
  {product $=8.3\times10^{-5}$};
\node[aibox,fill=MLPaleTeal,draw=MLGreen,minimum width=3.4cm,font=\scriptsize\color{MLNavy}]
  at (2.05,-1.55) {predicted: \textbf{positive}};
\node[ailabel,anchor=west,align=left,text width=4.6cm] at (5.5,-1.55)
  {``great'' does the work: it is ten times more likely under the positive class.};
\end{tikzpicture}
\end{aifig}

Five labeled sentences are converted to binary word features, used to train \texttt{fitcnb}, and tested on ``The product is wonderful.''

\begin{lstlisting}[style=mlmatlab]
texts = [
    "I love this product", ...
    "This is terrible", ...
    "Very happy with the results", ...
    "It is bad and disappointing", ...
    "Absolutely wonderful experience"
];
labels = [1; 0; 1; 0; 1];
bag = ["love", "terrible", "happy", "bad", "wonderful"];

features = zeros(length(texts), length(bag));
for i = 1:length(texts)
    for j = 1:length(bag)
        if contains(lower(texts(i)), bag(j))
            features(i, j) = 1;
        end
    end
end

keep = var(features) > 0;
features = features(:, keep);
activeBag = bag(keep);
Mdl = fitcnb(features, labels, 'DistributionNames', 'kernel');

testText = "The product is wonderful";
testFeatures = zeros(1, numel(activeBag));
for j = 1:numel(activeBag)
    testFeatures(j) = contains(lower(testText), activeBag(j));
end

prediction = predict(Mdl, testFeatures);
if prediction == 1
    disp("Predicted Sentiment: Positive");
else
    disp("Predicted Sentiment: Negative");
end
\end{lstlisting}

For the supplied test sentence, the program reports \texttt{Predicted Sentiment: Positive}.

The same \texttt{keep} mask must be applied to both training and test features. Otherwise, the classifier receives a different number or order of columns during prediction.

\section{Code 6: summarization with TF--IDF}

Raw frequency treats every word as equally informative, which is why ``the'' outranks the
topic. TF--IDF fixes this by scaling each count down by how many documents the word appears
in (\figref{fig:tfidf}). A word that occurs everywhere carries almost no information about
\emph{which} document you are reading, and its weight collapses accordingly.

\begin{aifig}[Raw counts versus TF--IDF weights]{The same three-sentence document scored two
ways. Raw frequency puts the stopword ``the'' on top; TF--IDF divides it out because it
appears in every sentence, letting the words that actually distinguish the document rise.
Nothing was removed by hand --- the weighting did the filtering.}{fig:tfidf}
\begin{tikzpicture}
\begin{axis}[
  name=raw, width=6.5cm, height=4.6cm,
  title={Raw frequency},
  xbar, y dir=reverse,
  xmin=0, xmax=6,
  ytick=data,
  symbolic y coords={the,robot,sensor,battery,charge},
  nodes near coords, nodes near coords style={font=\tiny\color{MLMuted}},
  bar width=7pt, axis lines=left,
  tick label style={font=\scriptsize\color{MLMuted}},
  enlarge y limits=0.16]
\addplot[draw=MLRed,fill=MLPaleRed] coordinates {(5,the) (3,robot) (2,sensor) (2,battery) (1,charge)};
\end{axis}
\begin{axis}[
  at={(raw.right of south east)}, xshift=1.35cm, anchor=left of south west,
  width=6.5cm, height=4.6cm,
  title={TF--IDF weight},
  xbar, y dir=reverse,
  xmin=0, xmax=1.05,
  ytick=data,
  symbolic y coords={the,robot,sensor,battery,charge},
  nodes near coords, nodes near coords style={font=\tiny\color{MLMuted}},
  bar width=7pt, axis lines=left,
  tick label style={font=\scriptsize\color{MLMuted}},
  enlarge y limits=0.16]
\addplot[draw=MLGreen,fill=MLPaleTeal] coordinates {(0.00,the) (0.41,robot) (0.62,sensor) (0.62,battery) (0.48,charge)};
\end{axis}
\end{tikzpicture}
\end{aifig}


Lab 7 then uses MATLAB's text-processing objects: \texttt{tokenizedDocument}, \texttt{bagOfWords}, and \texttt{tfidf}. Four sentences are converted into a TF--IDF matrix, sentence scores are computed by summing each row, and the maximum is returned.

\begin{lstlisting}[style=mlmatlab]
text = [
    "Artificial intelligence is a broad field."
    "Machine learning enables systems to learn patterns from data."
    "NLP helps computers process human language."
    "Robotics connects sensing, planning, and action."
];

documents = tokenizedDocument(text);
bag = bagOfWords(documents);
tfidfMatrix = tfidf(bag);
score = sum(tfidfMatrix, 2);
[~, idx] = max(score);
disp("Summary: " + text(idx));
disp(table(text, score));
\end{lstlisting}

For the supplied text, the program selects ``Machine learning enables systems to learn patterns from data.''

Summed TF--IDF may favor sentences with more distinct terms; compare length-normalized scores when sentence lengths differ.

\section{Code 7: rule-based sentiment analysis}

The second sentiment method uses positive and negative word lists rather than a trained classifier.

\begin{lstlisting}[style=mlmatlab]
text = "The scenery was amazing but the final scene was dull.";
positiveWords = ["good", "great", "excellent", "fantastic", ...
                 "amazing", "breathtaking"];
negativeWords = ["bad", "terrible", "awful", "boring", ...
                 "worst", "dull"];

words = lower(split(erasePunctuation(text)));
posCount = sum(ismember(words, positiveWords));
negCount = sum(ismember(words, negativeWords));

if posCount > negCount
    sentiment = "Positive";
elseif negCount > posCount
    sentiment = "Negative";
else
    sentiment = "Neutral";
end
fprintf('Positive=%d, Negative=%d, Result=%s\n', ...
    posCount, negCount, char(sentiment));
\end{lstlisting}

The program returns Positive, Negative, or Neutral according to which count is larger.

\section{Code 8: heuristic language detection}

The language detector defines small keyword lists for English, French, and German. It counts word matches for each list, takes the largest count, and maps the index to a language label. For the supplied French sentence, the program reports French.

\begin{lstlisting}[style=mlmatlab]
text = "Bonjour, comment allez-vous aujourd'hui";
english = ["the", "and", "hello", "good"];
french = ["bonjour", "comment", "vous", "aujourd'hui"];
german = ["hallo", "wie", "geht", "ihnen"];

normalized = lower(erase(text, [",", ".", "!", "?"]));
normalized = replace(normalized, "-", " ");
words = split(normalized);
enCount = sum(ismember(words, english));
frCount = sum(ismember(words, french));
deCount = sum(ismember(words, german));

counts = [enCount, frCount, deCount];
languages = ["English", "French", "German"];
[bestCount, idx] = max(counts);
if bestCount == 0
    result = "Unknown";
elseif sum(counts == bestCount) > 1
    result = "Ambiguous";
else
    result = languages(idx);
end
disp("Detected language: " + result);
\end{lstlisting}

The zero-match and tie checks prevent the program from reporting English merely because it occupies the first array position.

\section{Code 9: keyword extraction}

The final example tokenizes a paragraph, removes stop words and punctuation, builds a bag-of-words model, sorts vocabulary counts in descending order, and returns the top five words.

\begin{lstlisting}[style=mlmatlab]
text = "Data science involves extracting insights from structured " + ...
       "and unstructured data using statistical and machine " + ...
       "learning techniques.";

document = tokenizedDocument(text);
document = lower(document);            % fold case BEFORE counting
document = erasePunctuation(document); % strip punctuation ...
document = removeStopWords(document);  % ... then remove stop words

bow = bagOfWords(document);
[sortedCounts, sortedIndex] = sort(bow.Counts, 'descend');
sortedWords = bow.Vocabulary(sortedIndex);

n = min(5, numel(sortedWords));
disp(table(sortedWords(1:n)', sortedCounts(1:n)', ...
    'VariableNames', {'Keyword', 'Count'}));
\end{lstlisting}

\begin{pitfall}
\textbf{Two preprocessing details decide whether this ranking means anything.}

\emph{Case.} \texttt{tokenizedDocument} is case sensitive, so \texttt{Data} at the start of a sentence and \texttt{data} in the middle are different tokens. In the supplied paragraph ``data'' occurs three times but is counted as \texttt{Data}\,=\,2 and \texttt{data}\,=\,1 --- the most important keyword in the text, understated by a third. Call \texttt{lower(document)} before counting.

\emph{Order.} Strip punctuation \emph{before} removing stop words. A token still carrying a full stop does not match the stop-word list, so \texttt{and.} survives a removal that \texttt{and} would not.

\begin{lstlisting}[style=mlmatlab]
document = tokenizedDocument(text);
document = lower(document);          % fold case first
document = erasePunctuation(document);
document = removeStopWords(document); % then remove stop words
\end{lstlisting}
\end{pitfall}

\begin{pitfall}
\textbf{Print the counts, not only the words.} After case folding, ``data'' appears 3 times and every other term appears once. A top-five list built from that is one real keyword followed by four arbitrary ties, resolved by whatever order the tokenizer happened to produce.

A ranking in which almost everything ties is not a ranking. Report the counts alongside the words so the reader can see how thin the evidence is, and do not present all five as findings.
\end{pitfall}

\section{A taxonomy of the Lab 7 methods}

\begin{aifig}[Where the decision logic comes from]{The nine Lab 7 programs arranged by the
source of their decision logic. Moving right shifts the work from the programmer to the data.
The axis is not a quality ranking: a dictionary lookup beats a trained classifier whenever
the rules are genuinely known.}{fig:nlp-taxonomy}
\begin{tikzpicture}[
  b/.style={rounded corners=2mm,minimum width=4.0cm,minimum height=1.45cm,align=center,font=\small\color{MLNavy}},
  arr/.style={-{Stealth[length=2mm]},very thick,draw=MLMuted}
]
\node[b,draw=MLOrange,fill=MLPaleOrange] (rules)
  {\textbf{Rules and dictionaries}\\\scriptsize NER, grammar correction, lexicon sentiment};
\node[b,draw=MLTeal,fill=MLPaleTeal,right=0.75cm of rules] (stats)
  {\textbf{Text statistics}\\\scriptsize frequency, TF--IDF, keyword counts};
\node[b,draw=MLBlue,fill=MLPaleBlue,right=0.75cm of stats] (learned)
  {\textbf{Trained model}\\\scriptsize Naive Bayes from labeled examples};
\draw[arr] (rules)--(stats);
\draw[arr] (stats)--(learned);
\node[font=\scriptsize\color{MLMuted}] at ($(stats.south)+(0,-0.55)$)
  {increasing reliance on patterns learned from data};
\end{tikzpicture}
\end{aifig}

The spectrum identifies the source of decision logic: programmer rules, corpus statistics, or labeled training data. It is not a quality ranking.

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.25\linewidth}p{0.26\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Example & Main mechanism & What supplies the intelligence \\
\midrule
NER simulation & Exact dictionary matching & Hand-written entity lists \\
Frequency summary & Word counts & Repetition-based sentence score \\
Grammar correction & String replacement & Hand-written correction rules \\
POS-style output & Fixed tag list & Programmer-supplied tags \\
Naive Bayes sentiment & Binary word features + classifier & Labeled examples and manual vocabulary \\
TF--IDF summary & Weighted term representation & Document statistics \\
Rule sentiment & Positive/negative lexicons & Hand-written sentiment lists \\
Language detection & Keyword counts & Language-specific keyword lists \\
Keyword extraction & Bag-of-words counts & Frequency after stop-word/punctuation removal \\
\bottomrule
\end{tabularx}
\end{mltable}

\input{labs/07-nlp-part-b}

\clearpage
\begin{quickcheck}
\begin{enumerate}
  \item Why does the NER simulation miss multi-word entities under its current tokenization rule?
  \item What makes the frequency and TF--IDF summarizers extractive?
  \item Which sentiment example is trained from labeled data, and which uses a fixed lexicon?
  \item What does the language detector do when it receives the keyword match counts?
  \item Why are stop-word removal and punctuation removal useful before the keyword-count step in Code 9?
\end{enumerate}
\end{quickcheck}

\begin{checkanswers}
\begin{enumerate}
  \item A phrase such as \texttt{Barack Obama} cannot equal either single token \texttt{Barack} or \texttt{Obama}.
  \item They select and return an existing input sentence instead of generating a new sentence.
  \item Code 5 trains Naive Bayes from labeled examples; Code 7 counts words in fixed sentiment lexicons.
  \item It selects the language with the largest supported count; the completed version also handles zero matches and ties.
  \item They prevent frequent grammatical words and punctuation marks from occupying the limited keyword list.
\end{enumerate}
\end{checkanswers}

\begin{chapterrecap}
\begin{itemize}
  \item Lab 7 compares rules, text statistics, and a simple classifier.
  \item Frequency and TF--IDF can score existing sentences for extractive summarization.
  \item Sentiment can be implemented as a learned classification problem or as direct lexicon counting.
  \item Preprocessing choices expose limitations such as exact-match tokenization.
\end{itemize}
\end{chapterrecap}
