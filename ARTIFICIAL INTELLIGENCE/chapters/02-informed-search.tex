\chapter{Informed Search: Greedy Best-First Search and A*}
\label{ch:informed}

\chapterguide%
  {Trace BFS, DFS and UCS (\chapref{ch:uninformed}) and explain what a priority queue does.}
  {define a heuristic and compute $g$, $h$ and $f$ for a state; trace Greedy Best-First Search and A$^\star$; decide whether a heuristic is admissible and why that matters; and read the effort-versus-quality trade-off in Lab~2's tables.}

UCS found the shortest Kuala Lumpur--Keflav\'ik route but expanded 1,851 airports without geographical guidance. Lab~2 adds a \term{heuristic} --- an estimate of remaining distance --- and measures its effect on search effort and route quality.

\section{Three quantities on one path}

Every informed method reasons about a frontier state $n$ using up to three numbers:

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}
  >{\centering\arraybackslash\bfseries\leavevmode\color{MLNavy}}p{0.14\linewidth}
  X
  >{\centering\arraybackslash}p{0.20\linewidth}@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Quantity & Meaning & Used by \\
\midrule
$g(n)$ & Exact cost of the cheapest known path from the start to $n$ & UCS, A$^\star$ \\
$h(n)$ & Estimated cost from $n$ to the goal --- the heuristic & GBFS, A$^\star$ \\
$f(n)=g(n)+h(n)$ & Estimated total cost of a solution through $n$ & A$^\star$ \\
\bottomrule
\end{tabularx}
\end{mltable}

\figref{fig:fgh} separates known cost from estimated remaining cost: UCS uses the former, GBFS the latter, and A$^\star$ both.

\begin{aifig}[The three quantities $g$, $h$, and $f$]{$f(n)=g(n)+h(n)$ combines known cost with estimated remaining cost. UCS prioritises $g$, GBFS $h$, and A$^\star$ their sum.}{fig:fgh}
\begin{tikzpicture}[font=\small,
  method/.style={rounded corners=2mm,text width=2.75cm,minimum height=1.02cm,
                 align=center,font=\scriptsize\color{MLNavy},inner sep=1.5mm,
                 line width=0.75pt}]
\node[ainodedone,minimum size=9mm] (S) at (0,0) {S};
\node[ainodehot,minimum size=9mm] (N) at (4,0) {$n$};
\node[ainode,minimum size=9mm,fill=MLPaleTeal,draw=MLGreen] (G) at (8,0) {G};
\draw[aiedge,draw=MLBlue,line width=1.3pt]
  (S) -- (1.15,0.48) -- (2.55,-0.32) -- (N);
\draw[aiedge,draw=MLMuted!55,line width=1.2pt,dashed] (N) -- (G);
\draw[aibrace,draw=MLBlue] (-0.3,0.92) -- (4,0.92);
\draw[aibrace,draw=MLOrange] (4,0.92) -- (8.3,0.92);
\node[ailabel,anchor=south,color=MLBlue,align=center] at (1.85,1.22)
  {$g(n)$ --- exact cost\\from start to $n$};
\node[ailabel,anchor=south,color=MLOrange,align=center] at (6.15,1.22)
  {$h(n)$ --- estimated cost\\from $n$ to goal};
\node[method,draw=MLBlue,fill=MLPaleBlue] (ucs) at (0.75,-1.65)
  {\textbf{UCS}\\priority $g(n)$\\actual cost only};
\node[method,draw=MLOrange,fill=MLPaleOrange] (gbfs) at (4,-1.65)
  {\textbf{GBFS}\\priority $h(n)$\\estimate only};
\node[method,draw=MLGreen,fill=MLPaleTeal] (astar) at (7.25,-1.65)
  {\textbf{A*}\\priority $f(n)=g(n)+h(n)$\\combines both};
\end{tikzpicture}
\end{aifig}

\begin{keyidea}
GBFS asks which frontier state \emph{looks} closest to the goal. A$^\star$ also accounts for the cost already paid and, with a suitable heuristic, guarantees the cheapest path.
\end{keyidea}

\section{The Haversine heuristic}

A heuristic should be cheap to compute and use the same units as path cost. For kilometre-based routes, use the great-circle distance to the goal, whether or not a direct flight exists. The \term{Haversine formula} computes it from latitude--longitude pairs:
\[
h(n)=2R\,\arcsin\!\sqrt{\sin^2\!\tfrac{\Delta\varphi}{2}+\cos\varphi_n\cos\varphi_{\text{goal}}\sin^2\!\tfrac{\Delta\lambda}{2}}
\]
with $R=6371$~km. The same formula gives every edge weight in the graph, so $h$ and $g$ are directly comparable. In Lab~2 the heuristic is a small function that fixes the goal and returns a function of the airport:

\begin{lstlisting}[style=aipython]
def heuristic_to(goal, weight=1.0):
    def h(airport):
        return weight * haversine_km(coords[airport]["latitude"], coords[airport]["longitude"],
                                     coords[goal]["latitude"], coords[goal]["longitude"])
    return h
\end{lstlisting}

\texttt{utils/core.py} accepts any state-to-number callable, so changing the heuristic requires no changes to the search code.

\section{Greedy Best-First Search}

\term{Greedy Best-First Search (GBFS)} prioritises $h(n)$ alone, ignoring the cost already paid. It finds routes in two or three expansions on the airline graph, but need not find cheap ones.

\begin{workedexample}
GBFS expands two airports for \texttt{KUL -> LHR -> KEF}: 12,500~km, 9\,\% above the optimum. London looks close to Iceland, but the first leg alone exceeds the optimal route through Phuket, St Petersburg and Helsinki. Its \texttt{NRT}$\to$\texttt{CPT} route is 6.7\,\% too long; its \texttt{SIN}$\to$\texttt{EZE} route happens to be optimal.
\end{workedexample}

\begin{pitfall}
Because GBFS ignores $g$, a small $h$ can conceal an expensive path. Route quality depends on the graph around the goal.
\end{pitfall}

\section{A* search}

\term{A$^\star$} prioritises $f(n)=g(n)+h(n)$, balancing cost so far against estimated cost ahead. With a consistent $h$, this closed-set implementation returns the cheapest path, usually with far fewer expansions than UCS.

\begin{workedexample}
Suppose frontier state $X$ has $(g,h)=(5,3)$ and $Y$ has $(1,5)$. GBFS chooses $X$ because $3<5$. A$^\star$ chooses $Y$ because $f(Y)=6<f(X)=8$: the cheaper path so far is worth the extra estimated distance.
\end{workedexample}

Implementation is one line away from UCS. In \texttt{utils/core.py} the three algorithms share \texttt{best\_first}; only the priority differs:

\begin{lstlisting}[style=aipython]
def priority(state, g):
    if mode == "ucs":
        return g
    if mode == "gbfs":
        return h(state)
    return g + h(state)            # astar
\end{lstlisting}

\subsection{Admissibility and consistency}

The optimality guarantee rests on one condition. A heuristic is \term{admissible} when it never overestimates the cheapest remaining cost:
\[
h(n)\;\le\;h^\star(n)\quad\text{for every state } n,
\]
where $h^\star(n)$ is the true cost of the cheapest path from $n$ to the goal. A stronger and more convenient condition is \term{consistency}: for every edge $n\to n'$ of cost $c$, $h(n)\le c+h(n')$. Consistency implies admissibility and guarantees that the first time A$^\star$ pops a state, it has found the cheapest path to it --- which is what allows the closed-set implementation in \texttt{utils} to skip re-expansions.

Haversine is consistent by the triangle inequality on a sphere. Lab~2 also runs Dijkstra backwards from each goal to obtain $h^\star$, samples ten airports across the range, and checks $h\le h^\star$. In \figref{fig:admissible}, every sampled point must lie on or below the diagonal.

\begin{aifig}[Checking admissibility state by state]{A heuristic plotted against the true remaining cost. Admissibility requires every point to lie on or below the dashed line. The red points overestimate and void the A$^\star$ guarantee; the Haversine heuristic in Lab~2 produces only green points, with slack of 0 to 23,000~km.}{fig:admissible}
\begin{tikzpicture}
\begin{axis}[
  width=0.62\linewidth, height=5.4cm,
  xlabel={true remaining cost $h^\star(n)$}, ylabel={heuristic $h(n)$},
  xmin=0, xmax=7, ymin=0, ymax=7.6,
  xtick={0,1,...,7}, ytick={0,2,4,6},
  axis lines=left, grid=major, grid style={draw=MLPaleBlue},
  clip=false]
\addplot[draw=MLMuted,dashed,thick] coordinates {(0,0) (7,7)};
\addplot[only marks,mark=*,mark size=2.4pt,MLRed]
  coordinates {(4,7) (3,6) (1,2)};
\addplot[only marks,mark=*,mark size=2.4pt,MLGreen]
  coordinates {(6,4) (2,2) (0,0) (5,3.2) (3.5,1.8)};
\node[font=\scriptsize\color{MLMuted},anchor=west] at (axis cs:4.4,4.1) {$h=h^\star$};
\node[font=\scriptsize\color{MLRed},anchor=west,align=left] at (axis cs:0.3,6.2)
  {above the line:\\overestimates};
\node[font=\scriptsize\color{MLGreen},anchor=west,align=left] at (axis cs:4.6,1.4)
  {on or below:\\admissible};
\end{axis}
\end{tikzpicture}
\end{aifig}

\begin{intuition}
An inadmissible heuristic makes A$^\star$ behave like GBFS: it trusts the estimate too much and commits to a branch before the cost evidence justifies it. Admissibility keeps the $g$ term in charge whenever the two disagree.
\end{intuition}

\subsection{Weighted A*: trading the guarantee for speed}

\term{Weighted A$^\star$} uses $g+w\,h$ with $w>1$, trading the optimality guarantee for more goal-directed search. At $w=1.5$, Lab~2 uses two, three and four expansions, with routes 7.1\,\%, 0\,\% and 0.8\,\% above optimum. Inflating $h$ can discourage useful detours away from the goal.

\section{UCS, GBFS and A* side by side}

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}
  >{\centering\arraybackslash\bfseries\leavevmode\color{MLNavy}}p{0.10\linewidth}
  >{\centering\arraybackslash}p{0.16\linewidth}
  >{\centering\arraybackslash}p{0.22\linewidth}
  X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Method & Priority & Optimal? & Behaviour in Lab~2 \\
\midrule
UCS & $g(n)$ & Yes & Exact but blind: 1,851--3,042 expansions per query \\
GBFS & $h(n)$ & No & 2--3 expansions; routes up to 9\,\% too long \\
A$^\star$ & $g(n)+h(n)$ & Yes, if $h$ consistent & Same cost as UCS with 96\,\% fewer expansions \\
Weighted A$^\star$ & $g(n)+1.5\,h(n)$ & No & 2--4 expansions; routes up to 7\,\% too long \\
\bottomrule
\end{tabularx}
\end{mltable}

\figref{fig:expansion} visualises these expansion patterns on a shared grid.

\begin{aifig}[Cells expanded by UCS, GBFS, and A*]{Expansion on an identical grid: UCS spreads widely, GBFS heads towards the goal but enters a dead end, and A$^\star$ finds the optimal path through a narrower corridor.}{fig:expansion}
\begin{tikzpicture}[font=\small]
\def\W{0.46}
\def\wallmark{W}
\newcommand{\gridpanel}[3]{%
  \begin{scope}[xshift=#1 cm]
    \node[font=\bfseries\color{MLNavy}] at (1.38,1.0) {#2};
    \foreach \row [count=\ri from 0] in #3{
      \foreach \c [count=\ci from 0] in \row{
        \ifx\c\wallmark
          \node[aiwall,minimum width=\W cm,minimum height=\W cm,inner sep=0pt]
            at (\W*\ci,-\W*\ri) {};
        \else
          \ifx\c\expmark
            \node[draw=MLBlue!45,fill=MLBlue!38,minimum width=\W cm,minimum height=\W cm,
                  inner sep=0pt] at (\W*\ci,-\W*\ri) {};
          \else
            \node[draw=MLMuted!30,fill=white,minimum width=\W cm,minimum height=\W cm,
                  inner sep=0pt] at (\W*\ci,-\W*\ri) {};
          \fi
        \fi
      }
    }
    \node[ainodehot,minimum size=5mm,font=\tiny\bfseries] at (0,0) {S};
    \node[ainodedone,minimum size=5mm,font=\tiny\bfseries] at (6*\W,-4*\W) {G};
  \end{scope}}
\def\expmark{E}
\gridpanel{0}{UCS (sorts on $g$)}{{%
  {E,E,E,E,E,E,E},
  {E,E,W,E,E,E,E},
  {E,E,W,E,E,E,E},
  {E,E,W,E,E,E,E},
  {E,E,E,E,E,E,E}}}
\node[ailabel,anchor=north,align=center] at (1.38,-2.35) {31 cells expanded\\optimal path};
\gridpanel{4.3}{GBFS (sorts on $h$)}{{%
  {E,E,E,.,.,.,.},
  {E,E,W,.,.,.,.},
  {E,E,W,.,.,.,.},
  {E,E,W,E,E,E,E},
  {E,E,E,E,E,E,E}}}
\node[xshift=4.3cm,ailabel,anchor=north,align=center] at (1.38,-2.35)
  {21 cells expanded\\path not guaranteed optimal};
\gridpanel{8.6}{A* (sorts on $g+h$)}{{%
  {E,E,.,.,.,.,.},
  {E,E,W,.,.,.,.},
  {E,E,W,.,.,.,.},
  {E,E,W,E,E,E,.},
  {E,E,E,E,E,E,E}}}
\node[xshift=8.6cm,ailabel,anchor=north,align=center] at (1.38,-2.35)
  {19 cells expanded\\optimal path};
\end{tikzpicture}
\end{aifig}

\begin{keyidea}
A$^\star$ combines low effort with optimality here because Haversine is consistent. UCS explores without guidance; GBFS reduces effort but gives up the guarantee.
\end{keyidea}

\section{Reading the lab's evidence}

\texttt{metrics.csv} records cost, hops, expansions, peak frontier, runtime and the \term{optimality gap}: percentage cost above UCS. A$^\star$ has zero gap on every query; positive GBFS or weighted A$^\star$ gaps quantify the cost of speed. \texttt{heuristic\_check.csv} compares $h$ with $h^\star$ for thirty samples. Any failed check would invalidate the admissibility claim.

\input{labs/02-informed-search}

\begin{quickcheck}
\begin{enumerate}
  \item Which quantity does GBFS ignore that A$^\star$ uses, and which failure in Lab~2 does that explain?
  \item Frontier states $X$ and $Y$ have $(g,h)=(8,2)$ and $(3,6)$. Which does UCS expand first? GBFS? A$^\star$?
  \item Why must $h$ be expressed in the same units as the edge costs?
  \item Lab~2 computes $h^\star$ by running Dijkstra on the \emph{reversed} graph from the goal. Why reversed?
  \item Weighted A$^\star$ with $w=1.5$ changed the cost for KUL$\to$KEF but not for SIN$\to$EZE. What property of the SIN$\to$EZE route explains this?
\end{enumerate}
\end{quickcheck}
