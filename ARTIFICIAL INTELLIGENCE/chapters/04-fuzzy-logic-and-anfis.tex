\chapter{Fuzzy Logic and ANFIS}
\label{ch:fuzzy}

\chapterguide%
  {Know Boolean true/false logic and be comfortable with NumPy arrays and a scikit-learn train/test split.}
  {express a graded concept as a membership function; trace the four stages of a fuzzy inference system on one reading; distinguish Mamdani from Sugeno rules; explain what ANFIS learns and how; and evaluate a learned model without leaking test data into its selection.}

Lab~4 uses one air-quality dataset for two tasks. A hand-written \term{fuzzy controller} maps temperature and humidity to ventilation demand. An \term{ANFIS} regressor predicts carbon monoxide from two gas sensors, fitting its rule parameters to 5,000 measurements. We examine the controller first, then the learner.

\section{From crisp truth to fuzzy membership}

Classical logic uses true or false; fuzzy logic assigns a \term{degree of membership} $0\le\mu(x)\le1$. For ``hot'', a crisp threshold jumps at $30^\circ$, while fuzzy membership changes gradually (\figref{fig:crisp-fuzzy}). Gradual transitions reduce abrupt control changes near a threshold.

\begin{aifig}[Crisp versus fuzzy membership]{``Hot'' as a crisp set and as a fuzzy set. The crisp version forces a decision at one point; the fuzzy version reports a degree, so $35^\circ$ is hot to degree $0.5$.}{fig:crisp-fuzzy}
\begin{tikzpicture}
\begin{axis}[
  name=crisp, width=6.6cm, height=4.2cm,
  xlabel={temperature ($^\circ$C)}, ylabel={$\mu(x)$},
  title={Crisp set: ``hot''},
  xmin=0, xmax=60, ymin=-0.08, ymax=1.18,
  ytick={0,0.5,1}, xtick={0,20,30,40,60},
  axis lines=left, grid=major, grid style={draw=MLPaleBlue}]
\addplot[MLRed,thick] coordinates {(0,0) (30,0)};
\addplot[MLRed,thick] coordinates {(30,1) (60,1)};
\addplot[MLRed,dashed,thin] coordinates {(30,0) (30,1)};
\addplot[only marks,mark=*,MLRed,mark size=1.4pt] coordinates {(30,1)};
\addplot[only marks,mark=o,MLRed,mark size=1.4pt] coordinates {(30,0)};
\end{axis}
\begin{axis}[
  at={(crisp.right of south east)}, xshift=1.15cm, anchor=left of south west,
  width=6.6cm, height=4.2cm,
  xlabel={temperature ($^\circ$C)}, ylabel={$\mu(x)$},
  title={Fuzzy set: ``hot''},
  xmin=0, xmax=60, ymin=-0.08, ymax=1.18,
  ytick={0,0.5,1}, xtick={0,20,30,40,60},
  axis lines=left, grid=major, grid style={draw=MLPaleBlue}]
\addplot[MLGreen,thick] coordinates {(0,0) (25,0) (45,1) (60,1)};
\addplot[MLOrange,dashed,thin] coordinates {(35,0) (35,0.5)};
\addplot[MLOrange,dashed,thin] coordinates {(0,0.5) (35,0.5)};
\addplot[only marks,mark=*,MLOrange,mark size=1.6pt] coordinates {(35,0.5)};
\node[font=\scriptsize\color{MLOrange}] at (axis cs:46,0.38) {$\mu(35)=0.5$};
\end{axis}
\end{tikzpicture}
\end{aifig}

\begin{intuition}
Fuzzy logic attaches numbers to graded words such as ``cold'', ``humid'' and ``fast'', making verbal rules executable.
\end{intuition}

\section{Membership functions}

A \term{membership function} maps a crisp value to a degree. The lab uses triangles, the simplest shape that rises to 1 and falls back to 0, defined by three points $a\le b\le c$:
\[
\mu(x)=\max\!\Big(0,\;\min\!\Big(\frac{x-a}{b-a},\;\frac{c-x}{c-b}\Big)\Big).
\]
The outer sets are \term{shoulders}: $a=b$ gives membership~1 below the peak; $b=c$ gives membership~1 above it. Extreme readings therefore still activate rules. The temperature sets are cold $(0,0,18)$, mild $(12,22,32)$ and hot $(26,45,45)$ (\figref{fig:membership}).

\begin{aifig}[The lab's temperature memberships]{Cold and hot are shoulders; mild is triangular. At $15^\circ$C, cold membership is $0.17$ and mild membership $0.30$, activating both rules.}{fig:membership}
\begin{tikzpicture}
\begin{axis}[
  width=0.88\linewidth, height=5.6cm,
  xmin=0, xmax=45, ymin=0, ymax=1.08,
  xlabel={Temperature ($^\circ$C)}, ylabel={Membership},
  xtick={0,12,15,18,22,26,32,45}, ytick={0,0.17,0.3,0.5,1},
  yticklabels={0,0.17,0.30,0.5,1},
  legend columns=3, legend style={at={(0.5,-0.26)},anchor=north},
  clip=false]
\addplot[draw=MLBlue,very thick] coordinates {(0,1) (18,0) (45,0)};
\addlegendentry{cold $(0,0,18)$}
\addplot[draw=MLTeal,very thick] coordinates {(0,0) (12,0) (22,1) (32,0) (45,0)};
\addlegendentry{mild $(12,22,32)$}
\addplot[draw=MLOrange,very thick] coordinates {(0,0) (26,0) (45,1)};
\addlegendentry{hot $(26,45,45)$}
\addplot[draw=MLRed,dashed,thick] coordinates {(15,0) (15,1.02)};
\addplot[only marks,mark=*,mark size=2.2pt,MLBlue] coordinates {(15,0.167)};
\addplot[only marks,mark=*,mark size=2.2pt,MLTeal] coordinates {(15,0.3)};
\node[font=\scriptsize\color{MLBlue},anchor=west] at (axis cs:15.6,0.12) {$\mu_{\text{cold}}(15)=0.17$};
\node[font=\scriptsize\color{MLTeal},anchor=west] at (axis cs:15.6,0.36) {$\mu_{\text{mild}}(15)=0.30$};
\end{axis}
\end{tikzpicture}
\end{aifig}

The lab implements the triangle in five lines, with the shoulder cases written out explicitly:

\begin{lstlisting}[style=aipython]
def trimf(x, a, b, c):
    x = np.asarray(x, dtype=float)
    left = np.ones_like(x) if a == b else np.clip((x - a) / (b - a), 0, 1)
    right = np.ones_like(x) if b == c else np.clip((c - x) / (c - b), 0, 1)
    return np.minimum(left, right)
\end{lstlisting}

\begin{pitfall}
A \texttt{trimf} that assumes $a<b<c$ divides by zero for shoulders or returns incorrect zero memberships. This can disable the ``cold'' and ``dry'' rules, leaving only a fallback output. Question~3 catches this by checking five points with known answers.
\end{pitfall}

\section{The four stages of a fuzzy inference system}

A \term{fuzzy inference system} (FIS) maps crisp inputs to a crisp output through fuzzy intermediate stages (\figref{fig:fis-pipeline}).

\begin{aifig}[The four stages of a fuzzy inference system]{Inputs of $15^\circ$C and 45\,\% become membership degrees, activate rules and yield one crisp ventilation demand.}{fig:fis-pipeline}
\begin{tikzpicture}[font=\small]
\node[aibox,minimum width=1.65cm,minimum height=0.9cm,fill=MLPaleGray,draw=MLMuted,
      font=\scriptsize\color{MLNavy}] (in) at (0,0) {crisp in\\\textbf{$15^\circ$C, 45\,\%}};
\node[aibox,minimum width=2.25cm,minimum height=0.9cm,font=\scriptsize\color{MLNavy}]
  (fz) at (2.5,0) {\textbf{Fuzzification}\\$\to$ degrees};
\node[aibox,minimum width=2.25cm,minimum height=0.9cm,font=\scriptsize\color{MLNavy}]
  (rb) at (5.2,0) {\textbf{Rule base}\\IF--THEN};
\node[aibox,minimum width=2.25cm,minimum height=0.9cm,font=\scriptsize\color{MLNavy}]
  (ie) at (7.9,0) {\textbf{Inference}\\combine rules};
\node[aibox,minimum width=2.25cm,minimum height=0.9cm,font=\scriptsize\color{MLNavy}]
  (df) at (10.6,0) {\textbf{Defuzzification}\\$\to$ one number};
\node[aibox,minimum width=1.55cm,minimum height=0.9cm,fill=MLPaleTeal,draw=MLGreen,
      font=\scriptsize\color{MLNavy}] (out) at (13.0,0) {crisp out\\\textbf{40.4\,\%}};
\foreach \a/\b in {in/fz, fz/rb, rb/ie, ie/df, df/out}{\draw[aiarrow] (\a)--(\b);}
\node[ailabel,anchor=north,align=center,color=MLOrange] at (2.5,-0.6)
  {cold $0.17$, mild $0.30$\\comfortable $0.75$};
\node[ailabel,anchor=north,align=center,color=MLOrange] at (5.2,-0.6)
  {low fires $0.17$\\medium fires $0.30$};
\node[ailabel,anchor=north,align=center,color=MLOrange] at (7.9,-0.6)
  {clipped sets\\joined by max};
\node[ailabel,anchor=north,align=center,color=MLOrange] at (10.6,-0.6)
  {centroid of\\that shape};
\draw[aibrace] (1.4,0.68) -- (11.8,0.68);
\node[ailabel,anchor=south,color=MLNavy] at (6.6,1.0)
  {everything inside this brace is fuzzy; the two ends are not};
\end{tikzpicture}
\end{aifig}

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.20\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Stage & What happens, and where it is in the lab code \\
\midrule
Fuzzification & Each crisp input is turned into one degree per set: \texttt{degree(T\_range, mf, temperature)} for every set. \\
Rule base & IF--THEN rules over the set names. AND is \texttt{min}; rules with the same conclusion are joined with \texttt{max}. \\
Inference & Each output set is clipped at its rule's strength; the clipped sets are joined with \texttt{np.maximum.reduce}. \\
Defuzzification & The centroid of the joined shape: $\sum x\,\mu(x)\,/\,\sum\mu(x)$ over the output range. \\
\bottomrule
\end{tabularx}
\end{mltable}

\subsection{The rule base}

The controller has six rules, written so that heat or humidity raises the demand and cool, dry conditions lower it:

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.12\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Output & Fires when \\
\midrule
low & cold AND dry, \quad or \quad cold AND comfortable \\
medium & mild AND dry, \quad or \quad mild AND comfortable, \quad or \quad cold AND humid \\
high & hot, \quad or \quad humid \\
\bottomrule
\end{tabularx}
\end{mltable}

The three-line rule base uses \texttt{min} for AND (the weakest condition) and \texttt{max} for alternatives (the strongest support).

\begin{lstlisting}[style=aipython]
low = max(min(t["cold"], h["dry"]), min(t["cold"], h["comfortable"]))
medium = max(min(t["mild"], h["dry"]), min(t["mild"], h["comfortable"]), min(t["cold"], h["humid"]))
high = max(t["hot"], h["humid"])
\end{lstlisting}

\subsection{Defuzzification by centroid}

The rules produce a fuzzy output set; the fan needs a number. At $15^\circ$C and 45\,\%, clip low at $0.17$ and medium at $0.30$, combine them, and report the centroid (\figref{fig:defuzz}).

\begin{aifig}[Defuzzification by centroid]{Defuzzifying the $15^\circ$C, 45\,\% reading. Low is clipped at $0.17$ and medium at $0.30$; high does not fire. The centre of area of their union is $40.4\,\%$. A stronger medium rule would pull the centroid to the right.}{fig:defuzz}
\begin{tikzpicture}
\begin{axis}[
  width=0.82\linewidth, height=5.4cm,
  xmin=0, xmax=100, ymin=0, ymax=1.12,
  xlabel={Ventilation demand (\%)}, ylabel={Membership},
  xtick={0,25,40.4,50,75,100}, xticklabels={0,25,40.4,50,75,100},
  ytick={0,0.17,0.3,0.6,1}, yticklabels={0,0.17,0.30,0.6,1},
  axis lines=left, grid=major, grid style={draw=MLPaleBlue},
  clip=false]
\addplot[draw=MLMuted!45,thin] coordinates {(0,1) (45,0)};
\addplot[draw=MLMuted!45,thin] coordinates {(25,0) (50,1) (75,0)};
\addplot[draw=MLMuted!45,thin] coordinates {(55,0) (100,1)};
\addplot[draw=MLTeal,very thick,fill=MLPaleTeal,fill opacity=0.75]
  coordinates {(0,0) (0,0.167) (29.2,0.167) (32.5,0.3) (67.5,0.3) (75,0) (0,0)};
\addplot[MLRed,dashed,thick] coordinates {(40.4,0) (40.4,0.62)};
\addplot[only marks,mark=*,mark size=2.4pt,MLRed] coordinates {(40.4,0.3)};
\node[font=\scriptsize\color{MLRed},anchor=south] at (axis cs:40.4,0.65) {centroid $=40.4$};
\node[font=\scriptsize\color{MLBlue},anchor=west] at (axis cs:2,0.25) {low clipped at $0.17$};
\node[font=\scriptsize\color{MLTeal},anchor=west] at (axis cs:44,0.38) {medium clipped at $0.30$};
\node[font=\scriptsize\color{MLMuted},anchor=west] at (axis cs:78,0.5) {high: not fired};
\end{axis}
\end{tikzpicture}
\end{aifig}

\begin{workedexample}
At $15^\circ$C and 45\,\% relative humidity the memberships are cold $0.17$, mild $0.30$, comfortable $0.75$ and all others~$0$. The rule strengths follow:
\[
\text{low}=\min(0.17,0.75)=0.17,\qquad
\text{medium}=\min(0.30,0.75)=0.30,\qquad
\text{high}=0.
\]
Clipping the low and medium output sets at those strengths and taking the centroid of their union gives a ventilation demand of $40.4\,\%$: a little below ``medium'', pulled down by the cold rule.
\end{workedexample}

\section{Validating a controller}

Check the controller against expectations defined \emph{before} viewing its output. Lab~4 uses two checks:

\begin{description}
  \item[Fixed points.] Five operating conditions with a qualitative expected answer: $(10^\circ, 30\,\%)$ should be low, $(20^\circ, 50\,\%)$ medium, $(35^\circ, 50\,\%)$, $(25^\circ, 80\,\%)$ and $(40^\circ, 80\,\%)$ high. The controller returns 18.9, 50.0, 82.3, 83.8 and 84.2\,\%. Each lands in its band.
  \item[The control surface.] Plot output over a temperature--humidity grid. It should rise towards hot, humid conditions without cliffs or unexplained plateaus; these can reveal membership gaps or inactive rules.
\end{description}

\begin{keyidea}
Check known inputs and the full control surface. One plausible output can hide inactive rules.
\end{keyidea}

\section{Mamdani and Sugeno systems}

A \term{Mamdani} rule outputs a fuzzy \emph{set}, which is combined with others and defuzzified. A \term{Sugeno} rule outputs a constant or function such as $0.6\,x_1+0.2\,x_2+1.3$; its final output is the firing-strength-weighted average of these functions (\figref{fig:mamdani-sugeno}).

\begin{aifig}[Mamdani versus Sugeno rule output]{Mamdani combines fuzzy output sets and defuzzifies them. Sugeno averages numeric rule outputs by firing strength, enabling ANFIS training.}{fig:mamdani-sugeno}
\begin{tikzpicture}[font=\small]
\begin{scope}
\node[font=\bfseries\color{MLNavy}] at (2.5,2.2) {Mamdani};
\node[aibox,minimum width=4.6cm,font=\scriptsize\color{MLNavy}] (r1) at (2.5,1.5)
  {IF sensor is High THEN CO is \textbf{High}};
\node[ailabel,anchor=north,color=MLOrange] at (2.5,1.05) {\textbf{High} is a fuzzy set};
\begin{scope}[xshift=1.35cm,yshift=-0.55cm,x=0.6cm,y=0.6cm]
\draw[draw=MLMuted!60,-{Stealth[length=1.6mm]}] (-0.2,0)--(4.0,0);
\draw[draw=MLMuted!60,-{Stealth[length=1.6mm]}] (0,-0.2)--(0,1.35);
\draw[very thick,draw=MLOrange] (0.4,0)--(1.8,1.1)--(3.2,0);
\fill[MLOrange,opacity=0.2] (0.4,0)--(1.8,1.1)--(3.2,0)--cycle;
\end{scope}
\node[ailabel,anchor=north,align=center] at (2.5,-1.35)
  {needs joining, then a centroid};
\end{scope}
\begin{scope}[xshift=7.6cm]
\node[font=\bfseries\color{MLNavy}] at (2.5,2.2) {Sugeno};
\node[aibox,minimum width=5.1cm,font=\scriptsize\color{MLNavy},
      fill=MLPaleTeal,draw=MLGreen] (r2) at (2.5,1.5)
  {IF sensor is High THEN CO $=p_0+p_1x_1+p_2x_2$};
\node[ailabel,anchor=north,color=MLGreen] at (2.5,1.05) {the consequent is a formula};
\node[aicell,minimum width=3.2cm,minimum height=0.65cm,fill=MLPaleTeal,draw=MLGreen,
      font=\scriptsize\bfseries] at (2.5,0.15) {output $=\sum_r \bar w_r\,f_r(x)$};
\node[ailabel,anchor=north,align=center] at (2.5,-1.35)
  {weighted average, no centroid};
\end{scope}
\end{tikzpicture}
\end{aifig}

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.15\linewidth}XX@{}}
\toprule
\rowcolor{MLPaleBlue!92}
 & Mamdani & Sugeno \\
\midrule
Rule conclusion & A fuzzy set over the output & A function of the inputs \\
Output & Centroid of the joined, clipped sets & Weighted average of the rule functions \\
Strength & Readable: every conclusion is a word & Trainable: every conclusion is a set of coefficients \\
Used in Lab~4 by & The ventilation controller & ANFIS \\
\bottomrule
\end{tabularx}
\end{mltable}

The Sugeno form is what makes learning possible. Given the firing strengths, the output is \emph{linear} in the consequent coefficients $p$, so the best coefficients for a training set can be found by ordinary least squares in one step.

\section{ANFIS: a fuzzy system that learns}

An \term{Adaptive Neuro-Fuzzy Inference System} (ANFIS) expresses a Sugeno FIS as a five-layer network with learnable parameters (\figref{fig:anfis}). The inference structure stays the same; data determine the parameters.

\begin{aifig}[The five layers of an ANFIS network]{ANFIS with two inputs and two memberships each. Layers 1 and 4 contain trainable parameters; layers 2, 3 and 5 perform fixed arithmetic.}{fig:anfis}
\begin{tikzpicture}[font=\small,
  L/.style={circle,draw=MLBlue,fill=MLPaleBlue,minimum size=7.5mm,
            font=\scriptsize\color{MLNavy},inner sep=0.5pt},
  T/.style={circle,draw=MLOrange,fill=MLPaleOrange,minimum size=7.5mm,line width=0.9pt,
            font=\scriptsize\color{MLNavy},inner sep=0.5pt}]
\def\dx{2.45}
\node[ailabel,font=\small\bfseries\color{MLNavy}] (x) at (-\dx,0.75) {$x_1$};
\node[ailabel,font=\small\bfseries\color{MLNavy}] (y) at (-\dx,-0.75) {$x_2$};
\node[T] (A1) at (0,1.35) {$A_1$};
\node[T] (A2) at (0,0.45) {$A_2$};
\node[T] (B1) at (0,-0.45) {$B_1$};
\node[T] (B2) at (0,-1.35) {$B_2$};
\node[L] (P1) at (\dx,0.9) {$\Pi$};
\node[L] (P2) at (\dx,-0.9) {$\Pi$};
\node[L] (N1) at (2*\dx,0.9) {$N$};
\node[L] (N2) at (2*\dx,-0.9) {$N$};
\node[T] (C1) at (3*\dx,0.9) {$f_1$};
\node[T] (C2) at (3*\dx,-0.9) {$f_2$};
\node[L] (S) at (4*\dx,0) {$\Sigma$};
\node[ailabel,font=\small\bfseries\color{MLNavy}] (out) at (4*\dx+1.15,0) {output};
\foreach \t in {A1,A2}{\draw[aiptr] (x)--(\t);}
\foreach \t in {B1,B2}{\draw[aiptr] (y)--(\t);}
\draw[aiptr] (A1)--(P1); \draw[aiptr] (B1)--(P1);
\draw[aiptr] (A2)--(P2); \draw[aiptr] (B2)--(P2);
\draw[aiptr] (P1)--(N1); \draw[aiptr] (P2)--(N2);
\draw[aiptr] (P1)--(N2); \draw[aiptr] (P2)--(N1);
\draw[aiptr] (N1)--(C1); \draw[aiptr] (N2)--(C2);
\draw[aiptr] (C1)--(S);  \draw[aiptr] (C2)--(S);
\draw[aiptr] (S)--(out);
\foreach \i/\lbl in {0/{1. fuzzify}, 1/{2. rule\\strength}, 2/{3. normalise},
                     3/{4. rule\\output}, 4/{5. aggregate}}{
  \node[ailabel,anchor=north,align=center] at (\i*\dx,-1.95) {\lbl};
}
\node[ailabel,anchor=west,color=MLOrange] at (-\dx-0.55,-3.15)
  {shaded nodes hold the parameters ANFIS trains; the rest is fixed arithmetic};
\end{tikzpicture}
\end{aifig}

The lab's \texttt{SugenoANFIS} class is this diagram in code. With $m$ Gaussian memberships per input and two inputs there are $m^2$ rules, one for every combination of an $A$ set with a $B$ set:

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.13\linewidth}p{0.36\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Layer & Computes & Parameters \\
\midrule
1 & Gaussian membership $\mu(x)=\exp\!\big(-\tfrac12\big(\tfrac{x-c}{\sigma}\big)^2\big)$ per set & centres $c$, widths $\sigma$ --- \emph{premise} parameters \\
2 & Rule strength $w_r$: the product of one membership per input & none \\
3 & Normalised strength $\bar w_r=w_r/\sum_k w_k$ & none \\
4 & Rule output $\bar w_r\,(p_{r0}+p_{r1}x_1+p_{r2}x_2)$ & coefficients $p$ --- \emph{consequent} parameters \\
5 & Sum over rules & none \\
\bottomrule
\end{tabularx}
\end{mltable}

Layers 1--3 are the method \texttt{\_weights}, layer~4 is \texttt{\_design}, layer~5 is the matrix product in \texttt{predict}.

\subsection{Hybrid learning}

Hybrid learning fits the two parameter groups differently:

\begin{description}
  \item[Consequents by least squares.] With premises fixed, output is linear in $p$. \texttt{\_solve\_outputs} solves the linear system with a small ridge term for numerical stability.
  \item[Premises by numerical optimisation.] SciPy\textquotesingle s L-BFGS-B optimises centres and widths against training MSE, re-solving consequents at each evaluation and bounding widths.
\end{description}

Re-solving consequents leaves only $2m$ centres and $2m$ widths to optimise, rather than an additional $3m^2$ coefficients.

\begin{keyidea}
Use a hand-written fuzzy system when the rules are the knowledge you want to encode and audit. Use ANFIS when data should decide the numbers and the rule structure is only a flexible, interpretable function family.
\end{keyidea}

\section{Evaluating a learned model honestly}

ANFIS is a regressor, and it is judged like one: by its error on data it never saw. Three rules in the lab protect that judgement.

\begin{description}
  \item[Split once, before anything else.] 7,344 complete rows are divided 70/15/15 into training, validation and test with a fixed seed, and the assignment is saved to \texttt{split.csv}. The test rows are read once, in the final question.
  \item[Fit the scaler on training rows only.] Use \texttt{StandardScaler().fit(X[train])}, then \texttt{transform(X)}. Fitting on all rows would leak validation and test statistics.
  \item[Select on validation, report on test.] Two ANFIS sizes are trained. The one with the lower validation RMSE is chosen; its test RMSE is then reported once, next to the linear baseline's.
\end{description}

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.26\linewidth}>{\centering\arraybackslash}X>{\centering\arraybackslash}X>{\centering\arraybackslash}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Model & Validation RMSE & Test RMSE & Test MAE \\
\midrule
Linear regression & 0.510 & 0.537 & 0.382 \\
ANFIS, 2 sets per input & 0.455 & 0.472 & 0.326 \\
ANFIS, 3 sets per input (selected) & 0.449 & 0.467 & 0.325 \\
\bottomrule
\end{tabularx}
\end{mltable}

The selected ANFIS lowers test RMSE by 13\,\% relative to the linear baseline. The predicted-versus-actual plot shows where the gain comes from: the sensor response is nonlinear at high CO, where a straight line under-predicts and a fuzzy blend of local linear rules does not.

\begin{pitfall}
\textbf{Do not select on test RMSE.} Choosing the better of two test results makes the reported score a selected best result, not an unbiased estimate on new data.
\end{pitfall}

A random split of hourly readings ignores time order and sensor drift, so it may overstate long-term performance. Also, the controller encodes a policy while the regressor fits measurements; lower RMSE does not make one a substitute for the other.

\input{labs/04-fuzzy-anfis}

\begin{quickcheck}
\begin{enumerate}
  \item A temperature of $28^\circ$C: which of the lab's three temperature sets have non-zero membership, and roughly what are the degrees?
  \item Why is AND implemented with \texttt{min} and the joining of rules with \texttt{max}?
  \item Which single change turns the Mamdani controller into a Sugeno one, and why does that change make least-squares training possible?
  \item ANFIS with three sets per input and two inputs has how many rules, and how many consequent coefficients?
  \item The scaler in Lab~4 is fitted on the training rows only. What would be wrong with the reported test RMSE if it had been fitted on all rows?
\end{enumerate}
\end{quickcheck}
