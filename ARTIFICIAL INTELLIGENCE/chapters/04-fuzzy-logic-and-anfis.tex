\chapter{Fuzzy Logic and ANFIS}
\label{ch:fuzzy}

\chapterguide%
  {Know basic true/false Boolean logic and be able to run Python scripts.}
  {explain fuzzy truth values, identify the four components of a Fuzzy Inference System (FIS), distinguish Mamdani and Sugeno systems, explain the Adaptive Neuro-Fuzzy Inference System (ANFIS), and build/evaluate the Python examples from Lab 4.}

Lab 4 introduces \term{approximate reasoning}: fuzzy logic represents concepts such as ``cold'' or ``hot'' with degrees between 0 and 1.

\section{From crisp truth to fuzzy membership}

In classical logic, a statement is either false \((0)\) or true \((1)\). In fuzzy logic, a degree can lie anywhere in the interval
\[
0\leq \mu(x)\leq 1.
\]
For example, ``the water is hot'' may have membership \(0.7\).

\begin{intuition}
Human descriptions often use graded language: somewhat cold, very fast, mostly full. Fuzzy logic turns that kind of gradual description into numerical membership values that a rule system can process.
\end{intuition}

\figref{fig:crisp-fuzzy} shows the difference. A crisp set has a cliff: not hot at
$29.9^\circ$, fully hot at $30.0^\circ$. A fuzzy set replaces it with a ramp, so a small
change in temperature produces a small change in membership. That continuity is the practical
advantage: it stops a controller juddering when a reading hovers near a threshold.

\begin{aifig}[Crisp versus fuzzy membership]{``Hot'' as a crisp set and as a fuzzy set. The crisp version forces a decision at one point; the fuzzy version reports a degree, so $35^\circ$ is hot to degree $0.5$.}{fig:crisp-fuzzy}
\begin{tikzpicture}
\begin{axis}[
  name=crisp, width=6.6cm, height=4.2cm,
  xlabel={temperature ($^\circ$C)}, ylabel={$\mu(x)$},
  title={Crisp set: ``hot''},
  xmin=0, xmax=60, ymin=-0.08, ymax=1.18,
  ytick={0,0.5,1}, xtick={0,20,30,40,60},
  axis lines=left, grid=major, grid style={draw=MLPaleBlue}]
\addplot[MLRed,thick] coordinates {(0,0) (30,0)};
\addplot[MLRed,thick] coordinates {(30,1) (60,1)};
\addplot[MLRed,dashed,thin] coordinates {(30,0) (30,1)};
\addplot[only marks,mark=*,MLRed,mark size=1.4pt] coordinates {(30,1)};
\addplot[only marks,mark=o,MLRed,mark size=1.4pt] coordinates {(30,0)};
\end{axis}
\begin{axis}[
  at={(crisp.right of south east)}, xshift=1.15cm, anchor=left of south west,
  width=6.6cm, height=4.2cm,
  xlabel={temperature ($^\circ$C)}, ylabel={$\mu(x)$},
  title={Fuzzy set: ``hot''},
  xmin=0, xmax=60, ymin=-0.08, ymax=1.18,
  ytick={0,0.5,1}, xtick={0,20,30,40,60},
  axis lines=left, grid=major, grid style={draw=MLPaleBlue}]
\addplot[MLGreen,thick] coordinates {(0,0) (25,0) (45,1) (60,1)};
\addplot[MLOrange,dashed,thin] coordinates {(35,0) (35,0.5)};
\addplot[MLOrange,dashed,thin] coordinates {(0,0.5) (35,0.5)};
\addplot[only marks,mark=*,MLOrange,mark size=1.6pt] coordinates {(35,0.5)};
\node[font=\scriptsize\color{MLOrange}] at (axis cs:46,0.38) {$\mu(35)=0.5$};
\end{axis}
\end{tikzpicture}
\end{aifig}

\section{Why fuzzy logic is used}

Fuzzy logic supports:
\begin{itemize}
  \item handle uncertainty, imprecision, and approximate reasoning;
  \item support human-like linguistic rules;
  \item build decision and control systems for applications such as diagnosis and pattern recognition.
\end{itemize}

\section{The four components of a Fuzzy Inference System}

\begin{aifig}[The four stages of a fuzzy inference system]{A fuzzy inference system traced on one reading. A crisp $35^\circ$ enters, becomes membership degrees, fires the rules those degrees activate, and collapses back to one fan speed. Fuzziness exists only inside; both ends are ordinary numbers.}{fig:fis-pipeline}
\begin{tikzpicture}[font=\small]
\node[aibox,minimum width=1.55cm,minimum height=0.9cm,fill=MLPaleGray,draw=MLMuted,
      font=\scriptsize\color{MLNavy}] (in) at (0,0) {crisp in\\\textbf{$35^\circ$}};
\node[aibox,minimum width=2.25cm,minimum height=0.9cm,font=\scriptsize\color{MLNavy}]
  (fz) at (2.45,0) {\textbf{Fuzzification}\\$\to$ degrees};
\node[aibox,minimum width=2.25cm,minimum height=0.9cm,font=\scriptsize\color{MLNavy}]
  (rb) at (5.15,0) {\textbf{Rule base}\\IF--THEN};
\node[aibox,minimum width=2.25cm,minimum height=0.9cm,font=\scriptsize\color{MLNavy}]
  (ie) at (7.85,0) {\textbf{Inference}\\combine rules};
\node[aibox,minimum width=2.25cm,minimum height=0.9cm,font=\scriptsize\color{MLNavy}]
  (df) at (10.55,0) {\textbf{Defuzzification}\\$\to$ one number};
\node[aibox,minimum width=1.55cm,minimum height=0.9cm,fill=MLPaleTeal,draw=MLGreen,
      font=\scriptsize\color{MLNavy}] (out) at (12.95,0) {crisp out\\\textbf{6.2}};
\foreach \a/\b in {in/fz, fz/rb, rb/ie, ie/df, df/out}{\draw[aiarrow] (\a)--(\b);}
% under-labels showing the actual values flowing
\node[ailabel,anchor=north,align=center,color=MLOrange] at (2.45,-0.6)
  {Warm $0.5$\\Hot $0.5$};
\node[ailabel,anchor=north,align=center,color=MLOrange] at (5.15,-0.6)
  {R2 fires $0.5$\\R3 fires $0.5$};
\node[ailabel,anchor=north,align=center,color=MLOrange] at (7.85,-0.6)
  {aggregated\\output set};
\node[ailabel,anchor=north,align=center,color=MLOrange] at (10.55,-0.6)
  {centroid of\\that set};
\draw[aibrace] (1.35,0.68) -- (11.75,0.68);
\node[ailabel,anchor=south,color=MLNavy] at (6.55,1.0)
  {everything inside this brace is fuzzy; the two ends are not};
\end{tikzpicture}
\end{aifig}

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.20\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Component & Description from the laboratory material \\
\midrule
Fuzzification & Converts crisp inputs to fuzzy values using membership functions. \\
Rule base & Stores IF--THEN rules, such as IF temperature is high THEN fan speed is high. \\
Inference engine & Applies the rules to the fuzzy inputs to obtain fuzzy outputs. \\
Defuzzification & Converts fuzzy outputs back to a crisp numerical value. \\
\bottomrule
\end{tabularx}
\end{mltable}

\section{Membership functions in the temperature controller}

The Python example creates one input, \texttt{Temperature}, over \([0,100]\) and gives it three triangular membership functions: Cold, Warm, and Hot. The output \texttt{FanSpeed}, over \([0,10]\), receives Low, Medium, and High membership functions.

\begin{lstlisting}[style=mlpython]
import numpy as np

def trimf(x, a, b, c):
    x = np.asarray(x, dtype=float)
    left = np.ones_like(x) if a == b else (x - a) / (b - a)
    right = np.ones_like(x) if b == c else (c - x) / (c - b)
    return np.clip(np.minimum(left, right), 0.0, 1.0)

T = np.linspace(0, 100, 501)
fan = np.linspace(0, 10, 501)

temperature_mf = {
    "Cold": trimf(T, 0, 0, 50),
    "Warm": trimf(T, 25, 50, 75),
    "Hot":  trimf(T, 50, 100, 100),
}
fan_mf = {
    "Low":    trimf(fan, 0, 0, 5),
    "Medium": trimf(fan, 3, 5, 7),
    "High":   trimf(fan, 5, 10, 10),
}
\end{lstlisting}

\begin{aifig}[Overlapping membership functions]{The three temperature membership functions.
They deliberately overlap: at $70^\circ$ the reading is simultaneously Warm to degree $0.2$
and Hot to degree $0.4$, so two rules fire at once and the controller blends their outputs
instead of switching abruptly between them.}{fig:membership}
\begin{tikzpicture}
\begin{axis}[
  width=0.90\linewidth,
  height=6.1cm,
  xmin=0,xmax=100,ymin=0,ymax=1.08,
  xlabel={Temperature},ylabel={Membership},
  xtick={0,25,50,70,75,100},ytick={0,0.2,0.4,0.6,0.8,1},
  legend columns=3,
  legend style={at={(0.5,-0.24)},anchor=north},
  clip=false
]
\addplot[draw=MLBlue,very thick] coordinates {(0,1) (50,0) (100,0)};
\addlegendentry{Cold}
\addplot[draw=MLTeal,very thick] coordinates {(0,0) (25,0) (50,1) (75,0) (100,0)};
\addlegendentry{Warm}
\addplot[draw=MLOrange,very thick] coordinates {(0,0) (50,0) (100,1)};
\addlegendentry{Hot}
\addplot[draw=MLRed,dashed,thick] coordinates {(70,0) (70,1.02)};
\addplot[only marks,mark=*,mark size=2.2pt,MLTeal] coordinates {(70,0.2)};
\addplot[only marks,mark=*,mark size=2.2pt,MLOrange] coordinates {(70,0.4)};
\node[font=\scriptsize\color{MLMuted},anchor=west] at (axis cs:71,0.22) {$\mu_{Warm}=0.2$};
\node[font=\scriptsize\color{MLMuted},anchor=west] at (axis cs:71,0.43) {$\mu_{Hot}=0.4$};
\end{axis}
\end{tikzpicture}
\end{aifig}

At a temperature of 70, overlapping memberships activate both Warm and Hot rules; fuzzy inference combines their contributions.

Combining them produces a fuzzy \emph{set}, not a number, and a fan needs a number.
\figref{fig:defuzz} shows the last stage: each firing rule clips its output set to the
strength of its rule, the clipped shapes are merged, and the centroid of the merged area is
reported as the answer.

\begin{aifig}[Defuzzification by centroid]{Defuzzifying the $70^\circ$ reading. Medium is clipped at $0.2$ and High at $0.4$; the centre of area of their union gives the crisp fan speed. A stronger Hot rule pulls the centroid right.}{fig:defuzz}
\begin{tikzpicture}
\begin{axis}[
  width=0.82\linewidth, height=5.4cm,
  xmin=0, xmax=10, ymin=0, ymax=1.12,
  xlabel={Fan speed}, ylabel={Membership},
  xtick={0,2.5,5,6.4,7.5,10}, ytick={0,0.2,0.4,0.6,0.8,1},
  axis lines=left, grid=major, grid style={draw=MLPaleBlue},
  clip=false]
% full output sets, faint
\addplot[draw=MLMuted!45,thin] coordinates {(0,0) (2.5,1) (5,0)};
\addplot[draw=MLMuted!45,thin] coordinates {(2.5,0) (5,1) (7.5,0)};
\addplot[draw=MLMuted!45,thin] coordinates {(5,0) (7.5,1) (10,0)};
% clipped + aggregated region
\addplot[draw=MLTeal,very thick,fill=MLPaleTeal,fill opacity=0.75]
  coordinates {(2.5,0) (3,0.2) (4.5,0.2) (5.6,0.4) (8.5,0.4) (10,0) (2.5,0)};
% centroid
\addplot[MLRed,dashed,thick] coordinates {(6.4,0) (6.4,0.62)};
\addplot[only marks,mark=*,mark size=2.4pt,MLRed] coordinates {(6.4,0.4)};
\node[font=\scriptsize\color{MLRed},anchor=south] at (axis cs:6.4,0.65)
  {centroid $=6.4$};
\node[font=\scriptsize\color{MLTeal},anchor=west] at (axis cs:0.2,0.28)
  {Medium clipped at $0.2$};
\node[font=\scriptsize\color{MLOrange},anchor=west] at (axis cs:8.6,0.52)
  {High clipped at $0.4$};
\end{axis}
\end{tikzpicture}
\end{aifig}

The rule list maps each input term to its corresponding output term.

\begin{lstlisting}[style=mlpython]
def interp_mf(universe, mf, value):
    return float(np.interp(value, universe, mf))

def fan_speed(temp):
    firing = {
        "Low": interp_mf(T, temperature_mf["Cold"], temp),
        "Medium": interp_mf(T, temperature_mf["Warm"], temp),
        "High": interp_mf(T, temperature_mf["Hot"], temp),
    }
    aggregated = np.maximum.reduce([
        np.minimum(firing[name], fan_mf[name])
        for name in ["Low", "Medium", "High"]
    ])
    return np.sum(fan * aggregated) / np.sum(aggregated)

print(f"Fan speed at 70 C: {fan_speed(70):.2f}")
\end{lstlisting}

\begin{workedexample}
At \(70^\circ\mathrm{C}\), the triangular definitions give
\[
\mu_{Cold}(70)=0,\qquad
\mu_{Warm}(70)=\frac{75-70}{75-50}=0.2,\qquad
\mu_{Hot}(70)=\frac{70-50}{100-50}=0.4.
\]
The Medium and High rules fire at strengths 0.2 and 0.4. Default Mamdani inference with centroid defuzzification produces a fan speed of approximately \(7.16\) on the \([0,10]\) scale.
\end{workedexample}

\section{Mamdani and Sugeno FIS}

Lab 4 distinguishes two types of Fuzzy Inference System. They differ only in what a rule's
\emph{then} clause produces (\figref{fig:mamdani-sugeno}).

\begin{aifig}[Mamdani versus Sugeno rule output]{The same rule under both systems. Mamdani's
consequent is a fuzzy set that must be aggregated and defuzzified; Sugeno's is a formula that
evaluates straight to a number. Sugeno skips the centroid step, which is why it is the form
ANFIS can train.}{fig:mamdani-sugeno}
\begin{tikzpicture}[font=\small]
\begin{scope}
\node[font=\bfseries\color{MLNavy}] at (2.5,2.2) {Mamdani};
\node[aibox,minimum width=4.6cm,font=\scriptsize\color{MLNavy}] (r1) at (2.5,1.5)
  {IF Speed is Fast THEN Brake is \textbf{High}};
\node[ailabel,anchor=north,color=MLOrange] at (2.5,1.05) {\textbf{High} is a fuzzy set};
\begin{scope}[xshift=1.35cm,yshift=-0.55cm,x=0.6cm,y=0.6cm]
\draw[draw=MLMuted!60,-{Stealth[length=1.6mm]}] (-0.2,0)--(4.0,0);
\draw[draw=MLMuted!60,-{Stealth[length=1.6mm]}] (0,-0.2)--(0,1.35);
\draw[very thick,draw=MLOrange] (0.4,0)--(1.8,1.1)--(3.2,0);
\fill[MLOrange,opacity=0.2] (0.4,0)--(1.8,1.1)--(3.2,0)--cycle;
\end{scope}
\node[ailabel,anchor=north,align=center] at (2.5,-1.35)
  {needs aggregation, then a centroid};
\end{scope}
\begin{scope}[xshift=7.6cm]
\node[font=\bfseries\color{MLNavy}] at (2.5,2.2) {Sugeno};
\node[aibox,minimum width=4.9cm,font=\scriptsize\color{MLNavy},
      fill=MLPaleTeal,draw=MLGreen] (r2) at (2.5,1.5)
  {IF Speed is Fast THEN Brake $=0.006s+0.3$};
\node[ailabel,anchor=north,color=MLGreen] at (2.5,1.05) {the consequent is a formula};
\node[aicell,minimum width=2.4cm,minimum height=0.65cm,fill=MLPaleTeal,draw=MLGreen,
      font=\scriptsize\bfseries] at (2.5,0.15) {$s=60 \Rightarrow 0.66$};
\node[ailabel,anchor=north,align=center] at (2.5,-1.35)
  {weighted average, no centroid};
\end{scope}
\end{tikzpicture}
\end{aifig}

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.15\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Type & Laboratory description \\
\midrule
Mamdani & Intuitive and commonly used for control applications. \\
Sugeno & Uses constant or linear output functions and is suitable for optimization and learning. \\
\bottomrule
\end{tabularx}
\end{mltable}

The Sugeno example creates a \texttt{Speed} input and a \texttt{BrakeForce} output, with Gaussian input membership functions and linear output functions.

\begin{lstlisting}[style=mlpython]
def gaussian(x, sigma, center):
    return np.exp(-0.5 * ((x - center) / sigma) ** 2)

def sugeno_brake(speed):
    slow = gaussian(speed, 10, 20)
    fast = gaussian(speed, 10, 80)
    low = 0.002 * speed + 0.1
    high = 0.006 * speed + 0.3
    return (slow * low + fast * high) / (slow + fast)

print(f"Brake force at speed 60: {sugeno_brake(60):.3f}")
\end{lstlisting}

Each Sugeno rule produces a constant or linear value; the FIS returns their firing-strength-weighted average. For a \texttt{linear} output MF with one input, Python reads the parameters \([a\;b]\) as the function \(a\times\text{Speed}+b\).

At speed 60 the Gaussian memberships are \(\mu_{Slow}=0.0003\) and \(\mu_{Fast}=0.1353\), so the Fast rule dominates:
\[
\text{BrakeForce}=\frac{0.0003\times0.22+0.1353\times0.66}{0.0003+0.1353}\approx0.659.
\]

\begin{pitfall}
\textbf{Read the consequent function, not the rule's name.} A rule labelled ``High'' whose function is \(-0.01\times\text{Speed}+1\) still \emph{decreases} with speed: it returns 1.0 at a standstill and 0.0 at 100. The label says high; the arithmetic says the opposite, and the arithmetic wins.

An earlier version of the supplied \texttt{lab04\_fuzzy\_anfis\_worked.ipynb} used exactly those coefficients, giving
\[
\text{BrakeForce}(20)=0.200,\quad
\text{BrakeForce}(60)=0.400,\quad
\text{BrakeForce}(100)=0.000,
\]
a controller that brakes hardest when parked and not at all at top speed. Every Python call succeeded. The code was valid and the model was wrong.

The file now uses \([0.002\;0.1]\) and \([0.006\;0.3]\), so both consequents rise with speed and the numbers above hold.
\end{pitfall}

\begin{tryit}
Never judge a controller from one input value. Evaluate it across the whole range and look at the shape:

\begin{lstlisting}[style=mlpython]
speeds = np.array([0, 20, 40, 60, 80, 100], dtype=float)
print(np.column_stack([speeds, [sugeno_brake(v) for v in speeds]]))
\end{lstlisting}

A monotonic rise from about 0.10 to 0.90 is what this design intends. Anything non-monotonic, flat, or falling means a consequent slopes the wrong way --- the failure a single call such as \texttt{sugeno\_brake(60)} would have hidden completely.
\end{tryit}

\section{ANFIS: Adaptive Neuro-Fuzzy Inference System}

ANFIS can be summarized as
\[
\text{ANFIS}=\text{neural-network learning}+\text{fuzzy-logic representation}.
\]
It uses a Sugeno FIS and learns rule and membership-function parameters from data, commonly through least squares and gradient descent.

\figref{fig:anfis} unpacks that sum. The five layers are the stages of
\figref{fig:fis-pipeline} redrawn as a network. Once each stage is a differentiable node, the
membership shapes and rule outputs become parameters gradient descent can adjust. The fuzzy
reasoning is unchanged --- only who chooses the numbers.

\begin{aifig}[The five layers of an ANFIS network]{ANFIS for two inputs and two rules. Shaded layers (1 and 4) hold the trainable parameters; layers 2, 3, and 5 are fixed arithmetic. Same stages as \figref{fig:fis-pipeline}, drawn so a learning algorithm can reach inside.}{fig:anfis}
\begin{tikzpicture}[font=\small,
  L/.style={circle,draw=MLBlue,fill=MLPaleBlue,minimum size=7.5mm,
            font=\scriptsize\color{MLNavy},inner sep=0.5pt},
  T/.style={circle,draw=MLOrange,fill=MLPaleOrange,minimum size=7.5mm,line width=0.9pt,
            font=\scriptsize\color{MLNavy},inner sep=0.5pt}]
\def\dx{2.45}
% inputs
\node[ailabel,font=\small\bfseries\color{MLNavy}] (x) at (-\dx,0.75) {$x$};
\node[ailabel,font=\small\bfseries\color{MLNavy}] (y) at (-\dx,-0.75) {$y$};
% layer 1 (trainable MFs)
\node[T] (A1) at (0,1.35) {$A_1$};
\node[T] (A2) at (0,0.45) {$A_2$};
\node[T] (B1) at (0,-0.45) {$B_1$};
\node[T] (B2) at (0,-1.35) {$B_2$};
% layer 2 (rule firing)
\node[L] (P1) at (\dx,0.9) {$\Pi$};
\node[L] (P2) at (\dx,-0.9) {$\Pi$};
% layer 3 (normalise)
\node[L] (N1) at (2*\dx,0.9) {$N$};
\node[L] (N2) at (2*\dx,-0.9) {$N$};
% layer 4 (trainable consequents)
\node[T] (C1) at (3*\dx,0.9) {$f_1$};
\node[T] (C2) at (3*\dx,-0.9) {$f_2$};
% layer 5 (sum)
\node[L] (S) at (4*\dx,0) {$\Sigma$};
\node[ailabel,font=\small\bfseries\color{MLNavy}] (out) at (4*\dx+1.15,0) {output};

\foreach \t in {A1,A2}{\draw[aiptr] (x)--(\t);}
\foreach \t in {B1,B2}{\draw[aiptr] (y)--(\t);}
\draw[aiptr] (A1)--(P1); \draw[aiptr] (B1)--(P1);
\draw[aiptr] (A2)--(P2); \draw[aiptr] (B2)--(P2);
\draw[aiptr] (P1)--(N1); \draw[aiptr] (P2)--(N2);
\draw[aiptr] (P1)--(N2); \draw[aiptr] (P2)--(N1);
\draw[aiptr] (N1)--(C1); \draw[aiptr] (N2)--(C2);
\draw[aiptr] (C1)--(S);  \draw[aiptr] (C2)--(S);
\draw[aiptr] (S)--(out);

\foreach \i/\lbl in {0/{1. fuzzify}, 1/{2. rule\\strength}, 2/{3. normalise},
                     3/{4. rule\\output}, 4/{5. aggregate}}{
  \node[ailabel,anchor=north,align=center] at (\i*\dx,-1.95) {\lbl};
}
\node[ailabel,anchor=west,color=MLOrange] at (-\dx-0.55,-3.15)
  {shaded nodes hold the parameters ANFIS trains; the rest is fixed arithmetic};
\end{tikzpicture}
\end{aifig}

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.25\linewidth}p{0.32\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Feature & Fuzzy Logic & ANFIS \\
\midrule
Rule definition & Manual & Learned from data \\
Learning capability & No & Yes, through neural-network-style learning \\
Output type & Mamdani or Sugeno & Sugeno only \\
Typical use in handout & Expert-defined systems & Data-driven modeling \\
\bottomrule
\end{tabularx}
\end{mltable}

\begin{keyidea}
Use manual fuzzy logic for expert-defined rules and ANFIS when data should determine model parameters.
\end{keyidea}

\section{Training examples in Lab 4}

The examples cover one- and two-input training, prediction, FIS persistence, error analysis, and air-conditioning control.

\begin{lstlisting}[style=mlpython]
# The worked Lab 4 notebook provides a compact SugenoANFIS class.
# Each row is [input_1, input_2, target].
data = np.array([
    [0, 0, 0],
    [5, 2, 2.5],
    [10, 5, 5],
    [15, 7, 7.5],
    [20, 10, 10],
], dtype=float)

X, y = data[:, :2], data[:, 2]
model = SugenoANFIS(n_mfs=2, maxiter=30).fit(X, y)
predicted = model.predict(np.array([[12.0, 6.0]]))[0]
print(f"Predicted output: {predicted:.3f}")
\end{lstlisting}

ANFIS interprets the final column as the target. Thus, \texttt{data1} contains one input and one output, whereas \texttt{data2} contains two inputs and one output. A prediction row contains inputs only: \texttt{[12 6]}, without a placeholder for the unknown output.

\begin{pitfall}
Five rows demonstrate the application programming interface (API), not generalization. Evaluate separate validation data before making performance claims.
\end{pitfall}

\subsection{Preparing the air-quality regression data correctly}

Lab 4 uses \texttt{CO(GT)} as the regression target and two sensor signals, \texttt{PT08.S1(CO)} and \texttt{PT08.S2(NMHC)}, as predictors. The original UCI file uses \texttt{-200} to represent missing measurements, so those values must be converted to missing data before modelling.

\begin{pitfall}
\textbf{Do not scale the complete dataset before splitting.} A scaler fitted on all rows uses information from validation and test observations. This is data leakage even when the target column is excluded.

Fit the scaler on the training predictors only, then reuse the learned transformation for validation and test data:

\begin{lstlisting}[style=mlpython]
from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()
X_train_scaled = scaler.fit_transform(X_train)
X_val_scaled = scaler.transform(X_val)
X_test_scaled = scaler.transform(X_test)
\end{lstlisting}
\end{pitfall}

\begin{pitfall}
\textbf{Keep the regression target separate from the feature transformation.} Standardising the predictors is useful because the sensor channels have different numeric ranges. The target \texttt{CO(GT)} should remain in its original units so RMSE and MAE remain interpretable.
\end{pitfall}

\begin{keyidea}
The Lab 4 comparison is deliberately controlled: linear regression and every ANFIS candidate receive the same cleaned rows, the same train/validation/test split, and the same training-fitted scaling. ANFIS structure is selected with validation RMSE; the test partition is evaluated only after the model choice is fixed.
\end{keyidea}

\input{labs/04-fuzzy-anfis}

\begin{quickcheck}
\begin{enumerate}
  \item What is the difference between a crisp value and a fuzzy membership value?
  \item Name the four components of a Fuzzy Inference System in order.
  \item Why does Lab 4 associate ANFIS with Sugeno rather than Mamdani output functions?
  \item What does the smart air-conditioning ANFIS example use as inputs and output?
\end{enumerate}
\end{quickcheck}
