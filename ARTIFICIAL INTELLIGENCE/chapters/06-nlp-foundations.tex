\chapter{NLP Foundations: Getting Text and Measuring It}
\label{ch:nlp-foundations}

\chapterguide%
  {Manipulate Python strings, and know what a controlled experiment is.}
  {describe an NLP pipeline from raw input to task output; explain why text normalisation comes before any comparison; compute Levenshtein distance by hand and turn it into character error rate; design a fair before/after comparison of a preprocessing step; and separate segmentation failures from recognition failures in an OCR error analysis.}

Natural language processing begins before there is any language to process. Text arrives as keystrokes, as speech, or --- as in Lab~6 --- as photographs of handwriting, and the first job of a pipeline is to turn that input into a string a program can compare, count and classify. Lab~6 takes 89 photographs of handwritten medicine names, runs an off-the-shelf optical character recognition engine over them, and asks a question every NLP project must answer early: \emph{how good is the text we are starting from, and how would we know?}

\section{Every NLP task is a pipeline}

Whatever the task --- translation, sentiment, search --- the same stages appear in the same order (\figref{fig:nlp-pipeline}). Text is acquired, normalised into a canonical form, turned into a representation a model can use, and then the task-specific model runs. Errors made early propagate: a word the OCR stage misreads is a word the sentiment model never sees.

\begin{aifig}[The NLP pipeline]{The stages of an NLP pipeline and where the two NLP labs sit. Lab~6 is about acquisition and measurement: the OCR engine is the only learned component, and everything after it is plain string handling. Lab~7 starts from clean text and builds the representation and the model.}{fig:nlp-pipeline}
\begin{tikzpicture}[
  b/.style={draw=MLBlue,fill=MLPaleBlue,rounded corners=2mm,minimum width=2.22cm,minimum height=1.0cm,align=center,font=\small\color{MLNavy}},
  arr/.style={-{Stealth[length=2mm]},thick,draw=MLTeal}
]
\node[b] (acq) {Acquire\\\scriptsize photo, audio, keyboard};
\node[b,right=0.26cm of acq] (norm) {Normalise\\\scriptsize case, punctuation, tokens};
\node[b,right=0.26cm of norm] (rep) {Represent\\\scriptsize counts, n-grams, vectors};
\node[b,right=0.26cm of rep] (model) {Model\\\scriptsize classify, translate, search};
\node[b,right=0.26cm of model] (eval) {Evaluate\\\scriptsize against references};
\draw[arr] (acq)--(norm); \draw[arr] (norm)--(rep); \draw[arr] (rep)--(model); \draw[arr] (model)--(eval);
\draw[aibrace,decoration={mirror},draw=MLOrange] (-1.3,-0.75) -- (3.9,-0.75);
\node[font=\scriptsize\color{MLOrange},anchor=north] at (1.3,-0.95) {Lab~6: OCR, normalisation, measurement};
\draw[aibrace,decoration={mirror},draw=MLGreen] (4.1,-0.75) -- (12.1,-0.75);
\node[font=\scriptsize\color{MLGreen},anchor=north] at (8.1,-0.95) {Lab~7: bag of words, Naive Bayes, evaluation};
\end{tikzpicture}
\end{aifig}

\section{Optical character recognition as text acquisition}

\term{OCR} converts an image of text into a string. Modern engines such as Tesseract do it in two stages: a \term{segmentation} stage finds the lines and words in the image, and a \term{recognition} stage --- a neural network trained on printed text --- reads the characters in each segment. The distinction matters for the error analysis later, because the two stages fail differently: segmentation failure produces \emph{no} text, recognition failure produces the \emph{wrong} text.

Tesseract is configured through two switches that Lab~6 fixes once and never changes: \texttt{--oem 1} selects the neural recogniser and \texttt{--psm 7} tells the segmenter to expect a single line of text, which every cropped medicine name is. Fixing the configuration is what makes the later comparison an experiment rather than a tuning session.

\begin{intuition}
An OCR engine is a model like any other, trained on some distribution of images. Tesseract's distribution is printed documents. Handwritten cursive on lined paper is far from it, and no amount of clever downstream code recovers information the recogniser never extracted.
\end{intuition}

\section{Normalisation before comparison}

Two strings can mean the same thing and still differ byte for byte: ``Tab Azimax'' and ``tab azimax'', ``40 mg'' and ``40mg''. Before any comparison, both the prediction and the reference are mapped to a canonical form. Lab~6 keeps only lowercase letters and digits:

\begin{lstlisting}[style=aipython]
def normalise(text):
    return re.sub(r"[^a-z0-9]", "", str(text).lower())
\end{lstlisting}

The choice is deliberate and should be stated in any report: it declares that case, spaces and punctuation are \emph{not} errors for this task. A different task --- one where ``40 mg'' and ``4 0mg'' must be distinguished --- would need a different normaliser, and its accuracy numbers would not be comparable with these.

\begin{keyidea}
Normalise both sides identically, decide in advance what differences count as errors, and write the rule down. A metric is only meaningful relative to the normalisation it was computed under.
\end{keyidea}

\section{Measuring text quality: edit distance}

Exact match --- prediction equals reference after normalisation --- is the strictest measure and often the least informative: on Lab~6's baseline it is zero for all 89 images. A graded measure is needed, and the standard one is \term{Levenshtein distance}: the minimum number of single-character insertions, deletions and substitutions that turn one string into the other. \figref{fig:levenshtein} shows the dynamic-programming table that computes it.

\begin{aifig}[Levenshtein distance by dynamic programming]{The table for turning \texttt{nims} into the OCR output \texttt{nidu}. Cell $(i,j)$ holds the cost of matching the first $i$ characters of the reference with the first $j$ of the prediction: one more than the cell above (delete), one more than the cell to the left (insert), or the diagonal plus one if the characters differ (substitute). The answer, 2, is in the bottom-right corner: substitute \texttt{m}$\to$\texttt{d} and \texttt{s}$\to$\texttt{u}.}{fig:levenshtein}
\begin{tikzpicture}[font=\small]
\def\W{0.78}
\foreach \c [count=\ci from 1] in {n,i,d,u}{\node[font=\bfseries\color{MLOrange}] at (\W*\ci,\W*0.85) {\c};}
\foreach \r [count=\ri from 1] in {n,i,m,s}{\node[font=\bfseries\color{MLBlue}] at (-\W*0.85,-\W*\ri) {\r};}
\foreach \row [count=\ri from 0] in {{0,1,2,3,4},{1,0,1,2,3},{2,1,0,1,2},{3,2,1,1,2},{4,3,2,2,2}}{
  \foreach \v [count=\ci from 0] in \row{
    \ifnum\ri=\ci
      \node[aihot,minimum width=\W cm,minimum height=\W cm,font=\scriptsize\bfseries] at (\W*\ci,-\W*\ri) {\v};
    \else
      \node[aicell,minimum width=\W cm,minimum height=\W cm,font=\scriptsize] at (\W*\ci,-\W*\ri) {\v};
    \fi
  }
}
\node[ailabel,anchor=north,color=MLBlue] at (-\W*0.85,-\W*4.6) {reference};
\node[ailabel,anchor=south,color=MLOrange] at (\W*2.5,\W*1.3) {prediction};
\node[ailabel,anchor=west,align=left,text width=6.2cm] at (\W*5.4,-\W*1.2)
  {$D[i,j]=\min\big(D[i-1,j]+1,\ D[i,j-1]+1,\ D[i-1,j-1]+[r_i\neq p_j]\big)$\\[4pt]
   The highlighted diagonal is the cheapest path: match \texttt{n}, match \texttt{i}, substitute, substitute.\\[4pt]
   Distance $=2$; normalised by the longer length, $2/4=0.5$.};
\end{tikzpicture}
\end{aifig}

The lab's implementation is the table computed one row at a time:

\begin{lstlisting}[style=aipython]
def levenshtein(a, b):
    previous = list(range(len(b) + 1))
    for i, char_a in enumerate(a, start=1):
        current = [i]
        for j, char_b in enumerate(b, start=1):
            current.append(min(current[j - 1] + 1, previous[j] + 1, previous[j - 1] + (char_a != char_b)))
        previous = current
    return previous[-1]
\end{lstlisting}

From the raw distance the lab derives three summary metrics, each answering a different question:

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.30\linewidth}p{0.30\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Metric & Definition & Answers \\
\midrule
Exact-match accuracy & Fraction of images with distance $0$ & How often is the name usable as-is? \\
Mean normalised edit distance & Mean over images of distance $/$ longer length; in $[0,1]$ & How wrong is a typical image? \\
Character error rate (CER) & Total edits $/$ total reference characters; can exceed $1$ & How many corrections per 100 reference characters? \\
Blank predictions & Images whose normalised prediction is empty & How often did segmentation find nothing? \\
\bottomrule
\end{tabularx}
\end{mltable}

\begin{pitfall}
CER is a ratio of totals, so long references dominate it; the normalised distance is a mean of per-image ratios, so every image counts equally. They can move in opposite directions on the same data. Report both, and say which one a decision rests on.
\end{pitfall}

\section{A controlled preprocessing experiment}

The baseline numbers are poor --- CER close to $0.9$, and 39 of 89 images return nothing. The obvious response is to improve the images, and the obvious trap is to change several things and the engine settings at once and then attribute the improvement to whichever change one likes. Lab~6 changes the image \emph{only}, in one fixed pipeline: grayscale, a 20-pixel white border, a width cap, and a $1.5\times$ enlargement. Every step targets a known weakness of the segmenter --- colour noise, text touching the edge, characters smaller than the recogniser expects --- and the engine configuration stays exactly as it was.

The comparison is fair because three things are held constant: the same 89 images, the same normalisation, the same OCR configuration. Only then is a difference attributable to the preprocessing.

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.30\linewidth}>{\centering\arraybackslash}X>{\centering\arraybackslash}X>{\centering\arraybackslash}X>{\centering\arraybackslash}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Pipeline & Exact match & Norm.\ edit distance & CER & Blank \\
\midrule
Raw images & 0.000 & 0.856 & 0.875 & 39 \\
Grayscale, border, resize & 0.011 & 0.767 & 0.788 & 15 \\
\bottomrule
\end{tabularx}
\end{mltable}

Read across the rows and the story is specific: blank predictions fall by more than half and CER by about $0.09$, but exact match barely moves. Preprocessing helped the segmenter find the text; it did not help the recogniser read cursive. That is exactly the distinction between the two OCR stages, now visible in numbers.

\section{Error analysis: which stage failed?}

Aggregate metrics say \emph{how much} went wrong; an error analysis says \emph{what}. Lab~6 sorts the preprocessed predictions by normalised edit distance, takes the worst, and tags each with one of two failure types that map onto the two OCR stages:

\begin{description}
  \item[No text detected.] The normalised prediction is empty. Segmentation failed --- faint strokes, low contrast, or slanted writing the line finder rejected. Image preprocessing can help here.
  \item[Character confusion.] A line was found but the characters are wrong: \texttt{nims} read as \texttt{nidu}, \texttt{dicloran} as \texttt{divlarew}. Recognition failed --- the model has no knowledge of this handwriting. Preprocessing cannot help; only a recogniser trained on handwriting can.
\end{description}

The six hardest images are saved as a figure with the reference and the prediction under each, so that a reader can check the tags against the pictures. An error analysis that cannot be checked is an opinion.

\begin{keyidea}
Attribute each failure to the stage that produced it. The fix for ``no text found'' is different from the fix for ``wrong text found'', and an aggregate score cannot tell them apart.
\end{keyidea}

\section{Fitness for purpose}

The lab's closing note is part of the method, not a disclaimer. A benchmark measures the fit between a model and a domain, and this one shows a poor fit: a recogniser trained on print applied to cursive on lined paper. Nothing in the numbers supports using generic OCR for clinical transcription, where a misread name or dose is a safety failure. A system for that purpose would need a handwriting-specific recogniser, data representative of the clinic's own writers, validation on data held out from development, privacy controls, and a person in the loop for every uncertain reading. Writing this down is what turns a lab result into engineering judgement.

\input{labs/06-nlp-part-a}

\begin{quickcheck}
\begin{enumerate}
  \item Which of the two OCR stages is responsible for a blank prediction, and which for \texttt{nims} being read as \texttt{nidu}?
  \item Compute the Levenshtein distance between \texttt{breeky} and \texttt{beeky}, and the normalised distance.
  \item Why must the OCR configuration stay fixed when the preprocessing pipeline is tested?
  \item After preprocessing, CER fell by $0.09$ but exact-match accuracy rose by only one image. What does that pattern say about which stage improved?
  \item The lab's normaliser deletes spaces. Name a task for which that would be the wrong choice.
\end{enumerate}
\end{quickcheck}
