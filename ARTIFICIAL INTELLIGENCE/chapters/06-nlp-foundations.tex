\chapter{Natural Language Processing Foundations}
\label{ch:nlp-foundations}

\chapterguide%
  {Be able to run Python and read simple vector, image, and text-processing code.}
  {explain the purpose and significance of natural language processing, compute sentence similarity from word embeddings, describe the handout's classification-based image-caption simulation, and build and evaluate an optical character recognition pipeline with normalization and error metrics.}

\term{Natural Language Processing (NLP)} applies linguistics, computer science, and machine learning to analyze or generate text and speech.

\section{Why NLP is needed}

NLP converts unstructured language into representations suitable for interaction, automation, and information extraction.

Applications include:
\begin{itemize}
  \item healthcare: analyzing patient notes and assisting diagnosis;
  \item business: chatbots, sentiment analysis, and automated email sorting;
  \item education: grammar correction and automated essay scoring;
  \item law: legal-document summarization and contract analysis;
  \item robotics: verbal commands and real-time interaction.
\end{itemize}

\begin{keyidea}
Each example follows a pipeline: input, representation, and task-specific output.
\end{keyidea}

\section{Text similarity with word embeddings}

The first Python example compares
\begin{quote}
``The quick brown fox jumps over the lazy dog.''
\end{quote}
with
\begin{quote}
``A fast brown animal leaps above a sleeping dog.''
\end{quote}
using pretrained FastText word embeddings.

The pipeline has five stages.

\begin{mlsteps}
  \item Tokenize both sentences.
  \item Load pretrained FastText embeddings.
  \item Remove words not available in the embedding vocabulary.
  \item Convert each word to a vector and average the word vectors for each sentence.
  \item Compute cosine similarity between the two sentence vectors.
\end{mlsteps}

\begin{lstlisting}[style=mlpython]
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.metrics.pairwise import cosine_similarity

text1 = "The quick brown fox jumps over the lazy dog."
text2 = "A fast brown animal leaps above a sleeping dog."

vectorizer = TfidfVectorizer(stop_words="english")
X = vectorizer.fit_transform([text1, text2])
similarity = cosine_similarity(X[0], X[1])[0, 0]
print(f"Cosine similarity: {similarity:.4f}")
\end{lstlisting}

The \texttt{Vocabulary} property contains unique word types, so repeated words do not receive extra weight in this version. A token-level sentence representation would preserve repetition. This distinction matters when word frequency carries meaning.

\begin{pitfall}
\textbf{Averaging word vectors throws away word order.} The mean of a set of vectors does not depend on the order they were added, so

\begin{center}
``dog bites man'' \qquad and \qquad ``man bites dog''
\end{center}

produce \emph{exactly} the same sentence vector and a cosine similarity of 1.000. The two sentences describe opposite events, and this representation cannot tell them apart at all.

Because the code averages over \texttt{Vocabulary} rather than tokens, it is also blind to repetition: ``very very good'' and ``very good'' are identical too.

Try it --- it takes one line to change the input, and seeing 1.000 come back makes the limitation concrete in a way that reading about it does not.
\end{pitfall}

\begin{keyidea}
This is the \term{bag-of-embeddings} trade. Averaging is cheap, needs no training, and works surprisingly well for topic-level similarity --- which is what the lab example measures. It is the wrong tool the moment meaning depends on \emph{arrangement}: negation, subject versus object, or word order generally.

Models that do handle order (recurrent networks, transformers) exist precisely because this limitation is fundamental rather than a detail of the implementation. Say which kind of similarity your result supports before quoting the number.
\end{keyidea}

\section{Cosine similarity}

For sentence vectors \(\mathbf{u}\) and \(\mathbf{v}\), the code computes
\[
\cos(\theta)=\frac{\mathbf{u}\cdot\mathbf{v}}
{\|\mathbf{u}\|\,\|\mathbf{v}\|}.
\]
A value near 1 indicates high directional similarity.

\begin{intuition}
A word embedding replaces a word with a numerical vector. Averaging the word vectors gives one rough vector for a sentence. Cosine similarity then asks whether the two sentence vectors point in similar directions.
\end{intuition}

\figref{fig:embedding} shows why that averaging step is defensible. In a trained embedding
space, words used in similar contexts end up close together, so the average of a sentence's
word vectors lands in the region of meaning that the sentence is about. The space in the lab
has hundreds of dimensions; the two drawn here stand in for that.

\begin{aifig}[Words clustered in embedding space]{A two-dimensional sketch of an embedding
space. Words that appear in similar contexts sit near one another, and a sentence vector ---
the average of its word vectors --- lands inside the relevant cluster. This is what makes
comparing two averaged sentences meaningful at all.}{fig:embedding}
\begin{tikzpicture}[font=\small,x=1cm,y=1cm]
\draw[-{Stealth[length=2mm]},draw=MLMuted] (-0.2,0)--(8.4,0) node[right,font=\scriptsize] {dim 1};
\draw[-{Stealth[length=2mm]},draw=MLMuted] (0,-0.2)--(0,4.0) node[above,font=\scriptsize] {dim 2};
% cluster 1: animals
\begin{scope}
\fill[MLPaleBlue,opacity=0.85] (1.75,2.85) circle (1.15);
\foreach \x/\y/\w in {1.15/2.75/cat, 2.05/3.35/dog, 2.35/2.45/puppy, 1.35/2.15/kitten}{
  \node[circle,fill=MLBlue,inner sep=1.4pt] at (\x,\y) {};
  \node[font=\scriptsize\color{MLNavy},anchor=west] at (\x+0.12,\y) {\w};}
\node[font=\scriptsize\bfseries\color{MLBlue}] at (1.75,4.25) {animals};
\end{scope}
% cluster 2: vehicles
\begin{scope}
\fill[MLPaleTeal,opacity=0.85] (6.1,1.35) circle (1.15);
\foreach \x/\y/\w in {5.45/1.15/car, 6.35/1.85/truck, 6.55/0.85/van, 5.65/1.95/bus}{
  \node[circle,fill=MLTeal,inner sep=1.4pt] at (\x,\y) {};
  \node[font=\scriptsize\color{MLNavy},anchor=west] at (\x+0.12,\y) {\w};}
\node[font=\scriptsize\bfseries\color{MLTeal}] at (6.1,-0.35) {vehicles};
\end{scope}
% sentence vector
\draw[-{Stealth[length=2.5mm]},very thick,draw=MLOrange] (0,0)--(1.72,2.68);
\node[circle,fill=MLOrange,inner sep=2pt] at (1.72,2.68) {};
\node[font=\scriptsize\bfseries\color{MLOrange},anchor=west,align=left] at (2.9,3.75)
  {average of ``the cat and\\the dog'' lands here};
\draw[draw=MLOrange,thin,dashed] (1.9,2.75) -- (2.85,3.6);
\end{tikzpicture}
\end{aifig}

\begin{aifig}[Cosine similarity measures angle, not length]{Cosine compares direction only. $\mathbf{u}$ and $\mathbf{v}$ sit $13^\circ$ apart and score $0.97$. $\mathbf{w}$ has almost the same \emph{length} as $\mathbf{u}$ ($3.58$ against $3.59$) yet scores $0.56$ --- length carries no weight.}{fig:cosine}
\begin{tikzpicture}[x=1.2cm,y=1.2cm]
  \draw[-{Stealth[length=2mm]},draw=MLMuted] (-0.15,0)--(4.3,0) node[right,font=\scriptsize] {dimension 1};
  \draw[-{Stealth[length=2mm]},draw=MLMuted] (0,-0.15)--(0,3.1) node[above,font=\scriptsize] {dimension 2};
  \draw[-{Stealth[length=2.5mm]},very thick,draw=MLBlue] (0,0)--(3.4,1.15)
    node[right,font=\small\bfseries\color{MLBlue}] {$\mathbf{u}$};
  \draw[-{Stealth[length=2.5mm]},very thick,draw=MLTeal] (0,0)--(2.8,1.75)
    node[above right,font=\small\bfseries\color{MLTeal}] {$\mathbf{v}$};
  \draw[-{Stealth[length=2.5mm]},very thick,draw=MLRed] (0,0)--(0.95,3.45)
    node[left,font=\small\bfseries\color{MLRed}] {$\mathbf{w}$};
  \draw[-{Stealth[length=1.5mm]},draw=MLOrange,thick]
    (0.92,0.31) arc[start angle=18.7,end angle=32,radius=0.97];
  \node[font=\scriptsize\bfseries\color{MLOrange!85!black}] at (1.18,0.56) {$\theta$};
  \node[align=left,font=\scriptsize\color{MLMuted},anchor=west] at (3.55,2.35)
    {$\cos\theta=0.97$\\$13^\circ$ apart: similar};
  \node[align=left,font=\scriptsize\color{MLRed},anchor=west] at (1.35,3.25)
    {$\cos=0.56$ against $\mathbf{u}$\\same length, different direction};
\end{tikzpicture}
\end{aifig}

Cosine similarity compares direction, not magnitude: opposite vectors score negative,
perpendicular ones score zero, and length is ignored entirely. In \figref{fig:cosine},
$\mathbf{u}$ and $\mathbf{w}$ are almost the same length yet score only $0.56$.

\begin{workedexample}
For a small numerical check, let \(\mathbf{u}=(1,1)\) and \(\mathbf{v}=(2,1)\). Their dot product is 3, their norms are \(\sqrt{2}\) and \(\sqrt{5}\), and their cosine similarity is
\[
\frac{3}{\sqrt{2}\sqrt{5}}=\frac{3}{\sqrt{10}}\approx0.949.
\]
The value is high because the vectors point in similar directions; Python applies the same calculation in higher dimensions.
\end{workedexample}

\section{Image captioning using AlexNet: a classification-based simulation}

\figref{fig:alexnet} shows what the pipeline really does. AlexNet returns one class label;
the ``caption'' is that label dropped into a fixed sentence template. Nothing in the system
describes a scene --- it names an object and wraps it in words.

\begin{aifig}[Classification dressed as captioning]{The chapter captioning pipeline. A real
captioning model generates a sentence conditioned on the image; this one substitutes a class
label into a template, so it can only ever say one thing per label.}{fig:alexnet}
\begin{tikzpicture}[font=\small]
\node[aibox,minimum width=1.9cm,minimum height=0.95cm,font=\scriptsize\color{MLNavy}]
  (im) at (0,0) {image\\\texttt{plane.jpg}};
\node[aibox,minimum width=2.3cm,minimum height=0.95cm,font=\scriptsize\color{MLNavy},
      fill=MLPaleOrange,draw=MLOrange] (net) at (2.85,0) {AlexNet\\1000 classes};
\node[aibox,minimum width=2.1cm,minimum height=0.95cm,font=\scriptsize\color{MLNavy}]
  (lab) at (5.75,0) {label\\\texttt{"airliner"}};
\node[aibox,minimum width=2.6cm,minimum height=0.95cm,font=\scriptsize\color{MLNavy}]
  (tpl) at (8.75,0) {template\\\texttt{"This is a \_\_"}};
\node[aibox,minimum width=3.1cm,minimum height=0.95cm,font=\scriptsize\color{MLNavy},
      fill=MLPaleTeal,draw=MLGreen] (out) at (12.2,0) {\texttt{"This is a airliner"}};
\foreach \a/\b in {im/net, net/lab, lab/tpl, tpl/out}{\draw[aiarrow] (\a)--(\b);}
\node[ailabel,anchor=north,color=MLOrange] at (2.85,-0.62) {learned};
\node[ailabel,anchor=north,color=MLMuted] at (8.75,-0.62) {hard-coded};
\node[ailabel,anchor=north,align=center,color=MLRed] at (6.1,-1.35)
  {one label per image means one possible sentence per label:
   the model cannot mention colour, count, or relationship};
\end{tikzpicture}
\end{aifig}

The second chapter example loads an image, resizes it to AlexNet's expected input size, extracts \texttt{fc7} features, classifies the image, and turns the predicted class label into a short caption.

\begin{lstlisting}[style=mlpython]
# A classifier normally supplies the label. The language step is simple:
predicted_label = "airliner"
caption = f"This is a picture of a {predicted_label.lower()}."
print(caption)

# In a real system, replace predicted_label with the output of a
# pretrained image classifier that has been validated for the task.
\end{lstlisting}

The \texttt{features} variable demonstrates how an intermediate neural representation can be extracted, but the fixed-template caption does not use it directly. The \texttt{classify} call produces the label inserted into the caption.

\begin{pitfall}
This is classification with a fixed caption template, not free-form image captioning.
\end{pitfall}

\section{Scene text detection and translation}

The third chapter pipeline uses optical character recognition (OCR) to extract printed text from an image and a hard-coded English-to-French dictionary to simulate translation without an internet application programming interface (API).

\begin{aifig}[The OCR and translation pipeline]{The scene-text pipeline. Only the OCR stage
is a learned model; normalisation and lookup are ordinary string handling, which is why the
whole thing runs without a network connection --- and why an unseen word simply fails to
translate.}{fig:ocr-pipeline}
\begin{tikzpicture}[
  b/.style={draw=MLBlue,fill=MLPaleBlue,rounded corners=2mm,minimum width=2.45cm,minimum height=0.9cm,align=center,font=\small\color{MLNavy}},
  arr/.style={-{Stealth[length=2mm]},thick,draw=MLTeal}
]
\node[b] (img) {Scene image};
\node[b,right=0.30cm of img] (ocr) {OCR\\\scriptsize detected text};
\node[b,right=0.30cm of ocr] (norm) {Normalize\\\scriptsize uppercase / trim};
\node[b,right=0.30cm of norm] (dict) {Dictionary lookup};
\node[b,right=0.30cm of dict] (out) {Translated text};
\draw[arr] (img)--(ocr); \draw[arr] (ocr)--(norm); \draw[arr] (norm)--(dict); \draw[arr] (dict)--(out);
\node[font=\scriptsize\color{MLOrange}] at (2.75,-0.85) {learned};
\node[font=\scriptsize\color{MLMuted}] at (8.3,-0.85) {hard-coded};
\end{tikzpicture}
\end{aifig}

\begin{lstlisting}[style=mlpython]
from PIL import Image
import pytesseract

img = Image.open("street.jpg")
detected_text = pytesseract.image_to_string(img).upper().strip()
print("Detected text:", detected_text)

dictionary = {
    "STOP": "ARRET",
    "SPEED LIMIT": "LIMITATION DE VITESSE",
    "YIELD": "CEDEZ LE PASSAGE",
    "NO PARKING": "STATIONNEMENT INTERDIT",
    "ONE WAY": "SENS UNIQUE",
    "DO NOT ENTER": "ENTREE INTERDITE",
    "STREET": "RUE",
}

translation = next(
    (dictionary[p] for p in sorted(dictionary, key=len, reverse=True)
     if p in detected_text),
    "No dictionary translation found",
)
print("French translation:", translation)
\end{lstlisting}

The \texttt{contains} check tolerates surrounding OCR text, but translation remains limited to stored dictionary phrases.

\begin{workedexample}
\texttt{"STOP\textbackslash nAHEAD"} still matches \texttt{STOP} and returns \texttt{ARRET}; \texttt{S70P} requires partial matching or a clearer image.
\end{workedexample}

\section{Extensions in the OCR examples}

This chapter gives three additional variations.

\begin{description}
  \item[Multi-language dictionary] A STOP sign is mapped independently to French and German dictionaries.
  \item[Fuzzy keyword matching] Partial keywords such as \texttt{PARK} or \texttt{SPEED} trigger translations even when the OCR text is not an exact dictionary key.
  \item[Region-of-interest (ROI) selection] The user draws a rectangle around the relevant image region before OCR, reducing the text-recognition area.
\end{description}

\begin{lstlisting}[style=mlpython]
from PIL import Image
import pytesseract

img = Image.open("stop.jpg")
roi = img.crop((120, 80, 520, 300))  # left, top, right, bottom
text = pytesseract.image_to_string(roi).upper().strip()
print("Detected text:", text)
\end{lstlisting}

\input{labs/06-nlp-part-a}

\begin{quickcheck}
\begin{enumerate}
  \item What role do pretrained FastText vectors play in the similarity example?
  \item Why are the word vectors averaged before cosine similarity is computed?
  \item What produces the caption text in the AlexNet example?
  \item What is the difference between OCR and the dictionary translation step?
  \item Why can selecting a region of interest change the OCR result?
\end{enumerate}
\end{quickcheck}
