\chapter{Robotics: From Sensor Readings to Actions}
\label{ch:robotics}

\chapterguide%
  {Know what a classifier is, what a train/test split is for, and how to read a confusion matrix.}
  {describe the sense--think--act loop; explain wall following as a reactive policy; turn logged sensor data into a supervised control problem; read a decision tree as a set of threshold rules; evaluate a controller with the right metrics; and state why offline accuracy is not the same as safe control.}

Lab~5 learns the decision stage of a robot\textquotesingle s sense--act loop. A SCITOS G5 robot logged 5,456 readings from 24 ultrasonic sensors alongside a wall-following controller\textquotesingle s actions. The lab learns readable if--then rules and compares them with a more powerful, opaque model.

\section{The sense--think--act loop}

Sensors measure the world, a controller selects commands, and actuators change what the sensors see next (\figref{fig:sta-loop}). The controller may use rules, a lookup table, a classifier or a planner such as A$^\star$ (\chapref{ch:informed}).

\begin{aifig}[The sense--think--act loop]{Lab~5 learns the ``think'' stage from sensor--action pairs. The dashed feedback arrow --- actions changing future readings --- is not tested offline.}{fig:sta-loop}
\begin{tikzpicture}[font=\small]
\node[aibox,minimum width=2.9cm,minimum height=1.1cm] (sense) at (0,0) {\textbf{Sense}\\24 ultrasonic distances};
\node[aibox,minimum width=2.9cm,minimum height=1.1cm,fill=MLPaleOrange,draw=MLOrange,line width=1pt] (think) at (4.6,0) {\textbf{Think}\\readings $\to$ action};
\node[aibox,minimum width=2.9cm,minimum height=1.1cm] (act) at (9.2,0) {\textbf{Act}\\one of four motions};
\node[aibox,minimum width=3.4cm,minimum height=0.9cm,fill=MLPaleGray,draw=MLMuted] (world) at (4.6,-2.2) {the room: walls, corners, doorways};
\draw[aiarrow] (sense)--(think);
\draw[aiarrow] (think)--(act);
\draw[aiarrow] (act.south) |- (world.east);
\draw[aiarrow,dashed] (world.west) -| (sense.south);
\node[ailabel,anchor=south,color=MLOrange] at (4.6,0.75) {Lab~5: this box is learned from 5,456 logged pairs};
\node[ailabel,anchor=north,color=MLMuted] at (4.6,-2.72) {dashed arrow: feedback --- the next reading depends on this action};
\end{tikzpicture}
\end{aifig}

\begin{intuition}
Offline control looks like classification: 24 readings in, one of four actions out. In a robot, however, a wrong action can lead to states absent from the training data.
\end{intuition}

\section{Wall following as a reactive policy}

\term{Wall following} keeps the robot moving along a wall at a fixed distance --- in this dataset the robot circled the room clockwise, so the wall stayed on its left. Wall-facing sensors read short distances; others read open space, corners and doorways (\figref{fig:sensor-ring}).

\begin{aifig}[The ultrasonic ring and the wall-following task]{The SCITOS robot seen from above, with its ring of 24 ultrasonic sensors. Sensors on the wall side read short distances; the four that Lab~5 selects are the cluster that measures the wall from slightly different angles. The four actions keep the robot moving along the wall.}{fig:sensor-ring}
\begin{tikzpicture}[font=\small]
\draw[line width=2.2pt,draw=MLMuted] (-4.2,-2.6) -- (4.2,-2.6);
\node[ailabel,anchor=north,color=MLMuted] at (0,-2.7) {wall};
\draw[fill=MLPaleBlue,draw=MLBlue,line width=1pt] (0,0) circle (1.25);
\node[font=\scriptsize\bfseries\color{MLNavy}] at (0,0.15) {SCITOS};
\node[font=\scriptsize\color{MLMuted}] at (0,-0.2) {24 sensors};
\foreach \i in {0,...,23}{
  \pgfmathsetmacro{\ang}{90-\i*15}
  \draw[draw=MLBlue,thick] ({1.25*cos(\ang)},{1.25*sin(\ang)}) -- ({1.5*cos(\ang)},{1.5*sin(\ang)});
}
\foreach \i in {13,14,17,18}{
  \pgfmathsetmacro{\ang}{90-\i*15}
  \draw[draw=MLOrange,line width=1.6pt] ({1.25*cos(\ang)},{1.25*sin(\ang)}) -- ({1.62*cos(\ang)},{1.62*sin(\ang)});
}
\draw[aiptr,draw=MLOrange] (0.35,-1.55) -- (0.35,-2.55);
\draw[aiptr,draw=MLOrange] (-0.65,-1.4) -- (-1.05,-2.55);
\node[ailabel,anchor=west,color=MLOrange,align=left] at (1.0,-1.9) {short readings on the wall side\\(the selected sensors)};
\draw[aiptr,draw=MLMuted!70] (0,1.5) -- (0,2.4);
\node[ailabel,anchor=south,color=MLMuted] at (0,2.45) {long readings towards open space};
\draw[aiarrow,draw=MLGreen,line width=1.2pt] (1.7,0) -- (3.2,0);
\node[ailabel,anchor=west,align=left,color=MLGreen] at (3.3,0) {heading; the four actions are\\Move-Forward, Slight-Left-Turn,\\Slight-Right-Turn, Sharp-Right-Turn};
\end{tikzpicture}
\end{aifig}

The natural controller for this task is \term{reactive}: no map, no memory, just rules on the current readings.

\begin{itemize}
  \item If the wall-side distance is very short, turn sharply away from the wall.
  \item If it is a little short, turn slightly away; if a little long, turn slightly towards.
  \item Otherwise, move forward.
\end{itemize}

These rules are cheap and auditable, but need thresholds. Lab~5 learns them from data.

\section{Learning a controller from logged behaviour}

Each logged sensor--action pair demonstrates the original controller\textquotesingle s policy. Learning from these pairs is \term{behavioural cloning}: supervised classification with 24 features and four classes. Three data properties matter:

\begin{description}
  \item[The classes are imbalanced.] Move-Forward accounts for 40\,\% of rows, Sharp-Right-Turn 38\,\%, Slight-Right-Turn 15\,\% and Slight-Left-Turn 6\,\%. Ignoring slight left turns could still yield 94\,\% accuracy. Report \term{macro F1}, which weights classes equally, and train with \texttt{class\_weight="balanced"}.
  \item[Consecutive rows are not independent.] Nearby readings resemble each other, making the lab\textquotesingle s random-split test score optimistic. The noise experiment probes robustness beyond this split.
  \item[Hold test rows back.] Save a seeded, stratified 70/30 split first. Select features, tune depth and fit the reference model on training rows; evaluate test rows in Question~5.
\end{description}

\subsection{Choosing the sensors}

Rank sensors by \term{mutual information} with actions, using training rows only. Keep and save the top four: US13, US14, US17 and US18, a wall-facing cluster. Mutual information measures how much a reading reduces uncertainty about the action.

\section{An interpretable controller: the decision tree}

A \term{decision tree} applies feature thresholds until it reaches a class. Each root-to-leaf path is an if--then rule learned from demonstrations (\figref{fig:tree-rules}).

\begin{aifig}[A decision tree is a list of threshold rules]{The learned tree\textquotesingle s top three levels and three example rules. Nodes test sensor thresholds; leaves select actions. Limiting depth keeps rules readable.}{fig:tree-rules}
\begin{tikzpicture}[font=\small,
  q/.style={draw=MLBlue,fill=MLPaleBlue,rounded corners=1.5mm,minimum width=2.3cm,minimum height=0.7cm,align=center,font=\scriptsize\color{MLNavy}},
  a/.style={draw=MLGreen,fill=MLPaleTeal,rounded corners=1.5mm,minimum width=2.2cm,minimum height=0.6cm,align=center,font=\scriptsize\bfseries\color{MLNavy}},
  m/.style={draw=MLMuted!60,fill=MLPaleGray,rounded corners=1.5mm,minimum width=2.2cm,minimum height=0.6cm,align=center,font=\scriptsize\color{MLMuted}}]
\node[q] (r) at (0,0) {US14 $\le$ 0.90?};
\node[a] (l1) at (-2.4,-1.3) {Sharp-Right-Turn};
\node[q] (n1) at (2.4,-1.3) {US18 $\le$ 1.00?};
\node[q] (n2) at (0.6,-2.6) {US18 $\le$ 0.50?};
\node[q] (n3) at (4.2,-2.6) {US17 $\le$ 0.90?};
\node[m] (l2) at (0.6,-3.9) {$\cdots$ right turns};
\node[a] (l3) at (3.0,-3.9) {Move-Forward};
\node[m] (l4) at (5.4,-3.9) {$\cdots$ Slight-Left-Turn};
\draw[aiptr] (r)--(l1) node[midway,left,ailabel] {yes};
\draw[aiptr] (r)--(n1) node[midway,right,ailabel] {no};
\draw[aiptr] (n1)--(n2) node[midway,left,ailabel] {yes};
\draw[aiptr] (n1)--(n3) node[midway,right,ailabel] {no};
\draw[aiptr] (n2)--(l2);
\draw[aiptr] (n3)--(l3) node[midway,left,ailabel] {yes};
\draw[aiptr] (n3)--(l4) node[midway,right,ailabel] {no};
\node[ailabel,anchor=west,align=left,text width=5.2cm] at (7.0,-2.0)
  {\textbf{Three paths as rules}\\[3pt]
   IF US14 $\le$ 0.90\,m THEN Sharp-Right-Turn\\[2pt]
   IF US14 $>$ 0.90 AND US18 $>$ 1.00 AND US17 $\le$ 0.79 THEN Move-Forward\\[2pt]
   IF US14 $>$ 0.90 AND US18 $>$ 1.00 AND $0.79<$ US17 $\le 0.90$ THEN Slight-Right-Turn\\[3pt]
   14 leaves in total at depth~5; the full list is \texttt{rule\_controller.txt}.};
\end{tikzpicture}
\end{aifig}

The first split is the wall-following reflex itself: \emph{if the wall-side sensor reads less than 0.9\,m, turn sharply right, away from the wall}. Nobody typed that threshold; it was found from the demonstrations.

\subsection{Choosing the depth}

A deeper tree fits the training rows better and reads worse. The lab treats depth as a hyperparameter and picks it without touching the test rows: it carves a validation set out of the training rows, trains trees of depth 2 to 6 on the remainder, and scores each on the validation set. Macro F1 climbs steeply from depth~2 ($0.46$) to depth~4 ($0.87$), reaches $0.89$ at depth~5 and stops improving. Depth~5 is selected, the tree is refitted on all training rows, and \texttt{export\_text} writes it out as the rule list saved in \texttt{rule\_controller.txt}.

\begin{keyidea}
Interpretability is a design constraint, not a by-product. The lab fixes the number of sensors and caps the tree depth \emph{before} training, then measures what those constraints cost. Loosening either would raise the score and lose the ability to read the controller.
\end{keyidea}

\section{A reference model and a fair comparison}

The reference \term{random forest} uses 200 bootstrap-trained trees and all 24 sensors. It provides a less interpretable comparison for the compact rule tree. Evaluate both on the same held-out rows.

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.30\linewidth}>{\centering\arraybackslash}X>{\centering\arraybackslash}X>{\centering\arraybackslash}X>{\centering\arraybackslash}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Model & Accuracy & Macro precision & Macro recall & Macro F1 \\
\midrule
Rule tree, 4 sensors, depth 5 & 0.923 & 0.879 & 0.921 & 0.895 \\
Random forest, 24 sensors & 0.995 & 0.993 & 0.992 & 0.992 \\
\bottomrule
\end{tabularx}
\end{mltable}

The readable controller sacrifices seven percentage points of accuracy and ten of macro F1. Whether this is acceptable depends on the need to audit its behaviour.

\subsection{Reading the confusion matrix}

The rule tree\textquotesingle s recalls are $0.94$ for Move-Forward, $0.91$ for Sharp-Right-Turn, $0.94$ for Slight-Left-Turn and $0.89$ for Slight-Right-Turn. The confusion matrix shows slight right turns mistaken for forward motion or sharp right turns: the narrow middle band is sensitive to nearby thresholds (\figref{fig:bands}).

\begin{aifig}[Adjacent actions along one sensor]{Why Slight-Right-Turn is the hardest action. Along the wall-side distance the actions occupy adjacent bands, and the slight turn is the narrow one between the other two. A tree draws thresholds at the band edges, and readings near an edge are classified by luck.}{fig:bands}
\begin{tikzpicture}[font=\small]
\draw[aiptr] (0,0) -- (11.5,0) node[anchor=west,ailabel] {distance (m)};
\foreach \x/\l in {0/0.0, 2.6/0.9, 4.4/1.5, 11/3.8}{\draw (\x,0.08)--(\x,-0.08); \node[ailabel,anchor=north] at (\x,-0.12) {\l};}
\fill[MLRed,opacity=0.18] (0,0.25) rectangle (2.6,1.05);
\fill[MLOrange,opacity=0.22] (2.6,0.25) rectangle (4.4,1.05);
\fill[MLGreen,opacity=0.18] (4.4,0.25) rectangle (11,1.05);
\node[font=\scriptsize\bfseries\color{MLRed}] at (1.3,0.65) {Sharp-Right-Turn};
\node[font=\scriptsize\bfseries\color{MLOrange!80!black},align=center] at (3.5,0.65) {Slight-\\Right-Turn};
\node[font=\scriptsize\bfseries\color{MLGreen}] at (7.7,0.65) {Move-Forward};
\draw[draw=MLNavy,dashed,thick] (2.6,-0.35)--(2.6,1.35);
\draw[draw=MLNavy,dashed,thick] (4.4,-0.35)--(4.4,1.35);
\node[ailabel,anchor=south,color=MLNavy] at (2.6,1.38) {threshold};
\node[ailabel,anchor=south,color=MLNavy] at (4.4,1.38) {threshold};
\foreach \x in {2.45,2.75,4.25,4.55}{\node[circle,fill=MLNavy,inner sep=1.4pt] at (\x,0.65) {};}
\node[ailabel,anchor=north,align=center,color=MLMuted] at (3.5,-0.6) {readings near an edge sit a few centimetres\\from being another action};
\end{tikzpicture}
\end{aifig}

\section{Robustness: what noise does to thresholds}

Noise can move readings across action thresholds: 0.90\,m may become 0.87 or 0.93\,m. Lab~5 adds Gaussian noise at 2, 5 and 10\,\% of each selected sensor\textquotesingle s training standard deviation. Test macro F1 falls from $0.90$ to $0.86$, $0.79$ and $0.71$.

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.34\linewidth}>{\centering\arraybackslash}X>{\centering\arraybackslash}X>{\centering\arraybackslash}X>{\centering\arraybackslash}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Noise, as a fraction of training SD & 0 & 2\,\% & 5\,\% & 10\,\% \\
\midrule
Rule-tree macro F1 & 0.895 & 0.858 & 0.787 & 0.706 \\
\bottomrule
\end{tabularx}
\end{mltable}

Possible changes trade simplicity for robustness: more sensors provide redundancy, deeper trees allow finer partitions, and ensembles combine decisions. For a 5\,\% noise budget, justify a choice and its cost.

\begin{pitfall}
Noise added only to \emph{test} readings measures robustness of a clean-trained controller. Training with noisy data is a different experiment; state which you ran.
\end{pitfall}

\section{From offline accuracy to safe control}

Four factors separate fixed-table evaluation from a running robot:

\begin{description}
  \item[Feedback.] A wrong action changes subsequent readings and may lead beyond the training distribution, compounding errors.
  \item[Timing.] Sensor, computation and motor delays can make a correct decision act on an outdated reading.
  \item[Failure modes.] Sensor failures or ultrasound-absorbing walls can produce unfamiliar readings that the classifier still labels confidently.
  \item[Safety limits.] Minimum distances, speed caps and emergency stops must constrain the learned policy; accuracy does not measure them.
\end{description}

The rule tree is an offline \emph{perception-to-action prototype}, not a validated robot controller. Its readability supports further closed-loop testing.

\input{labs/05-robotics}

\begin{quickcheck}
\begin{enumerate}
  \item Why does Lab~5 report macro F1 rather than accuracy, and what accuracy would a controller that never predicts Slight-Left-Turn achieve?
  \item Feature selection and depth tuning both use only the training rows. What would be wrong with the test score if the four sensors had been chosen on all rows?
  \item Write the first rule of the learned tree in plain English and say what physical situation it responds to.
  \item Which action is hardest for the rule tree, and what does \figref{fig:bands} say about why?
  \item Name one reason a controller with 92\,\% offline accuracy could still drive a robot into a wall.
\end{enumerate}
\end{quickcheck}

\begin{checkanswers}
\begin{enumerate}
  \item Accuracy is dominated by the two large classes; a controller that never predicts Slight-Left-Turn could still score about 94\,\%. Macro F1 gives each action equal weight, so the rare turns count.
  \item The test rows would have influenced which sensors were chosen, so the test score would no longer estimate performance on data the model had never seen.
  \item \emph{If sensor US14 reads less than 0.9\,m, turn sharply right.} The robot has come too close to the wall it is following and must steer away from it.
  \item Slight-Right-Turn. Its readings occupy a narrow band between Move-Forward and Sharp-Right-Turn, so both of its thresholds sit close to its own examples and small differences flip the class.
  \item Any of: feedback (one wrong action leads to a sensor state the training data never contained), actuator and sensing delays, a failed sensor returning an unfamiliar reading, or the absence of hard safety limits.
\end{enumerate}
\end{checkanswers}
