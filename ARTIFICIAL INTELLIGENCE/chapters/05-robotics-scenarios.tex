\chapter{Robotics: From Sensor Readings to Actions}
\label{ch:robotics}

\chapterguide%
  {Know what a classifier is, what a train/test split is for, and how to read a confusion matrix.}
  {describe the sense--think--act loop; explain wall following as a reactive policy; turn logged sensor data into a supervised control problem; read a decision tree as a set of threshold rules; evaluate a controller with the right metrics; and state why offline accuracy is not the same as safe control.}

A mobile robot is a loop: it senses, decides, acts, and the world it acted on is what it senses next. Lab~5 studies the middle of that loop. A SCITOS G5 robot with a ring of 24 ultrasonic sensors was driven around a room by a working wall-following controller, and every sensor reading was logged together with the action taken. The lab uses those 5,456 records to build a controller of its own --- one compact enough to read as a list of if--then rules --- and to measure what such a controller gives up against a more powerful but opaque model.

\section{The sense--think--act loop}

Every autonomous robot, from a vacuum cleaner to a planetary rover, runs the loop in \figref{fig:sta-loop}. Sensors turn the physical world into numbers; a controller maps those numbers to a command; actuators execute the command and change the world; the sensors read the changed world. The controller is where the intelligence lives, and it can be a hand-written rule, a lookup table, a learned classifier, or a planner such as the A$^\star$ of \chapref{ch:informed}.

\begin{aifig}[The sense--think--act loop]{The loop every mobile robot runs. Lab~5 replaces the ``think'' box with a classifier learned from logged sensor--action pairs. The dashed arrow is the part the lab cannot test: the robot's action changes what it senses next.}{fig:sta-loop}
\begin{tikzpicture}[font=\small]
\node[aibox,minimum width=2.9cm,minimum height=1.1cm] (sense) at (0,0) {\textbf{Sense}\\24 ultrasonic distances};
\node[aibox,minimum width=2.9cm,minimum height=1.1cm,fill=MLPaleOrange,draw=MLOrange,line width=1pt] (think) at (4.6,0) {\textbf{Think}\\readings $\to$ action};
\node[aibox,minimum width=2.9cm,minimum height=1.1cm] (act) at (9.2,0) {\textbf{Act}\\one of four motions};
\node[aibox,minimum width=3.4cm,minimum height=0.9cm,fill=MLPaleGray,draw=MLMuted] (world) at (4.6,-2.2) {the room: walls, corners, doorways};
\draw[aiarrow] (sense)--(think);
\draw[aiarrow] (think)--(act);
\draw[aiarrow] (act.south) |- (world.east);
\draw[aiarrow,dashed] (world.west) -| (sense.south);
\node[ailabel,anchor=south,color=MLOrange] at (4.6,0.75) {Lab~5: this box is learned from 5,456 logged pairs};
\node[ailabel,anchor=north,color=MLMuted] at (4.6,-2.72) {dashed arrow: feedback --- the next reading depends on this action};
\end{tikzpicture}
\end{aifig}

\begin{intuition}
Offline, the ``think'' box looks like an ordinary classification problem: 24 numbers in, one of four labels out. What makes robotics different is the dashed arrow. A wrong action does not just cost one point of accuracy; it puts the robot somewhere the training data never saw.
\end{intuition}

\section{Wall following as a reactive policy}

The task is \term{wall following}: keep moving while holding a fixed distance to the wall on the robot's right. The sensors are arranged around the body (\figref{fig:sensor-ring}); the ones facing the wall read short distances, the ones facing the open room read long ones, and the sensors near the front see corners and doorways coming.

\begin{aifig}[The ultrasonic ring and the wall-following task]{The SCITOS robot seen from above, with its ring of 24 ultrasonic sensors. Sensors on the wall side read short distances; the four that Lab~5 selects are the cluster that measures the wall from slightly different angles. The four actions keep the robot moving along the wall.}{fig:sensor-ring}
\begin{tikzpicture}[font=\small]
\draw[line width=2.2pt,draw=MLMuted] (-4.2,-2.6) -- (4.2,-2.6);
\node[ailabel,anchor=north,color=MLMuted] at (0,-2.7) {wall};
\draw[fill=MLPaleBlue,draw=MLBlue,line width=1pt] (0,0) circle (1.25);
\node[font=\scriptsize\bfseries\color{MLNavy}] at (0,0.15) {SCITOS};
\node[font=\scriptsize\color{MLMuted}] at (0,-0.2) {24 sensors};
\foreach \i in {0,...,23}{
  \pgfmathsetmacro{\ang}{90-\i*15}
  \draw[draw=MLBlue,thick] ({1.25*cos(\ang)},{1.25*sin(\ang)}) -- ({1.5*cos(\ang)},{1.5*sin(\ang)});
}
\foreach \i in {13,14,17,18}{
  \pgfmathsetmacro{\ang}{90-\i*15}
  \draw[draw=MLOrange,line width=1.6pt] ({1.25*cos(\ang)},{1.25*sin(\ang)}) -- ({1.62*cos(\ang)},{1.62*sin(\ang)});
}
\draw[aiptr,draw=MLOrange] (0.35,-1.55) -- (0.35,-2.55);
\draw[aiptr,draw=MLOrange] (-0.65,-1.4) -- (-1.05,-2.55);
\node[ailabel,anchor=west,color=MLOrange,align=left] at (1.0,-1.9) {short readings on the wall side\\(the selected sensors)};
\draw[aiptr,draw=MLMuted!70] (0,1.5) -- (0,2.4);
\node[ailabel,anchor=south,color=MLMuted] at (0,2.45) {long readings towards open space};
\draw[aiarrow,draw=MLGreen,line width=1.2pt] (1.7,0) -- (3.2,0);
\node[ailabel,anchor=west,align=left,color=MLGreen] at (3.3,0) {heading; the four actions are\\Move-Forward, Slight-Left-Turn,\\Slight-Right-Turn, Sharp-Right-Turn};
\end{tikzpicture}
\end{aifig}

The natural controller for this task is \term{reactive}: no map, no memory, just rules on the current readings.

\begin{itemize}
  \item If the wall-side distance is very short, turn sharply away from the wall.
  \item If it is a little short, turn slightly away; if a little long, turn slightly towards.
  \item Otherwise, move forward.
\end{itemize}

Such rules are cheap to run and easy to audit, and their weakness is equally clear: the thresholds are numbers someone has to choose. Lab~5 chooses them from data.

\section{Learning a controller from logged behaviour}

Because a working controller was driving the robot when the data was logged, every row is a demonstration: \emph{in this sensor state, the right action was this one}. Learning a policy from such pairs is \term{behavioural cloning}, and technically it is supervised classification with 24 numeric features and four classes. Three properties of the data shape every decision in the lab.

\begin{description}
  \item[The classes are imbalanced.] Move-Forward is 40\,\% of the rows, Sharp-Right-Turn 38\,\%, Slight-Right-Turn 15\,\% and Slight-Left-Turn only 6\,\%. A controller that never turns slightly left would still score 94\,\% accuracy. The lab therefore reports \term{macro F1} --- the F1 score averaged over the four classes with equal weight --- so that the rare actions count as much as the common ones, and it trains with \texttt{class\_weight="balanced"} so that the learner does not ignore them.
  \item[Consecutive rows are not independent.] The readings were logged as the robot moved, so neighbouring rows look alike. A random split is still what the lab uses, and its test score is therefore somewhat optimistic; the noise experiment later is one way to probe beyond it.
  \item[The test rows are held back from everything.] A stratified 70/30 split with a fixed seed is made first and saved. Feature selection, depth tuning and the reference model all use the training rows only; the test rows are read once, in Question~5.
\end{description}

\subsection{Choosing the sensors}

Twenty-four sensors are more than a readable controller needs. The lab ranks them by \term{mutual information} with the action --- how much knowing the reading reduces uncertainty about which action was taken --- computed on the training rows only, and keeps the top four. They turn out to be US13, US14, US17 and US18, a cluster from one short arc of the ring: the sensors that see the wall. The ranking is saved so that the choice can be audited.

\section{An interpretable controller: the decision tree}

A \term{decision tree} is a sequence of threshold tests on single features that ends in a class. Read from the root, every path is an if--then rule; read as a whole, the tree \emph{is} a reactive controller of the kind sketched above, with the thresholds chosen to fit the demonstrations (\figref{fig:tree-rules}).

\begin{aifig}[A decision tree is a list of threshold rules]{The top three levels of the tree Lab~5 learns, and three of its paths written as rules. Each internal node compares one sensor with one threshold; each leaf is an action. Depth limits how many tests a rule may contain, which is what keeps the controller readable.}{fig:tree-rules}
\begin{tikzpicture}[font=\small,
  q/.style={draw=MLBlue,fill=MLPaleBlue,rounded corners=1.5mm,minimum width=2.3cm,minimum height=0.7cm,align=center,font=\scriptsize\color{MLNavy}},
  a/.style={draw=MLGreen,fill=MLPaleTeal,rounded corners=1.5mm,minimum width=2.2cm,minimum height=0.6cm,align=center,font=\scriptsize\bfseries\color{MLNavy}},
  m/.style={draw=MLMuted!60,fill=MLPaleGray,rounded corners=1.5mm,minimum width=2.2cm,minimum height=0.6cm,align=center,font=\scriptsize\color{MLMuted}}]
\node[q] (r) at (0,0) {US14 $\le$ 0.90?};
\node[a] (l1) at (-2.4,-1.3) {Sharp-Right-Turn};
\node[q] (n1) at (2.4,-1.3) {US18 $\le$ 1.00?};
\node[q] (n2) at (0.6,-2.6) {US18 $\le$ 0.50?};
\node[q] (n3) at (4.2,-2.6) {US17 $\le$ 0.90?};
\node[m] (l2) at (0.6,-3.9) {$\cdots$ right turns};
\node[a] (l3) at (3.0,-3.9) {Move-Forward};
\node[m] (l4) at (5.4,-3.9) {$\cdots$ Slight-Left-Turn};
\draw[aiptr] (r)--(l1) node[midway,left,ailabel] {yes};
\draw[aiptr] (r)--(n1) node[midway,right,ailabel] {no};
\draw[aiptr] (n1)--(n2) node[midway,left,ailabel] {yes};
\draw[aiptr] (n1)--(n3) node[midway,right,ailabel] {no};
\draw[aiptr] (n2)--(l2);
\draw[aiptr] (n3)--(l3) node[midway,left,ailabel] {yes};
\draw[aiptr] (n3)--(l4) node[midway,right,ailabel] {no};
\node[ailabel,anchor=west,align=left,text width=5.2cm] at (7.0,-2.0)
  {\textbf{Three paths as rules}\\[3pt]
   IF US14 $\le$ 0.90\,m THEN Sharp-Right-Turn\\[2pt]
   IF US14 $>$ 0.90 AND US18 $>$ 1.00 AND US17 $\le$ 0.79 THEN Move-Forward\\[2pt]
   IF US14 $>$ 0.90 AND US18 $>$ 1.00 AND $0.79<$ US17 $\le 0.90$ THEN Slight-Right-Turn\\[3pt]
   14 leaves in total at depth~5; the full list is \texttt{rule\_controller.txt}.};
\end{tikzpicture}
\end{aifig}

The first split is the wall-following reflex itself: \emph{if the wall-side sensor reads less than 0.9\,m, turn sharply right}. Nobody typed that threshold; it was found from the demonstrations.

\subsection{Choosing the depth}

A deeper tree fits the training rows better and reads worse. The lab treats depth as a hyperparameter and picks it without touching the test rows: it carves a validation set out of the training rows, trains trees of depth 2 to 6 on the remainder, and scores each on the validation set. Macro F1 climbs steeply from depth~2 ($0.46$) to depth~4 ($0.87$), reaches $0.89$ at depth~5 and stops improving. Depth~5 is selected, the tree is refitted on all training rows, and \texttt{export\_text} writes it out as the rule list saved in \texttt{rule\_controller.txt}.

\begin{keyidea}
Interpretability is a design constraint, not a by-product. The lab fixes the number of sensors and caps the tree depth \emph{before} training, then measures what those constraints cost. Loosening either would raise the score and lose the ability to read the controller.
\end{keyidea}

\section{A reference model and a fair comparison}

To know what the constraints cost, the lab also trains a \term{random forest} --- 200 trees on all 24 sensors, each grown on a bootstrap sample, voting together. It is not a candidate controller; nobody can read 200 trees. It is a ceiling: roughly the best accuracy this data supports. Both models are evaluated once on the same held-out rows.

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.30\linewidth}>{\centering\arraybackslash}X>{\centering\arraybackslash}X>{\centering\arraybackslash}X>{\centering\arraybackslash}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Model & Accuracy & Macro precision & Macro recall & Macro F1 \\
\midrule
Rule tree, 4 sensors, depth 5 & 0.923 & 0.879 & 0.921 & 0.895 \\
Random forest, 24 sensors & 0.995 & 0.993 & 0.992 & 0.992 \\
\bottomrule
\end{tabularx}
\end{mltable}

Seven points of accuracy and ten of macro F1 is the price of a controller that fits on one page. Whether that price is worth paying depends on who must sign off on the robot's behaviour, which is a question the numbers cannot answer on their own.

\subsection{Reading the confusion matrix}

A single score hides \emph{which} actions the controller confuses. The per-class table for the rule tree shows recall of $0.94$ for Move-Forward, $0.91$ for Sharp-Right-Turn, $0.94$ for Slight-Left-Turn and $0.89$ for Slight-Right-Turn, the hardest action. The confusion matrix explains why: slight right turns are mistaken for their two neighbours, Move-Forward and Sharp-Right-Turn. \figref{fig:bands} shows the geometry. Along the wall-side distance the three actions occupy adjacent bands, and a ``slight'' turn is the narrow band in the middle; a threshold placed on either side of it is close to some of its examples.

\begin{aifig}[Adjacent actions along one sensor]{Why Slight-Right-Turn is the hardest action. Along the wall-side distance the actions occupy adjacent bands, and the slight turn is the narrow one between the other two. A tree draws thresholds at the band edges, and readings near an edge are classified by luck.}{fig:bands}
\begin{tikzpicture}[font=\small]
\draw[aiptr] (0,0) -- (11.5,0) node[anchor=west,ailabel] {distance (m)};
\foreach \x/\l in {0/0.0, 2.6/0.9, 4.4/1.5, 11/3.8}{\draw (\x,0.08)--(\x,-0.08); \node[ailabel,anchor=north] at (\x,-0.12) {\l};}
\fill[MLRed,opacity=0.18] (0,0.25) rectangle (2.6,1.05);
\fill[MLOrange,opacity=0.22] (2.6,0.25) rectangle (4.4,1.05);
\fill[MLGreen,opacity=0.18] (4.4,0.25) rectangle (11,1.05);
\node[font=\scriptsize\bfseries\color{MLRed}] at (1.3,0.65) {Sharp-Right-Turn};
\node[font=\scriptsize\bfseries\color{MLOrange!80!black},align=center] at (3.5,0.65) {Slight-\\Right-Turn};
\node[font=\scriptsize\bfseries\color{MLGreen}] at (7.7,0.65) {Move-Forward};
\draw[draw=MLNavy,dashed,thick] (2.6,-0.35)--(2.6,1.35);
\draw[draw=MLNavy,dashed,thick] (4.4,-0.35)--(4.4,1.35);
\node[ailabel,anchor=south,color=MLNavy] at (2.6,1.38) {threshold};
\node[ailabel,anchor=south,color=MLNavy] at (4.4,1.38) {threshold};
\foreach \x in {2.45,2.75,4.25,4.55}{\node[circle,fill=MLNavy,inner sep=1.4pt] at (\x,0.65) {};}
\node[ailabel,anchor=north,align=center,color=MLMuted] at (3.5,-0.6) {readings near an edge sit a few centimetres\\from being another action};
\end{tikzpicture}
\end{aifig}

\section{Robustness: what noise does to thresholds}

Ultrasonic sensors are noisy: a reading of 0.90\,m may come back as 0.87 or 0.93 a moment later. A threshold controller has no tolerance for this. A reading just inside a band is classified correctly; the same reading plus a little noise crosses the threshold and becomes a different action. The lab measures the effect directly: Gaussian noise with a standard deviation of 2, 5 and 10\,\% of each selected sensor's training standard deviation is added to the test readings, and the rule tree's macro F1 falls from $0.90$ to $0.86$, $0.79$ and $0.71$.

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.34\linewidth}>{\centering\arraybackslash}X>{\centering\arraybackslash}X>{\centering\arraybackslash}X>{\centering\arraybackslash}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Noise, as a fraction of training SD & 0 & 2\,\% & 5\,\% & 10\,\% \\
\midrule
Rule-tree macro F1 & 0.895 & 0.858 & 0.787 & 0.706 \\
\bottomrule
\end{tabularx}
\end{mltable}

Three remedies exist, and each costs something the lab deliberately held fixed: more sensors give the tree redundant evidence, a deeper tree can carve finer bands, and an ensemble averages away single-threshold flips. The lab's deliverable asks which you would choose for a 5\,\% noise budget and why; the honest answer names the cost.

\begin{pitfall}
Adding noise to the \emph{test} readings only, as the lab does, measures how a controller trained on clean data copes with a noisier sensor. It does not measure what a controller trained on noisy data could achieve, which is a different and usually better number. State which experiment you ran.
\end{pitfall}

\section{From offline accuracy to safe control}

The scores above are computed on a fixed table of readings. A running robot is not a fixed table, and four things separate the two.

\begin{description}
  \item[Feedback.] Each action changes the next reading. A controller that is wrong 8\,\% of the time on independent rows may be wrong far more often in practice, because one wrong turn leads to states the training log never contained.
  \item[Timing.] Sensors sample, controllers compute and motors respond with delays. A rule that is correct for the reading it sees may be acting on a world that has already moved.
  \item[Failure modes.] A sensor that returns its maximum range when it fails, or a wall that absorbs ultrasound, produces readings the classifier has never seen and will classify anyway --- confidently.
  \item[Safety limits.] A real controller wraps any learned policy in hard constraints: minimum distances, speed caps, an emergency stop. None of these appear in a classification score.
\end{description}

The lab's own summary is the right framing: the rule tree is a \emph{perception-to-action prototype}, evaluated offline, whose readability makes it a good starting point for a controller that would then have to be tested in the loop.

\input{labs/05-robotics}

\begin{quickcheck}
\begin{enumerate}
  \item Why does Lab~5 report macro F1 rather than accuracy, and what accuracy would a controller that never predicts Slight-Left-Turn achieve?
  \item Feature selection and depth tuning both use only the training rows. What would be wrong with the test score if the four sensors had been chosen on all rows?
  \item Write the first rule of the learned tree in plain English and say what physical situation it responds to.
  \item Which action is hardest for the rule tree, and what does \figref{fig:bands} say about why?
  \item Name one reason a controller with 92\,\% offline accuracy could still drive a robot into a wall.
\end{enumerate}
\end{quickcheck}
