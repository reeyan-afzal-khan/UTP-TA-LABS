\chapter{Uninformed Search: BFS, DFS, and Uniform-Cost Search}
\label{ch:uninformed}

\chapterguide%
  {Be comfortable reading a graph, a grid, and a simple Python container such as a list, set, or dictionary.}
  {represent a search problem, explain frontier and visited sets, trace BFS and DFS, use Uniform-Cost Search on weighted graphs, and recognize when each method is appropriate.}

The first laboratory introduces Breadth-First Search (BFS), Depth-First Search (DFS), and Uniform-Cost Search (UCS). These \term{uninformed} methods use no estimate of remaining distance; they differ in frontier order.

\section{A search problem is more than a graph}

The exercises use graphs, mazes, word transformations, and binary trees. These domains look different, but each can be expressed with the same ingredients.

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.15\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Ingredient & Meaning in the laboratory problems \\
\midrule
State & A graph node such as \texttt{A}; a maze coordinate; a word such as \texttt{hot}; or a tree node. \\
Start & The state where the search begins, for example \texttt{A} or \texttt{S}. \\
Goal & The desired destination, for example \texttt{G} or \texttt{cog}. \\
Successors & Legal next states, obtained from graph adjacency, grid movement, one-letter changes, or child links. \\
Path & The ordered sequence of states used to reach the current state. \\
Path cost & The total edge or movement cost accumulated so far; UCS explicitly uses this quantity. \\
\bottomrule
\end{tabularx}
\end{mltable}

\begin{intuition}
A search algorithm needs only a successor function and a rule for selecting the next frontier state; the state may represent a city, word, or maze cell.
\end{intuition}

\section{The frontier and the visited set}

Most graph-search implementations maintain two central structures.

\begin{description}
  \item[Frontier] States that have been discovered but not fully explored. The data structure used for this frontier gives BFS, DFS, and UCS their characteristic behavior.
  \item[Visited set] States already processed. It prevents repeated work and is essential when the graph contains cycles or loops.
\end{description}

\begin{aifig}[The general graph-search loop]{The graph-search loop shared by BFS, DFS, and
UCS. Only the shaded box differs between the three algorithms: BFS takes the oldest frontier
entry, DFS the newest, UCS the cheapest. Everything else --- the successor generation, the
goal test, the visited check --- is identical code.}{fig:search-loop}
\begin{tikzpicture}[font=\small]
\node[aibox,minimum width=2.3cm] (start) at (0,0) {Start\\state};
\node[aibox,minimum width=2.6cm,fill=MLPaleOrange,draw=MLOrange,line width=1pt]
  (pick) at (3.6,0) {\textbf{Select from}\\\textbf{frontier}};
\node[aibox,minimum width=2.4cm] (goal) at (7.2,0) {Goal\\test?};
\node[aibox,minimum width=2.6cm,fill=MLPaleTeal,draw=MLGreen] (done) at (10.7,0) {Return\\the path};
\node[aibox,minimum width=2.6cm] (expand) at (7.2,-2.1) {Generate\\successors};
\node[aibox,minimum width=2.6cm] (vis) at (3.6,-2.1) {Not already\\visited?};

\draw[aiarrow] (start)--(pick);
\draw[aiarrow] (pick)--(goal);
\draw[aiarrow] (goal)--(done) node[midway,above,ailabel,color=MLGreen] {yes};
\draw[aiarrow] (goal)--(expand) node[midway,right,ailabel,color=MLRed] {no};
\draw[aiarrow] (expand)--(vis);
\draw[aiarrow] (vis) -- (pick) node[midway,right,ailabel,color=MLGreen] {keep};
\node[ailabel,anchor=south,align=center,color=MLOrange] at (3.6,0.72)
  {\textbf{the only line that changes:} FIFO / LIFO / min-cost};
\node[ailabel,anchor=east,align=right] at (2.15,-2.1) {discard\\duplicates};
\end{tikzpicture}
\end{aifig}

\section{Breadth-First Search}

\term{Breadth-First Search} expands states level by level. From \texttt{A}, it processes \texttt{B} and \texttt{C} before deeper states.

A first-in, first-out (FIFO) queue implements this rule:

\begin{mlsteps}
  \item Put the start state in the queue.
  \item Remove the oldest state from the front.
  \item If it is the goal, return its path.
  \item Add unvisited successors to the back of the queue.
  \item Repeat until the queue is empty.
\end{mlsteps}

For the unweighted graph from Lab 1,
\[
A\rightarrow\{B,C\},\quad B\rightarrow\{D,E\},\quad C\rightarrow\{F\},
\]
with \(D,E,F\rightarrow G\), BFS returns a path with the fewest edges because every step is treated as equal cost.

\begin{workedexample}
With the stated neighbor order, BFS expands \texttt{A}, \texttt{B}, and \texttt{C} before the next layer; \texttt{D} then yields the path \texttt{A -> B -> D -> G}.

The queue trace below separates the state being expanded from the paths still waiting. The leftmost path is removed next.
\[
\begin{array}{c|l}
\text{expand} & \text{frontier after expansion} \\
\hline
A & [A,B],\ [A,C] \\
B & [A,C],\ [A,B,D],\ [A,B,E] \\
C & [A,B,D],\ [A,B,E],\ [A,C,F] \\
D & [A,B,E],\ [A,C,F],\ [A,B,D,G]
\end{array}
\]
The returned path has three edges and is selected by queue order, not a distance estimate.
\end{workedexample}

\subsection{BFS in a maze}

The maze exercise uses four-direction movement. A cell is a state, and a legal successor is an in-bounds adjacent cell that is not a wall. BFS is suitable when every move has equal cost and the objective is the shortest number of steps.

\begin{lstlisting}[style=mlpython]
from collections import deque

def bfs_grid(grid, start, goal):
    rows, cols = len(grid), len(grid[0])
    visited = {start}
    queue = deque([(start, [start])])

    while queue:
        (x, y), path = queue.popleft()
        if (x, y) == goal:
            return path

        for dx, dy in [(-1, 0), (1, 0), (0, -1), (0, 1)]:
            nx, ny = x + dx, y + dy
            if (0 <= nx < rows and 0 <= ny < cols
                    and grid[nx][ny] == 0
                    and (nx, ny) not in visited):
                visited.add((nx, ny))
                queue.append(((nx, ny), path + [(nx, ny)]))
    return None
\end{lstlisting}

\figref{fig:bfs-wave} shows why this works. BFS reaches every cell at distance~1 before any
at distance~2, so the first time it touches the goal it has arrived by a shortest route.

\begin{aifig}[BFS expanding as a wavefront in a grid]{BFS in a grid, shaded by distance from the start. The frontier is always one connected ring, so the goal is first touched at its true shortest distance. Dark cells are walls.}{fig:bfs-wave}
\begin{tikzpicture}[font=\small]
\def\W{0.72}
\def\wallmark{W}
% grid: 0 = open, 1 = wall; distances precomputed
\foreach \r [count=\ri from 0] in {%
  {0,1,2,3,4},
  {1,W,3,W,5},
  {2,3,4,5,6},
  {3,W,W,W,7},
  {4,5,6,7,8}}{
  \foreach \v [count=\ci from 0] in \r{
    \ifx\v\wallmark
      \node[aiwall,minimum width=\W cm,minimum height=\W cm] at (\W*\ci,-\W*\ri) {};
    \else
      \pgfmathsetmacro{\shade}{12+10*\v}
      \node[draw=MLBlue!45,fill=MLBlue!\shade,minimum width=\W cm,minimum height=\W cm,
            font=\scriptsize\bfseries\color{MLNavy},inner sep=0pt]
            at (\W*\ci,-\W*\ri) {\v};
    \fi
  }
}
\node[ainodehot,minimum size=6.5mm] at (0,0) {S};
\node[ainodedone,minimum size=6.5mm] at (4*\W,-4*\W) {G};
\node[ailabel,anchor=west,align=left,text width=5.4cm] at (4.05,-1.1)
  {Each number is the step at which BFS first reaches that cell.
   \\[0.4em]
   The goal is found at distance~8, and no shorter route exists:
   BFS could not have reached it earlier without skipping a ring.
   \\[0.4em]
   Dark cells are walls and are never entered.};
\end{tikzpicture}
\end{aifig}

\begin{pitfall}
Mark a state visited when it is inserted into the BFS queue, not only when it is removed. Otherwise the same state can be inserted many times by different parents.
\end{pitfall}

\section{Depth-First Search}

\term{Depth-First Search} follows one branch as far as possible before trying alternatives. A stack or recursion naturally implements this behavior.

\begin{mlsteps}
  \item Start at the current state and perform the goal test.
  \item Mark the state visited.
  \item Choose an unvisited successor.
  \item Recurse down that branch.
  \item If the branch fails, return to the previous state and try the next successor.
\end{mlsteps}

The maze implementation expresses this behavior directly through recursion:

\begin{lstlisting}[style=mlpython]
def dfs_maze(grid, x, y, path, visited, goal):
    if (x, y) == goal:
        return path

    visited.add((x, y))
    for dx, dy in [(-1, 0), (1, 0), (0, -1), (0, 1)]:
        nx, ny = x + dx, y + dy
        if (0 <= nx < len(grid) and 0 <= ny < len(grid[0])
                and grid[nx][ny] == 0
                and (nx, ny) not in visited):
            result = dfs_maze(
                grid, nx, ny,
                path + [(nx, ny)],
                visited, goal
            )
            if result:
                return result
    return None
\end{lstlisting}

\begin{intuition}
BFS expands a wavefront; DFS follows one branch. DFS does not guarantee a minimum-edge path.
\end{intuition}

\begin{aifig}[BFS and DFS visit order compared]{BFS and DFS on one tree, each node numbered by visit order. BFS finishes a level before descending; DFS exhausts \texttt{B}'s subtree before touching \texttt{C}. The frontier column is the memory argument: wide for BFS, shallow for DFS.}{fig:bfs-dfs-order}
\begin{tikzpicture}[font=\small]
\newcommand{\ordertree}[7]{%
  \node[ainode,minimum size=7.5mm] (#1a) at (0,1.3) {A};
  \node[ainode,minimum size=7.5mm] (#1b) at (-1.3,0.25) {B};
  \node[ainode,minimum size=7.5mm] (#1c) at (1.3,0.25) {C};
  \node[ainode,minimum size=7.5mm] (#1d) at (-1.95,-0.8) {D};
  \node[ainode,minimum size=7.5mm] (#1e) at (-0.65,-0.8) {E};
  \node[ainode,minimum size=7.5mm] (#1f) at (0.65,-0.8) {F};
  \node[ainode,minimum size=7.5mm] (#1g) at (1.95,-0.8) {G};
  \draw[aiedge] (#1a)--(#1b) (#1a)--(#1c) (#1b)--(#1d) (#1b)--(#1e)
                (#1c)--(#1f) (#1c)--(#1g);
  \foreach \n/\o in {#1a/#2,#1b/#3,#1c/#4,#1d/#5,#1e/#6,#1f/#7}{
    \node[circle,fill=MLOrange,text=white,font=\tiny\bfseries,inner sep=1.1pt]
      at ($(\n.north east)+(0.055,0.055)$) {\o};}
}
% ---- BFS ----
\begin{scope}
\node[font=\bfseries\color{MLNavy}] at (0,2.05) {BFS (queue)};
\ordertree{b}{1}{2}{3}{4}{5}{6}
\node[circle,fill=MLOrange,text=white,font=\tiny\bfseries,inner sep=1.1pt]
  at ($(bg.north east)+(0.055,0.055)$) {7};
\node[ailabel,anchor=north,align=center] at (0,-1.35) {order: A B C D E F G};
\end{scope}
% ---- DFS ----
\begin{scope}[xshift=5.6cm]
\node[font=\bfseries\color{MLNavy}] at (0,2.05) {DFS (stack)};
\ordertree{d}{1}{2}{5}{3}{4}{6}
\node[circle,fill=MLOrange,text=white,font=\tiny\bfseries,inner sep=1.1pt]
  at ($(dg.north east)+(0.055,0.055)$) {7};
\node[ailabel,anchor=north,align=center] at (0,-1.35) {order: A B D E C F G};
\end{scope}
% ---- frontier comparison ----
\begin{scope}[xshift=9.05cm,yshift=1.35cm]
\node[ailabel,anchor=west,color=MLNavy] at (0,0.5) {\textbf{frontier at step 2}};
\node[aicell,minimum width=1.9cm,minimum height=0.5cm,font=\scriptsize\bfseries]
  at (1.05,0) {C D E};
\node[ailabel,anchor=west,color=MLBlue] at (2.15,0) {BFS: wide};
\node[aicell,minimum width=1.9cm,minimum height=0.5cm,font=\scriptsize\bfseries,
      fill=MLPaleTeal,draw=MLGreen] at (1.05,-0.75) {C D E};
\node[ailabel,anchor=west,color=MLGreen] at (2.15,-0.75) {DFS: same};
\node[ailabel,anchor=west,align=left,text width=3.15cm] at (0,-1.95)
  {On a deep space BFS holds $O(b^d)$ states; DFS holds only $O(bm)$.};
\end{scope}
\end{tikzpicture}
\end{aifig}

With left children first, BFS completes each depth while DFS completes the left branch first. Neighbor order changes the sequence, not this distinction.

\subsection{DFS preorder on a binary tree}

The Lab 1 tree has root 1, children 2 and 3, and leaves 4--7. Preorder DFS visits the current node before its left and right subtrees:
\[
1,2,4,5,3,6,7.
\]

\begin{lstlisting}[style=mlpython]
def dfs_preorder(root):
    if not root:
        return []
    return [root.val] + dfs_preorder(root.left) + dfs_preorder(root.right)
\end{lstlisting}

\section{Uniform-Cost Search}

\term{Uniform-Cost Search} expands the frontier path with the smallest accumulated cost \(g(n)\), using a min-priority queue instead of BFS's FIFO queue.

UCS assumes nonnegative edge costs; negative costs invalidate its usual visited-set logic.

For the weighted Lab 1 graph,
\begin{align*}
A&\to B:1, & A&\to C:4,\\
B&\to D:3, & B&\to E:2, & C&\to F:5,\\
D&\to G:4, & E&\to G:1, & F&\to G:2.
\end{align*}
The path through \texttt{B} and \texttt{E} has cost
\[
1+2+1=4,
\]
which is lower than the alternatives shown in the handout.

\begin{workedexample}
UCS starts with \((0,A)\). Expanding \(A\) inserts \((1,B)\) and \((4,C)\); expanding \(B\) inserts \((4,D)\) and \((3,E)\). It therefore selects \(E\) before \(C\), inserts \((4,G)\), and returns \texttt{A -> B -> E -> G} with optimal cost 4. Equal-cost tie order does not affect that minimum cost.
\end{workedexample}

\begin{lstlisting}[style=mlpython]
import heapq

def ucs(graph, start, goal):
    pq = [(0, [start])]
    visited = set()

    while pq:
        cost, path = heapq.heappop(pq)
        node = path[-1]
        if node == goal:
            return path, cost

        if node not in visited:
            visited.add(node)
            for neighbor, weight in graph[node]:
                heapq.heappush(
                    pq,
                    (cost + weight, path + [neighbor])
                )
    return None
\end{lstlisting}

\figref{fig:ucs-vs-bfs} puts the two answers side by side. Both paths use three edges, so
BFS sees no reason to prefer either; UCS adds the weights and finds one costs twice the
other. That is what ``BFS is optimal only for equal step costs'' means in practice.

\begin{aifig}[BFS and UCS on the same weighted graph]{The same weighted graph solved twice.
BFS returns the first three-edge path it encounters, costing 8. UCS orders its frontier by
accumulated cost $g(n)$ and returns the three-edge path costing 4. Neither algorithm is
wrong; they are answering different questions.}{fig:ucs-vs-bfs}
\begin{tikzpicture}[font=\small]
\newcommand{\wgraph}[1]{%
  \node[ainode,minimum size=7mm] (#1A) at (0,0) {A};
  \node[ainode,minimum size=7mm] (#1B) at (1.5,0.95) {B};
  \node[ainode,minimum size=7mm] (#1C) at (1.5,-0.95) {C};
  \node[ainode,minimum size=7mm] (#1D) at (3.0,1.6) {D};
  \node[ainode,minimum size=7mm] (#1E) at (3.0,0.35) {E};
  \node[ainode,minimum size=7mm] (#1F) at (3.0,-0.95) {F};
  \node[ainode,minimum size=7mm] (#1G) at (4.5,0) {G};
}
% ---- BFS ----
\begin{scope}
\node[font=\bfseries\color{MLNavy}] at (2.25,2.35) {BFS: fewest edges};
\wgraph{p}
\draw[aiedge,draw=MLMuted!45] (pA)--(pC) node[midway,below,ailabel] {4};
\draw[aiedge,draw=MLMuted!45] (pB)--(pE) node[midway,above,ailabel] {2};
\draw[aiedge,draw=MLMuted!45] (pC)--(pF) node[midway,below,ailabel] {5};
\draw[aiedge,draw=MLMuted!45] (pE)--(pG) node[midway,below,ailabel] {1};
\draw[aiedge,draw=MLMuted!45] (pF)--(pG) node[midway,below,ailabel] {2};
\draw[aiedge,draw=MLRed,line width=1.2pt] (pA)--(pB) node[midway,above,ailabel,color=MLRed] {1};
\draw[aiedge,draw=MLRed,line width=1.2pt] (pB)--(pD) node[midway,above,ailabel,color=MLRed] {3};
\draw[aiedge,draw=MLRed,line width=1.2pt] (pD)--(pG) node[midway,above,ailabel,color=MLRed] {4};
\node[ailabel,anchor=north,align=center,color=MLRed] at (2.25,-1.75)
  {A $\to$ B $\to$ D $\to$ G\\3 edges, cost $1+3+4=\mathbf{8}$};
\end{scope}
% ---- UCS ----
\begin{scope}[xshift=6.5cm]
\node[font=\bfseries\color{MLNavy}] at (2.25,2.35) {UCS: cheapest total};
\wgraph{q}
\draw[aiedge,draw=MLMuted!45] (qA)--(qC) node[midway,below,ailabel] {4};
\draw[aiedge,draw=MLMuted!45] (qB)--(qD) node[midway,above,ailabel] {3};
\draw[aiedge,draw=MLMuted!45] (qC)--(qF) node[midway,below,ailabel] {5};
\draw[aiedge,draw=MLMuted!45] (qD)--(qG) node[midway,above,ailabel] {4};
\draw[aiedge,draw=MLMuted!45] (qF)--(qG) node[midway,below,ailabel] {2};
\draw[aiedge,draw=MLGreen,line width=1.2pt] (qA)--(qB) node[midway,above,ailabel,color=MLGreen] {1};
\draw[aiedge,draw=MLGreen,line width=1.2pt] (qB)--(qE) node[midway,above,ailabel,color=MLGreen] {2};
\draw[aiedge,draw=MLGreen,line width=1.2pt] (qE)--(qG) node[midway,below,ailabel,color=MLGreen] {1};
\node[ailabel,anchor=north,align=center,color=MLGreen] at (2.25,-1.75)
  {A $\to$ B $\to$ E $\to$ G\\3 edges, cost $1+2+1=\mathbf{4}$};
\end{scope}
\end{tikzpicture}
\end{aifig}

\begin{keyidea}
BFS orders the frontier by depth, DFS by recency/depth, and UCS by accumulated path cost. The graph representation may stay the same while only the frontier policy changes.
\end{keyidea}

\section{Comparing the three methods}

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.15\linewidth}p{0.19\linewidth}p{0.19\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Method & Frontier rule & Main structure & What the lab examples emphasize \\
\midrule
BFS & Shallowest first & FIFO queue & Shortest path in equal-cost graph/grid; level-order tree traversal \\
DFS & Deepest first & Stack / recursion & Reachability, maze exploration, preorder tree traversal \\
UCS & Lowest \(g(n)\) & Min-priority queue & Least-cost path in a weighted graph \\
\bottomrule
\end{tabularx}
\end{mltable}

\subsection{The four properties every search algorithm is judged on}

Choosing between these methods means asking four standard questions, not just ``which one found a path''.

\begin{description}
  \item[Completeness] If a solution exists, is the algorithm guaranteed to find it?
  \item[Optimality] Is the solution it returns guaranteed to be the cheapest one?
  \item[Time complexity] How many states might it expand?
  \item[Space complexity] How many states might it hold in memory at once?
\end{description}

Write \(b\) for the \term{branching factor} (successors per state), \(d\) for the depth of the shallowest goal, and \(m\) for the maximum depth of the search space.

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.09\linewidth}p{0.15\linewidth}p{0.20\linewidth}p{0.15\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Method & Complete? & Optimal? & Time & Space \\
\midrule
BFS & Yes, if \(b\) is finite & Yes, but only when every step costs the same & \(O(b^{d})\) & \(O(b^{d})\) \\
DFS & Not in general & No & \(O(b^{m})\) & \(O(bm)\) \\
UCS & Yes, if step costs \(\geq\varepsilon>0\) & Yes, for any non-negative costs & \(O(b^{1+\lfloor C^\star/\varepsilon\rfloor})\) & same as time \\
\bottomrule
\end{tabularx}
\end{mltable}

Here \(C^\star\) is the cost of the optimal solution and \(\varepsilon\) the smallest step cost.

\figref{fig:branching} shows what $O(b^d)$ costs in practice. At branching factor 3, a goal
eight levels down puts about 9\,800 states in memory; at branching factor 5 the same depth
needs nearly 500\,000. This is why the space column, not the time column, decides which
algorithm survives.

\begin{aifig}[Frontier growth with depth]{States a BFS frontier may hold at depth $d$, for
three branching factors. The vertical axis is logarithmic: each straight line is exponential
growth. DFS stores $O(bm)$ instead, which on these scales is a flat line near the
bottom.}{fig:branching}
\begin{tikzpicture}
\begin{axis}[
  width=0.72\linewidth, height=5.2cm,
  xlabel={depth $d$}, ylabel={states stored},
  xmin=1, xmax=10, ymin=1, ymax=2e7,
  ymode=log, log basis y=10,
  axis lines=left, grid=major, grid style={draw=MLPaleBlue},
  legend columns=4, legend style={at={(0.5,-0.28)},anchor=north},
  domain=1:10, samples=10]
\addplot[draw=MLGreen,very thick] {2^x};   \addlegendentry{$b=2$}
\addplot[draw=MLBlue,very thick]  {3^x};   \addlegendentry{$b=3$}
\addplot[draw=MLRed,very thick]   {5^x};   \addlegendentry{$b=5$}
\addplot[draw=MLMuted,dashed,thick] {5*x}; \addlegendentry{DFS, $O(bm)$}
\end{axis}
\end{tikzpicture}
\end{aifig}

\begin{keyidea}
Read the space column first. BFS and UCS store the whole frontier, which grows exponentially --- memory, not time, is what usually stops them on a real problem. DFS needs only the current branch, \(O(bm)\), which is why it survives on deep spaces that exhaust BFS.

That is the trade: DFS buys its small memory footprint by giving up both completeness and optimality.
\end{keyidea}

\begin{pitfall}
Two entries above are easy to misread.

\textbf{``BFS is optimal''} holds only for \emph{equal} step costs. On the weighted Lab~1 graph, BFS returns \texttt{A -> B -> D -> G} costing 8 while UCS returns \texttt{A -> B -> E -> G} costing 4 --- both use three edges. Fewest edges and cheapest are different questions, and BFS only answers the first.

\textbf{``DFS is not complete''} refers to spaces that are infinite or contain cycles: DFS can descend forever and never come back. On a \emph{finite} graph with a visited set --- every graph in this lab --- DFS does terminate and will find a goal if one is reachable. It still gives no guarantee about which one.
\end{pitfall}

\input{labs/01-search-foundations}

\begin{quickcheck}
\begin{enumerate}
  \item Why does BFS ignore the numerical edge weights in the first graph problem?
  \item What changes in the data structure when moving from BFS to UCS?
  \item Why is a visited set especially important for the ``graph with loops'' UCS problem?
  \item What traversal order does DFS preorder produce for the seven-node binary tree in Lab 1?
\end{enumerate}
\end{quickcheck}

\begin{checkanswers}
\begin{enumerate}
  \item BFS treats every edge as one step, so numerical weights are outside its ordering rule.
  \item Replace the FIFO queue with a min-priority queue ordered by cumulative cost \(g\).
  \item Without a visited or best-cost record, loop edges continually create longer cyclic paths.
  \item \(1,2,4,5,3,6,7\).
\end{enumerate}
\end{checkanswers}

\begin{chapterrecap}
\begin{itemize}
  \item Search problems specify states, successors, a start, a goal test, and optionally path costs.
  \item BFS uses a FIFO queue and is suited to equal-cost shortest-path problems.
  \item DFS follows one branch before backtracking, using recursion or a stack.
  \item UCS uses a priority queue ordered by cumulative cost.
\end{itemize}
\end{chapterrecap}
