\chapter{Uninformed Search: BFS, DFS, and Uniform-Cost Search}
\label{ch:uninformed}

\chapterguide%
  {Read a graph drawn as nodes and edges, and use a Python list, set, dictionary and \texttt{for} loop.}
  {state a route-finding task as a search problem; trace BFS, DFS and UCS by hand; explain which quantity each one optimises; read the search code in \texttt{utils/core.py}; and judge a search algorithm by completeness, optimality, time and memory.}

Lab~1 asks a concrete question: \emph{how do you fly from Kuala Lumpur to Keflav\'ik?} There is no direct flight, so an answer is a chain of routes, and there are many chains. This chapter gives the vocabulary for asking such questions precisely and the three \term{uninformed} algorithms --- Breadth-First Search, Depth-First Search and Uniform-Cost Search --- that answer them without any knowledge of geography. \chapref{ch:informed} adds that knowledge; \chapref{ch:searchpatterns} turns the code into a tested package.

\section{Route finding as a search problem}

A \term{search problem} is defined by five ingredients. The table gives each one in general and as it appears in the airline network built by \texttt{utils/openflights.py}.

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.16\linewidth}p{0.36\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Ingredient & In general & In Lab~1 \\
\midrule
State & A situation the search can be in & An airport, named by its IATA code (\texttt{KUL}) \\
Start & The initial state & The origin airport of a query \\
Goal & The state we want to reach & The destination airport \\
Successors & The states reachable in one step & The airports with a direct route from here \\
Path cost & The accumulated cost of a path & Number of legs, \emph{or} the sum of great-circle distances in km \\
\bottomrule
\end{tabularx}
\end{mltable}

The last row is the important one. The same graph supports two different cost definitions, and the three algorithms in this chapter do not agree on which to use. BFS and DFS treat every route as one step; UCS adds up kilometres. ``Shortest route'' means nothing until the cost is stated.

In Python the graph is a dictionary from each airport to a list of \texttt{(neighbour, distance\_km)} pairs:

\begin{lstlisting}[style=aipython]
graph["KUL"][:3]
# [('ADL', 5681.8), ('AKL', 8703.4), ('ALA', 5130.5)]
\end{lstlisting}

The cleaned OpenFlights graph has 6,464 airports and 37,337 directed routes. It is an educational abstraction: distance stands in for the real costs of fares, schedules and connections, and the lab says so in its interpretation.

\begin{intuition}
A search algorithm needs only two things from a problem: a way to list the successors of a state, and a rule for choosing which frontier state to look at next. Everything about airports lives in the first; everything about BFS, DFS and UCS lives in the second.
\end{intuition}

\section{The graph-search loop}

All three algorithms run the same loop. They keep a \term{frontier} of states that have been discovered but not yet examined, and a record of states already seen so that no state is processed twice. \figref{fig:search-loop} shows the loop; only the shaded box differs between algorithms.

\begin{aifig}[The general graph-search loop]{The graph-search loop shared by BFS, DFS and UCS. Only the shaded box differs: BFS takes the oldest frontier entry, DFS the newest, UCS the cheapest. Successor generation, the goal test and the duplicate check are identical code.}{fig:search-loop}
\begin{tikzpicture}[font=\small]
\node[aibox,minimum width=2.3cm] (start) at (0,0) {Start\\state};
\node[aibox,minimum width=2.6cm,fill=MLPaleOrange,draw=MLOrange,line width=1pt]
  (pick) at (3.6,0) {\textbf{Select from}\\\textbf{frontier}};
\node[aibox,minimum width=2.4cm] (goal) at (7.2,0) {Goal\\test?};
\node[aibox,minimum width=2.6cm,fill=MLPaleTeal,draw=MLGreen] (done) at (10.7,0) {Return\\the path};
\node[aibox,minimum width=2.6cm] (expand) at (7.2,-2.1) {Generate\\successors};
\node[aibox,minimum width=2.6cm] (vis) at (3.6,-2.1) {Not already\\seen?};

\draw[aiarrow] (start)--(pick);
\draw[aiarrow] (pick)--(goal);
\draw[aiarrow] (goal)--(done) node[midway,above,ailabel,color=MLGreen] {yes};
\draw[aiarrow] (goal)--(expand) node[midway,right,ailabel,color=MLRed] {no};
\draw[aiarrow] (expand)--(vis);
\draw[aiarrow] (vis) -- (pick) node[midway,right,ailabel,color=MLGreen] {keep};
\node[ailabel,anchor=south,align=center,color=MLOrange] at (3.6,0.72)
  {\textbf{the only line that changes:} FIFO / LIFO / min-cost};
\node[ailabel,anchor=east,align=right] at (2.15,-2.1) {discard\\duplicates};
\end{tikzpicture}
\end{aifig}

Two bookkeeping choices are shared by every implementation in \texttt{utils/core.py}:

\begin{description}
  \item[Parent links instead of copied paths.] Each discovered state records which state discovered it. The path is rebuilt only once, by following the links back from the goal. This uses far less memory than storing a full path in every frontier entry.
  \item[Effort counters.] Every function counts the states it \term{expands} (generates successors for) and the largest size the frontier reaches. These two numbers, not the wall-clock time, are how the labs compare search effort, because they do not change from one computer to the next.
\end{description}

The BFS function is short enough to read in full; DFS and UCS differ from it in a handful of lines each.

\begin{lstlisting}[style=aipython]
def bfs(graph, start, goal):
    frontier = deque([start])            # FIFO queue
    parent = {start: None}               # also serves as the "seen" set
    expanded, max_frontier = 0, 1
    while frontier:
        state = frontier.popleft()       # oldest entry first
        if state == goal:
            return _result(True, parent, goal, graph, expanded, max_frontier, started)
        expanded += 1
        for neighbour, _ in graph.get(state, ()):
            if neighbour not in parent:
                parent[neighbour] = state
                frontier.append(neighbour)
        max_frontier = max(max_frontier, len(frontier))
    return _result(False, parent, goal, graph, expanded, max_frontier, started)
\end{lstlisting}

\begin{pitfall}
Mark a state as seen when it is \emph{added} to the frontier, not when it is removed. The \texttt{parent} dictionary does this: a state gets a parent the moment it is discovered. Marking on removal lets the same airport be queued many times by different neighbours, which inflates the frontier and, for BFS, can attach a longer path than necessary.
\end{pitfall}

\section{Breadth-First Search}

\term{Breadth-First Search (BFS)} uses a first-in, first-out queue, so it examines every state one step from the start before any state two steps away, and so on outwards. The first time it touches the goal, it has arrived by a path with the fewest possible edges.

\begin{mlsteps}
  \item Put the start state in the queue.
  \item Remove the \emph{oldest} state from the front of the queue.
  \item If it is the goal, rebuild the path from the parent links and stop.
  \item Add each successor that has not been seen to the back of the queue, recording its parent.
  \item Repeat until the queue is empty.
\end{mlsteps}

\figref{fig:bfs-wave} shows why the first arrival is a fewest-edges path: BFS expands as a wavefront, and the wavefront cannot reach distance~8 without passing through distance~7 first.

\begin{aifig}[BFS expanding as a wavefront]{BFS on a grid, each cell shaded by the step at which it is first reached. The frontier is always one connected ring, so the goal is first touched at its true fewest-steps distance. Dark cells are walls and are never entered.}{fig:bfs-wave}
\begin{tikzpicture}[font=\small]
\def\W{0.72}
\def\wallmark{W}
\foreach \r [count=\ri from 0] in {%
  {0,1,2,3,4},
  {1,W,3,W,5},
  {2,3,4,5,6},
  {3,W,W,W,7},
  {4,5,6,7,8}}{
  \foreach \v [count=\ci from 0] in \r{
    \ifx\v\wallmark
      \node[aiwall,minimum width=\W cm,minimum height=\W cm] at (\W*\ci,-\W*\ri) {};
    \else
      \pgfmathsetmacro{\shade}{12+10*\v}
      \node[draw=MLBlue!45,fill=MLBlue!\shade,minimum width=\W cm,minimum height=\W cm,
            font=\scriptsize\bfseries\color{MLNavy},inner sep=0pt]
            at (\W*\ci,-\W*\ri) {\v};
    \fi
  }
}
\node[ainodehot,minimum size=6.5mm] at (0,0) {S};
\node[ainodedone,minimum size=6.5mm] at (4*\W,-4*\W) {G};
\node[ailabel,anchor=west,align=left,text width=5.4cm] at (4.05,-1.1)
  {Each number is the step at which BFS first reaches that cell.
   \\[0.4em]
   The goal is found at distance~8, and no shorter route exists:
   BFS could not have reached it earlier without skipping a ring.
   \\[0.4em]
   On the airline graph the ``rings'' are airports one leg away, two legs away, and so on.};
\end{tikzpicture}
\end{aifig}

\begin{workedexample}
On the airline graph, BFS from \texttt{KUL} to \texttt{KEF} expands 284 airports and returns \texttt{KUL -> AMS -> KEF}: two legs, 12,274~km. Every other route to Keflav\'ik needs at least two legs as well, so on the \emph{fewest legs} objective this answer is optimal. It is not the shortest in kilometres, because BFS never looked at the distances.
\end{workedexample}

\section{Depth-First Search}

\term{Depth-First Search (DFS)} replaces the queue with a stack: the \emph{most recently} discovered state is examined next. The search therefore follows one branch as deep as it can go and only backs up when the branch is exhausted. \figref{fig:bfs-dfs-order} contrasts the two visit orders on one tree.

\begin{aifig}[BFS and DFS visit order compared]{BFS and DFS on one tree, each node numbered by visit order. BFS finishes a level before descending; DFS exhausts \texttt{B}'s subtree before touching \texttt{C}. The frontier column is the memory argument: wide for BFS, shallow for DFS.}{fig:bfs-dfs-order}
\begin{tikzpicture}[font=\small]
\newcommand{\ordertree}[7]{%
  \node[ainode,minimum size=7.5mm] (#1a) at (0,1.3) {A};
  \node[ainode,minimum size=7.5mm] (#1b) at (-1.3,0.25) {B};
  \node[ainode,minimum size=7.5mm] (#1c) at (1.3,0.25) {C};
  \node[ainode,minimum size=7.5mm] (#1d) at (-1.95,-0.8) {D};
  \node[ainode,minimum size=7.5mm] (#1e) at (-0.65,-0.8) {E};
  \node[ainode,minimum size=7.5mm] (#1f) at (0.65,-0.8) {F};
  \node[ainode,minimum size=7.5mm] (#1g) at (1.95,-0.8) {G};
  \draw[aiedge] (#1a)--(#1b) (#1a)--(#1c) (#1b)--(#1d) (#1b)--(#1e)
                (#1c)--(#1f) (#1c)--(#1g);
  \foreach \n/\o in {#1a/#2,#1b/#3,#1c/#4,#1d/#5,#1e/#6,#1f/#7}{
    \node[circle,fill=MLOrange,text=white,font=\tiny\bfseries,inner sep=1.1pt]
      at ($(\n.north east)+(0.055,0.055)$) {\o};}
}
\begin{scope}
\node[font=\bfseries\color{MLNavy}] at (0,2.05) {BFS (queue)};
\ordertree{b}{1}{2}{3}{4}{5}{6}
\node[circle,fill=MLOrange,text=white,font=\tiny\bfseries,inner sep=1.1pt]
  at ($(bg.north east)+(0.055,0.055)$) {7};
\node[ailabel,anchor=north,align=center] at (0,-1.35) {order: A B C D E F G};
\end{scope}
\begin{scope}[xshift=5.6cm]
\node[font=\bfseries\color{MLNavy}] at (0,2.05) {DFS (stack)};
\ordertree{d}{1}{2}{5}{3}{4}{6}
\node[circle,fill=MLOrange,text=white,font=\tiny\bfseries,inner sep=1.1pt]
  at ($(dg.north east)+(0.055,0.055)$) {7};
\node[ailabel,anchor=north,align=center] at (0,-1.35) {order: A B D E C F G};
\end{scope}
\begin{scope}[xshift=9.05cm,yshift=1.35cm]
\node[ailabel,anchor=west,color=MLNavy] at (0,0.5) {\textbf{frontier at step 2}};
\node[aicell,minimum width=1.9cm,minimum height=0.5cm,font=\scriptsize\bfseries]
  at (1.05,0) {C D E};
\node[ailabel,anchor=west,color=MLBlue] at (2.15,0) {BFS: wide};
\node[aicell,minimum width=1.9cm,minimum height=0.5cm,font=\scriptsize\bfseries,
      fill=MLPaleTeal,draw=MLGreen] at (1.05,-0.75) {C D E};
\node[ailabel,anchor=west,color=MLGreen] at (2.15,-0.75) {DFS: same};
\node[ailabel,anchor=west,align=left,text width=3.15cm] at (0,-1.95)
  {On a deep space BFS holds $O(b^d)$ states; DFS holds only $O(bm)$.};
\end{scope}
\end{tikzpicture}
\end{aifig}

The code differs from BFS in two lines: \texttt{frontier.pop()} takes the newest entry, and the successors are pushed in \emph{reverse} order so that the first-listed neighbour is popped first. That second line is why DFS is \term{order dependent}: it commits to whichever neighbour the graph happens to list first and only reconsiders when that whole branch fails.

\begin{workedexample}
From \texttt{KUL}, DFS commits to the alphabetically first neighbour, \texttt{ADL} (Adelaide), then to Adelaide's first neighbour, and so on. It reaches Keflav\'ik after 23 legs and 41,768~km --- more than the circumference of the Earth. Reversing every neighbour list, so that the last-listed neighbour is tried first, gives a different route of 8 legs. Neither is a meaningful travel plan: DFS answers ``is there \emph{a} route?'', not ``what is a good one?''.
\end{workedexample}

\begin{intuition}
BFS expands a wavefront; DFS follows one thread. The wavefront is expensive to store but arrives by the fewest steps; the thread is cheap to store but says nothing about how good its path is.
\end{intuition}

\section{Uniform-Cost Search}

\term{Uniform-Cost Search (UCS)} is the algorithm to use when edges have different costs. It keeps the frontier in a \term{priority queue} ordered by $g(n)$, the cost of the cheapest known path from the start to state $n$, and always expands the cheapest frontier state. When the goal is expanded, the path to it is the cheapest of all, provided no edge cost is negative.

\figref{fig:ucs-vs-bfs} shows BFS and UCS disagreeing on a small weighted graph. Both routes use three edges, so BFS has no reason to prefer either; UCS adds the weights and finds that one costs twice the other.

\begin{aifig}[BFS and UCS on the same weighted graph]{The same weighted graph solved twice. BFS returns the first three-edge path it meets, costing 8. UCS orders its frontier by accumulated cost $g(n)$ and returns the three-edge path costing 4. Neither is wrong; they answer different questions.}{fig:ucs-vs-bfs}
\begin{tikzpicture}[font=\small]
\newcommand{\wgraph}[1]{%
  \node[ainode,minimum size=7mm] (#1A) at (0,0) {A};
  \node[ainode,minimum size=7mm] (#1B) at (1.5,0.95) {B};
  \node[ainode,minimum size=7mm] (#1C) at (1.5,-0.95) {C};
  \node[ainode,minimum size=7mm] (#1D) at (3.0,1.6) {D};
  \node[ainode,minimum size=7mm] (#1E) at (3.0,0.35) {E};
  \node[ainode,minimum size=7mm] (#1F) at (3.0,-0.95) {F};
  \node[ainode,minimum size=7mm] (#1G) at (4.5,0) {G};
}
\begin{scope}
\node[font=\bfseries\color{MLNavy}] at (2.25,2.35) {BFS: fewest edges};
\wgraph{p}
\draw[aiedge,draw=MLMuted!45] (pA)--(pC) node[midway,below,ailabel] {4};
\draw[aiedge,draw=MLMuted!45] (pB)--(pE) node[midway,above,ailabel] {2};
\draw[aiedge,draw=MLMuted!45] (pC)--(pF) node[midway,below,ailabel] {5};
\draw[aiedge,draw=MLMuted!45] (pE)--(pG) node[midway,below,ailabel] {1};
\draw[aiedge,draw=MLMuted!45] (pF)--(pG) node[midway,below,ailabel] {2};
\draw[aiedge,draw=MLRed,line width=1.2pt] (pA)--(pB) node[midway,above,ailabel,color=MLRed] {1};
\draw[aiedge,draw=MLRed,line width=1.2pt] (pB)--(pD) node[midway,above,ailabel,color=MLRed] {3};
\draw[aiedge,draw=MLRed,line width=1.2pt] (pD)--(pG) node[midway,above,ailabel,color=MLRed] {4};
\node[ailabel,anchor=north,align=center,color=MLRed] at (2.25,-1.75)
  {A $\to$ B $\to$ D $\to$ G\\3 edges, cost $1+3+4=\mathbf{8}$};
\end{scope}
\begin{scope}[xshift=6.5cm]
\node[font=\bfseries\color{MLNavy}] at (2.25,2.35) {UCS: cheapest total};
\wgraph{q}
\draw[aiedge,draw=MLMuted!45] (qA)--(qC) node[midway,below,ailabel] {4};
\draw[aiedge,draw=MLMuted!45] (qB)--(qD) node[midway,above,ailabel] {3};
\draw[aiedge,draw=MLMuted!45] (qC)--(qF) node[midway,below,ailabel] {5};
\draw[aiedge,draw=MLMuted!45] (qD)--(qG) node[midway,above,ailabel] {4};
\draw[aiedge,draw=MLMuted!45] (qF)--(qG) node[midway,below,ailabel] {2};
\draw[aiedge,draw=MLGreen,line width=1.2pt] (qA)--(qB) node[midway,above,ailabel,color=MLGreen] {1};
\draw[aiedge,draw=MLGreen,line width=1.2pt] (qB)--(qE) node[midway,above,ailabel,color=MLGreen] {2};
\draw[aiedge,draw=MLGreen,line width=1.2pt] (qE)--(qG) node[midway,below,ailabel,color=MLGreen] {1};
\node[ailabel,anchor=north,align=center,color=MLGreen] at (2.25,-1.75)
  {A $\to$ B $\to$ E $\to$ G\\3 edges, cost $1+2+1=\mathbf{4}$};
\end{scope}
\end{tikzpicture}
\end{aifig}

\begin{workedexample}
Trace UCS on the graph of \figref{fig:ucs-vs-bfs}. The frontier holds $(g,\text{state})$ pairs and always pops the smallest $g$.
\[
\begin{array}{c|l}
\text{expand} & \text{frontier after expansion} \\
\hline
A\ (0) & (1,B),\ (4,C) \\
B\ (1) & (3,E),\ (4,C),\ (4,D) \\
E\ (3) & (4,C),\ (4,D),\ (4,G) \\
C\ (4) & (4,D),\ (4,G),\ (9,F) \\
D\ (4) & (4,G),\ (9,F) \qquad\text{($G$ via $D$ would cost 8: not pushed)} \\
G\ (4) & \text{goal popped: return } A\to B\to E\to G \text{ at cost } 4
\end{array}
\]
When $D$ is expanded it offers a route to $G$ costing $4+4=8$, which is worse than the known cost of~4, so nothing is pushed. Equal-cost ties ($C$, $D$, $G$ all at 4) are broken by insertion order, which does not affect the minimum cost.
\end{workedexample}

In \texttt{utils/core.py}, UCS, GBFS and A$^\star$ share one function, \texttt{best\_first}, because they differ only in the priority they attach to a frontier entry. The frontier is a Python \texttt{heapq} of tuples \texttt{(priority, counter, state, g)}:

\begin{lstlisting}[style=aipython]
_, _, state, g = heappop(frontier)
if state in expanded_states:
    continue                       # an older, costlier entry for this state
if state == goal:
    return ...
expanded_states.add(state)
for neighbour, cost in graph.get(state, ()):
    new_g = g + cost
    if neighbour not in expanded_states and new_g < best_g.get(neighbour, inf):
        best_g[neighbour] = new_g
        parent[neighbour] = state
        counter += 1
        heappush(frontier, (priority(neighbour, new_g), counter, neighbour, new_g))
\end{lstlisting}

Three details matter. The goal test happens when a state is \emph{popped}, not when it is generated --- testing on generation would return the first path found, not the cheapest. A state can be pushed more than once when a cheaper path to it is found later; the \texttt{expanded\_states} check discards the stale copies when they surface. And the \texttt{counter} sits between the priority and the state so that ties are broken by insertion order rather than by comparing states, which would fail for mixed types. \chapref{ch:searchpatterns} returns to this last point.

\begin{keyidea}
BFS orders the frontier by discovery time, DFS by recency, UCS by accumulated cost. The graph, the successor function and the goal test never change; only the frontier policy does, and that single choice is the name of the algorithm.
\end{keyidea}

\section{Comparing the three methods}

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}
  >{\centering\arraybackslash\bfseries\leavevmode\color{MLNavy}}p{0.10\linewidth}
  >{\centering\arraybackslash}p{0.18\linewidth}
  >{\centering\arraybackslash}p{0.22\linewidth}
  X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Method & Frontier rule & Data structure & What it optimises \\
\midrule
BFS & Oldest first & FIFO queue (\texttt{deque}) & Fewest edges; every route counts as one leg \\
DFS & Newest first & Stack (list) & Nothing --- finds \emph{a} path, order dependent \\
UCS & Lowest $g(n)$ & Min-heap (\texttt{heapq}) & Cheapest total cost, e.g.\ kilometres \\
\bottomrule
\end{tabularx}
\end{mltable}

Search algorithms are judged on four standard properties. Write $b$ for the \term{branching factor} (successors per state), $d$ for the depth of the shallowest goal, and $m$ for the maximum depth of the space.

\begin{description}
  \item[Completeness] If a solution exists, is it guaranteed to be found?
  \item[Optimality] Is the solution returned guaranteed to be the cheapest?
  \item[Time] How many states may be expanded?
  \item[Space] How many states may be held in memory at once?
\end{description}

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}
  >{\centering\arraybackslash\bfseries\leavevmode\color{MLNavy}}p{0.10\linewidth}
  p{0.19\linewidth}
  p{0.25\linewidth}
  >{\centering\arraybackslash}p{0.24\linewidth}
  >{\centering\arraybackslash}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Method & Complete? & Optimal? & Time & Space \\
\midrule
BFS & Yes, if $b$ is finite & Only when every step costs the same & $O(b^{d})$ & $O(b^{d})$ \\
DFS & Not in general & No & $O(b^{m})$ & $O(bm)$ \\
UCS & Yes, if step costs $\geq\varepsilon>0$ & Yes, for non-negative costs & $O(b^{1+\lfloor C^\star/\varepsilon\rfloor})$ & same as time \\
\bottomrule
\end{tabularx}
\end{mltable}

Here $C^\star$ is the cost of the optimal solution and $\varepsilon$ the smallest step cost. \figref{fig:branching} shows what $O(b^d)$ means in practice: at branching factor 3 a goal eight levels down leaves about 9,800 states in memory; at branching factor 5 the same depth needs nearly 500,000. The airline graph has a mean branching factor near 6 but its hubs have over a hundred routes each, which is why the maximum frontier in Lab~1 reaches 1,679 airports for a two-leg BFS answer.

\begin{aifig}[Frontier growth with depth]{States a BFS frontier may hold at depth $d$, for three branching factors. The vertical axis is logarithmic, so each straight line is exponential growth. DFS stores $O(bm)$ instead, which on these scales is a flat line near the bottom.}{fig:branching}
\begin{tikzpicture}
\begin{axis}[
  width=0.72\linewidth, height=5.2cm,
  xlabel={depth $d$}, ylabel={states stored},
  xmin=1, xmax=10, ymin=1, ymax=2e7,
  ymode=log, log basis y=10,
  axis lines=left, grid=major, grid style={draw=MLPaleBlue},
  legend columns=4, legend style={at={(0.5,-0.28)},anchor=north},
  domain=1:10, samples=10]
\addplot[draw=MLGreen,very thick] {2^x};   \addlegendentry{$b=2$}
\addplot[draw=MLBlue,very thick]  {3^x};   \addlegendentry{$b=3$}
\addplot[draw=MLRed,very thick]   {5^x};   \addlegendentry{$b=5$}
\addplot[draw=MLMuted,dashed,thick] {5*x}; \addlegendentry{DFS, $O(bm)$}
\end{axis}
\end{tikzpicture}
\end{aifig}

\begin{pitfall}
Two table entries are easy to misread.

\textbf{``BFS is optimal''} holds only for \emph{equal} step costs. On the airline graph BFS returns \texttt{KUL -> AMS -> KEF} at 12,274~km while UCS returns \texttt{KUL -> HKT -> LED -> HEL -> KEF} at 11,461~km. Fewest legs and fewest kilometres are different questions, and BFS only answers the first.

\textbf{``DFS is not complete''} refers to spaces that are infinite or contain cycles without a seen-set. On a \emph{finite} graph with the \texttt{parent} dictionary --- every graph in these labs --- DFS terminates and finds a goal if one is reachable. It still gives no guarantee about which one.
\end{pitfall}

\section{What the lab measures}

Lab~1 runs the three algorithms on three queries and records six numbers for each run. Reading them correctly is the point of the exercise.

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{MLNavy}}p{0.20\linewidth}X@{}}
\toprule
\rowcolor{MLPaleBlue!92}
Field & What it tells you \\
\midrule
\texttt{hops}, \texttt{path\_cost\_km} & The two objectives. Compare BFS on the first, UCS on the second. \\
\texttt{expanded} & States whose successors were generated: the machine-independent measure of effort. \\
\texttt{max\_frontier} & Peak memory demand in states; the space column of the table above, measured. \\
\texttt{runtime\_ms} & Wall-clock time. Useful within one machine, meaningless across machines. \\
\texttt{path} & The evidence. Every leg must exist in the graph and the legs must sum to the cost. \\
\bottomrule
\end{tabularx}
\end{mltable}

The last row is a habit worth keeping: a search result is only evidence once it has been checked independently of the search that produced it. Lab~1 recomputes every path cost from the edge list and asserts that every leg is a real route; a search bug that returned an impossible path would fail loudly instead of quietly entering the report.

\begin{workedexample}
The three queries of Lab~1, as the notebook reports them:
\[
\begin{array}{l|rr|rr|rr}
 & \multicolumn{2}{c|}{\text{BFS}} & \multicolumn{2}{c|}{\text{DFS}} & \multicolumn{2}{c}{\text{UCS}} \\
\text{query} & \text{hops} & \text{km} & \text{hops} & \text{km} & \text{hops} & \text{km} \\
\hline
\text{KUL}\to\text{KEF} & 2 & 12{,}274 & 23 & 41{,}768 & 4 & 11{,}461 \\
\text{SIN}\to\text{EZE} & 2 & 21{,}976 & 39 & 65{,}214 & 3 & 17{,}817 \\
\text{NRT}\to\text{CPT} & 2 & 19{,}006 & 64 & 91{,}541 & 3 & 14{,}813
\end{array}
\]
BFS wins every hops column, UCS wins every kilometre column, and DFS wins neither. Read across a row and the trade-off is visible: UCS pays one or two extra legs to save 800--4,200~km.
\end{workedexample}

\input{labs/01-search-foundations}

\begin{quickcheck}
\begin{enumerate}
  \item Why does BFS ignore the distances stored in the graph even though they are available to it?
  \item Which two lines change when \texttt{bfs} is turned into \texttt{dfs}, and which of them makes DFS order dependent?
  \item UCS tests for the goal when a state is popped from the heap. What could go wrong if it tested when the state was first generated?
  \item Lab~1 reports \texttt{expanded} and \texttt{runtime\_ms}. Which one should you use to compare BFS and UCS with a classmate's run on a different laptop, and why?
\end{enumerate}
\end{quickcheck}
