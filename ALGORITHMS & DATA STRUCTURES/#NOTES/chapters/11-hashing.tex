\chapter{Hashing}
\label{ch:hashing}

\chapterguide%
  {Know direct array indexing, searching, and why reducing search time is valuable.}
  {explain a hash function, trace a modular hash, identify collisions, and implement the three collision-resolution techniques named in the slides: linear probing, quadratic hashing, and double hashing.}

Hashing is used for fast access to elements. The slide deck describes the core idea as converting a key into a smaller value $I$ with a \term{hash function}, then using $I$ as an index where the key is stored or searched.

\section{Hash function and table index}

A simple modular hash function has the form
\[
h(k)=k\bmod m,
\]
where $m$ is the number of table slots.

The slide's hashing example uses $H(x)=x\bmod5$ for keys 10, 12, and 14.

\begin{workedexample}
\[
10\bmod5=0,\qquad 12\bmod5=2,\qquad 14\bmod5=4.
\]
So the keys occupy indices 0, 2, and 4.
\end{workedexample}

\begin{mlfigure}
\begin{tikzpicture}[
 cell/.style={draw=ADSBlue,fill=ADSPaleBlue,minimum width=1.6cm,minimum height=0.85cm,font=\bfseries\color{ADSNavy}}
]
\foreach \i in {0,...,4}{
  \node[cell] (c\i) at (1.65*\i,0) {};
  \node[below=0.35cm of c\i,font=\scriptsize\color{ADSMuted}] {\i};
}
\node at (c0) {10}; \node at (c2) {12}; \node at (c4) {14};
\node[above=0.7cm of c2,font=\small\bfseries\color{ADSTeal}] {$H(x)=x\bmod5$};
\end{tikzpicture}
\end{mlfigure}

The slides state that a simple modular hash calculation can be $O(1)$ and therefore supports very efficient storage and access in the intended hashing model.

\begin{clarification}
The slide's $O(1)$ statement describes the intended constant-time hash calculation and typical fast table access. Actual access time depends on collision handling and table load; heavy collisions can require multiple probes.
\end{clarification}

\section{Collision}

A \term{collision} occurs when two different keys produce the same hash value and therefore target the same table slot.

The slide example asks what happens when key 15 is inserted:
\[
15\bmod5=0,
\]
which conflicts with key 10 already at index 0.

The deck names three collision-handling techniques:

\begin{itemize}
  \item linear probing;
  \item double hashing;
  \item quadratic hashing.
\end{itemize}

\section{Linear probing}

Linear probing checks the next slot, then the next, wrapping around the table if required.

A probe sequence can be written as
\[
(h(k)+i)\bmod m,\qquad i=0,1,2,\dots
\]

\begin{workedexample}
With $m=5$, key 15 hashes to 0. If index 0 is occupied, linear probing tries 1, then 2, then 3, then 4 until an empty slot is found.
\end{workedexample}

\begin{lstlisting}
int insertLinear(vector<int>& table, int key, int EMPTY) {
    int m = static_cast<int>(table.size());
    int start = key % m;

    for (int i = 0; i < m; ++i) {
        int idx = (start + i) % m;
        if (table[idx] == EMPTY) {
            table[idx] = key;
            return idx;
        }
    }
    return -1;  // no free slot
}
\end{lstlisting}

\section{Quadratic hashing / quadratic probing}

Quadratic probing changes the step size quadratically, for example
\[
(h(k)+i^2)\bmod m.
\]

\begin{lstlisting}
int insertQuadratic(vector<int>& table, int key, int EMPTY) {
    int m = static_cast<int>(table.size());
    int h = key % m;

    for (int i = 0; i < m; ++i) {
        int idx = (h + i * i) % m;
        if (table[idx] == EMPTY) {
            table[idx] = key;
            return idx;
        }
    }
    return -1;
}
\end{lstlisting}

\begin{clarification}
The slide calls the technique ``quadratic hashing.'' In common data-structures terminology, the collision-resolution method is usually called \emph{quadratic probing}. The notes keep both labels visible.
\end{clarification}

\section{Double hashing}

Double hashing uses one hash function for the initial position and another to choose the probe step.

One illustrative form is
\[
\operatorname{index}_i=(h_1(k)+i\,h_2(k))\bmod m.
\]

\begin{lstlisting}
int insertDoubleHash(vector<int>& table, int key, int EMPTY) {
    int m = static_cast<int>(table.size());
    int h1 = key % m;
    int h2 = 1 + (key % (m - 1));

    for (int i = 0; i < m; ++i) {
        int idx = (h1 + i * h2) % m;
        if (table[idx] == EMPTY) {
            table[idx] = key;
            return idx;
        }
    }
    return -1;
}
\end{lstlisting}

\begin{clarification}
The hashing deck names double hashing but does not specify a second hash function. The formula above is an implementation example for the lab, not a formula taken from the slide.
\end{clarification}

\section{Searching under open addressing}

The probe rule used for insertion must also be used for searching. Otherwise the program may stop before reaching the slot where a collided key was placed.

\begin{keyidea}
A hash table does not eliminate the need for searching logic; it changes the search space from ``scan the whole collection'' to ``calculate an index, then follow the collision-resolution probe sequence if necessary.''
\end{keyidea}

\section{Deletion caution}

With open addressing, simply marking a deleted slot as never-used can incorrectly terminate a later search whose probe sequence must pass through that slot.

\begin{clarification}
Deletion behavior and tombstone markers are not covered in the supplied hashing slides. This caution is included only to explain why a complete open-addressing implementation needs more than insertion logic.
\end{clarification}

\begin{quickcheck}
Using $H(x)=x\bmod5$, where do keys 10, 12, 14, and 15 initially hash? Which pair collides? How does linear probing react?
\end{quickcheck}

\begin{chapterrecap}
\begin{itemize}
  \item A hash function converts a key into a table index.
  \item The slide example uses modular hashing and highlights $O(1)$ hash calculation / fast-access intent.
  \item A collision occurs when multiple keys map to the same slot.
  \item Linear probing checks consecutive slots.
  \item Quadratic probing changes the probe distance quadratically.
  \item Double hashing uses a second hash function to determine the probe step.
\end{itemize}
\end{chapterrecap}

\input{labs/11-hashing}
