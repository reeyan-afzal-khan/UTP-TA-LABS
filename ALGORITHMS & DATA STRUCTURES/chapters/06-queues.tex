\chapter{Queues}\label{ch:queues}

\chapterguide%
  {Know the constrained-access idea from stacks and the array/linked-list implementation alternatives.}
  {explain FIFO, trace front/rear movement, implement enqueue/dequeue, explain why circular queues reuse array space, and distinguish linear, circular, and priority queues at the level covered by the slides.}

A \term{queue} is a linear data structure that follows \term{FIFO}: First In, First Out. Insertion occurs at the \term{rear}; deletion occurs at the \term{front}.

\section{Front and rear}

\begin{adsfig}[The two ends of a queue]{A queue has two active ends. Items join at the rear and leave from the
front, so the order of departure matches the order of arrival --- the opposite discipline
to a stack.}{fig:queue-fifo}
\begin{tikzpicture}
\foreach \v [count=\i from 0] in {A,B,C,D}{
  \node[adscell,minimum width=1.5cm,minimum height=0.82cm] (c\i) at (1.5*\i,0) {\v};
}
\node[adstag,anchor=north] at (0,-0.6) {FRONT};
\node[adstag,anchor=north] at (4.5,-0.6) {REAR};
\draw[adsarrow] (c0.west) -- ++(-1.5,0)
  node[midway,above,adslabel] {dequeue}
  node[midway,below,adslabel] {oldest leaves};
\draw[adsarrow] (6.0,0) -- (c3.east)
  node[midway,above,adslabel] {enqueue}
  node[midway,below,adslabel] {newest joins};
\end{tikzpicture}
\end{adsfig}

The Week VI slides use the terms \term{Create Q}, \term{Enqueue(Q,item)}, and \term{Dequeue(Q)}. The enqueue operation is stated as $O(1)$ in the slide implementation model.

\section{Core queue operations}

\begin{description}[leftmargin=1.3in,style=nextline]
  \item[Create Q] Create an empty queue.
  \item[enqueue()] Add an item at the rear.
  \item[dequeue()] Remove and return the oldest item at the front.
  \item[peek/front] Read the front item without removing it.
  \item[isEmpty()] Check whether the queue contains no items.
  \item[isFull()] Check capacity in a fixed array implementation.
\end{description}

\section{A simple linear array queue}

A basic fixed array can use indexes for front and rear.

\begin{lstlisting}
class LinearQueue {
private:
    static const int CAP = 5;
    int data[CAP];
    int front = 0;
    int rear = -1;

public:
    bool isEmpty() const { return rear < front; }
    bool isFull() const { return rear == CAP - 1; }

    bool enqueue(int value) {
        if (isFull()) return false;
        data[++rear] = value;
        return true;
    }

    bool dequeue(int& value) {
        if (isEmpty()) return false;
        value = data[front++];
        return true;
    }
};
\end{lstlisting}

This model exposes the limitation described by the slides: after repeated dequeues, unused positions may exist at the beginning even though \texttt{rear} has reached the end of the array.

\figref{fig:queue-falsefull} shows the failure. Both \texttt{front} and \texttt{rear} only
ever move right, so the queue drifts up the array and eventually reports itself full while
sitting on empty slots. Shifting everything back down would fix it, but that costs
\cxr{O(n)} on every dequeue and throws away the whole point of the structure.

\begin{adsfig}[The ``false full'' problem]{The ``false full'' problem in a linear array queue. After three enqueues and
two dequeues, \texttt{rear} has reached the last index and further enqueues are rejected ---
even though indices 0 and 1 are free. The array is not full; the queue has merely walked off
the end of it.}{fig:queue-falsefull}
\begin{tikzpicture}[font=\small]
% state after 5 enqueues, 2 dequeues (capacity 5)
\foreach \v [count=\i from 0] in {{},{},C,D,E}{
  \ifx\v\empty
    \node[adsdim,minimum width=1.15cm,minimum height=0.85cm] (z\i) at (1.2*\i,0) {};
  \else
    \node[adscell,minimum width=1.15cm,minimum height=0.85cm] (z\i) at (1.2*\i,0) {\v};
  \fi
  \node[adslabel] at (1.2*\i,-0.68) {\i};
}
\node[adstag,anchor=south] at (2.4,0.55) {front};
\draw[adsptr,draw=ADSOrange] (2.4,0.52) -- (z2.north);
\node[adstag,anchor=south] at (4.8,0.55) {rear};
\draw[adsptr,draw=ADSOrange] (4.8,0.52) -- (z4.north);
\draw[decorate,decoration={brace,mirror,amplitude=4pt,raise=3pt},draw=ADSGreen,semithick]
  (-0.62,-0.9) -- (0.62,-0.9);
\node[adslabel,anchor=north,color=ADSGreen,align=center] at (0,-1.3) {free, but\\unreachable};
\node[adslabel,anchor=west,color=ADSRed,align=left] at (5.7,0)
  {\texttt{enqueue(F)} fails:\\\texttt{rear} is at the last index};
\end{tikzpicture}
\end{adsfig}

\section{Circular queue: reusing array space}

The circular queue addresses that limitation by connecting the logical last position back to the first. The slides call this a \term{ring buffer} and state that modular arithmetic is used for enqueue and dequeue.

\begin{adsfig}[Circular queue wraparound]{The same five slots viewed as a ring. Advancing with $(\text{index}+1)\bmod N$
sends \texttt{rear} from index 4 back to index 0, so the free space at the front of the array
is reclaimed instead of stranded.}{fig:queue-circular}
\begin{tikzpicture}[font=\small]
% ring
\foreach \idx/\ang in {0/90, 1/18, 2/-54, 3/-126, 4/162}{
  \ifnum\idx<2
    \node[adsdim,circle,minimum size=9.5mm] (k\idx) at (\ang:1.7) {\idx};
  \else
    \node[adsnode,minimum size=9.5mm] (k\idx) at (\ang:1.7) {\idx};
  \fi
}
\draw[adsarrow] (k0) to[bend left=20] (k1);
\draw[adsarrow] (k1) to[bend left=20] (k2);
\draw[adsarrow] (k2) to[bend left=20] (k3);
\draw[adsarrow] (k3) to[bend left=20] (k4);
\draw[-{Stealth[length=2.4mm]},line width=1pt,draw=ADSOrange] (k4) to[bend left=20] (k0);
\node[adslabel,align=center,color=ADSOrange] at (0,0) {$(4+1)\bmod5$\\$=0$};
\node[adstag,anchor=east] at (162:2.5) {rear};
\node[adstag,anchor=north west] at (-54:2.35) {front};

% flat equivalent
\begin{scope}[xshift=6.4cm,yshift=0.15cm]
\foreach \v [count=\i from 0] in {F,{},C,D,E}{
  \ifx\v\empty
    \node[adsdim,minimum width=1.0cm,minimum height=0.8cm] (w\i) at (1.05*\i,0) {};
  \else
    \ifnum\i=0
      \node[adshot,minimum width=1.0cm,minimum height=0.8cm] (w\i) at (1.05*\i,0) {\v};
    \else
      \node[adscell,minimum width=1.0cm,minimum height=0.8cm] (w\i) at (1.05*\i,0) {\v};
    \fi
  \fi
  \node[adslabel] at (1.05*\i,-0.62) {\i};
}
\draw[-{Stealth[length=2.2mm]},line width=1pt,draw=ADSOrange]
  (w4.east) -- (4.98,0) -- (4.98,-1.3) -- (-0.98,-1.3) -- (-0.98,0) -- (w0.west);
\node[adslabel,anchor=north,align=center] at (2.1,-1.42)
  {\texttt{enqueue(F)} now succeeds: \texttt{rear} wraps to index 0};
\end{scope}
\end{tikzpicture}
\end{adsfig}

For capacity $N$, moving to the next slot uses
\[
\text{next} = (\text{index}+1) \bmod N.
\]

\begin{workedexample}
With capacity $5$, if the rear is at index $4$, then the next rear position is
\[
(4+1) \bmod 5 = 0.
\]
The index wraps back to the beginning instead of moving outside the array.
\end{workedexample}

\section{Circular queue implementation pattern}

One robust representation stores the number of active elements in addition to front and rear.

\begin{lstlisting}
class CircularQueue {
private:
    static const int CAP = 5;
    int data[CAP];
    int front = 0;
    int rear = 0;
    int count = 0;

public:
    bool isEmpty() const { return count == 0; }
    bool isFull() const { return count == CAP; }

    bool enqueue(int value) {
        if (isFull()) return false;
        data[rear] = value;
        rear = (rear + 1) % CAP;
        ++count;
        return true;
    }

    bool dequeue(int& value) {
        if (isEmpty()) return false;
        value = data[front];
        front = (front + 1) % CAP;
        --count;
        return true;
    }
};
\end{lstlisting}

\section{Linked-list queue}

A linked queue normally keeps both a front pointer and a rear pointer so enqueue can add at the rear without traversing the list.

\begin{lstlisting}
struct QNode {
    int data;
    QNode* next;
};

class LinkedQueue {
private:
    QNode* front = nullptr;
    QNode* rear = nullptr;

public:
    bool isEmpty() const { return front == nullptr; }

    void enqueue(int value) {
        QNode* n = new QNode{value, nullptr};
        if (rear == nullptr) {
            front = rear = n;
        } else {
            rear->next = n;
            rear = n;
        }
    }

    bool dequeue(int& value) {
        if (isEmpty()) return false;
        QNode* old = front;
        value = old->data;
        front = front->next;
        if (front == nullptr) rear = nullptr;
        delete old;
        return true;
    }
};
\end{lstlisting}

\section{Linear, circular, and priority queues}

The lecture deck asks students to differentiate these queue types, and the official lab manual adds the processing rules for a \term{priority queue}.

\begin{mltable}
\begin{tabularx}{\linewidth}{@{}>{\bfseries\leavevmode\color{ADSNavy}}p{0.19\linewidth}X X@{}}
\toprule
\rowcolor{ADSPaleBlue!92}
Queue type & Ordering rule & Course role \\
\midrule
Linear queue & FIFO & Front removes oldest; rear inserts newest. \\
Circular queue & FIFO with wrap-around array positions & Reuses vacant positions with modulo arithmetic. \\
Priority queue & Higher priority is processed before lower priority; equal-priority items follow arrival order & Models work where urgency matters more than pure arrival time. \\
\bottomrule
\end{tabularx}
\end{mltable}

The lab manual states two priority rules:
\begin{enumerate}
  \item an element of higher priority is processed before an element of lower priority;
  \item when two elements have the same priority, they are processed in the order in which they were added.
\end{enumerate}
It also states that priority queues can be implemented using arrays or linked lists.

\begin{sourcealignment}{Queue lab manual}
The manual supplies the priority-ordering rules and the array/linked-list implementation options, but it does not specify a particular insertion strategy, heap representation, or priority convention such as whether a numerically larger value means higher priority. Any implementation should document that design choice.
\end{sourcealignment}

\begin{quickcheck}
Why can a simple linear array queue appear full even when unused slots exist near index 0? How does modulo arithmetic fix that issue in a circular queue?
\end{quickcheck}

\begin{chapterrecap}
\begin{itemize}
  \item A queue follows FIFO.
  \item Enqueue inserts at the rear; dequeue removes at the front.
  \item Queues can be implemented with arrays or linked lists.
  \item A circular queue connects the logical last position to the first and uses modulo arithmetic to reuse space.
  \item Priority queues use priority rather than pure arrival order.
\end{itemize}
\end{chapterrecap}

\input{labs/06-queues}
