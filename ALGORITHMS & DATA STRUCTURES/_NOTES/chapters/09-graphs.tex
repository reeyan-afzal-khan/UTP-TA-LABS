\chapter{Graphs}\label{ch:graphs}

\chapterguide%
  {Understand non-linear structures, linked representations, and queue/stack behavior.}
  {define $G(V,E)$, distinguish directed and undirected graphs, represent a graph with an adjacency matrix or adjacency list, trace BFS and DFS, and explain the spanning-tree constraints stated in the slides.}

A \term{graph} generalizes the idea of connected data. The graph $G(V,E)$ contains a set of vertices $V$ and a set of edges $E$ that connect vertices.

\section{Vertices and edges}

The graph example in the slides uses
\[
V=\{A,B,C,D,E\}
\]
and
\[
E=\{(A,B),(B,E),(A,D),(A,C),(C,D),(D,E),(B,D)\}.
\]

\begin{mlfigure}
\begin{tikzpicture}[
 node/.style={circle,draw=ADSBlue,fill=ADSPaleBlue,minimum size=8mm,font=\small\bfseries},
 edge/.style={thick,draw=ADSTeal}
]
\node[node] (A) at (0,1.5) {A};
\node[node] (B) at (2.4,1.5) {B};
\node[node] (C) at (0,0) {C};
\node[node] (D) at (2.4,0) {D};
\node[node] (E) at (4.2,0.75) {E};
\foreach \u/\v in {A/B,B/E,A/D,A/C,C/D,D/E,B/D}{\draw[edge] (\u)--(\v);}
\end{tikzpicture}
\end{mlfigure}

The slides contrast trees and graphs by noting that a tree is acyclic while a graph may contain cycles. They also motivate graphs with a city-distance example: cities become vertices and distances become edge information.

\section{Directed and undirected graphs}

\begin{description}[leftmargin=1.4in,style=nextline]
  \item[Directed graph] Edges have direction. $(a,b)$ and $(b,a)$ can represent different relationships.
  \item[Undirected graph] Edges have no direction. $(a,b)$ and $(b,a)$ are equivalent descriptions of the same connection.
\end{description}

\begin{mlfigure}
\begin{tikzpicture}[
 node/.style={circle,draw=ADSBlue,fill=ADSPaleBlue,minimum size=8mm,font=\small\bfseries},
 darr/.style={-{Stealth[length=2mm]},thick,draw=ADSTeal},
 uarr/.style={thick,draw=ADSOrange}
]
\node[node] (a) {A}; \node[node,right=1.4cm of a] (b) {B};
\draw[darr] (a)--(b);
\node[above=0.3cm of a,xshift=0.7cm,font=\scriptsize\color{ADSMuted}] {directed};
\node[node,right=4.2cm of a] (c) {A}; \node[node,right=1.4cm of c] (d) {B};
\draw[uarr] (c)--(d);
\node[above=0.3cm of c,xshift=0.7cm,font=\scriptsize\color{ADSMuted}] {undirected};
\end{tikzpicture}
\end{mlfigure}

\section{Weighted and unweighted graphs}

The official lab manual extends the graph classification beyond direction.

\begin{description}[leftmargin=1.45in,style=nextline]
  \item[Weighted graph] Each edge carries an associated value such as distance or cost. The manual's examples include distances between cities and communication cost between workstations.
  \item[Unweighted graph] Edges indicate that two vertices are related, but no numerical edge weight is attached.
\end{description}

\begin{mlfigure}
\begin{tikzpicture}[
 node/.style={circle,draw=ADSBlue,fill=ADSPaleBlue,minimum size=8mm,font=\small\bfseries},
 edge/.style={thick,draw=ADSTeal}
]
\node[node] (A) at (0,0) {A};
\node[node] (B) at (2.0,0.8) {B};
\node[node] (C) at (2.0,-0.8) {C};
\draw[edge] (A)-- node[above,sloped,font=\scriptsize\color{ADSOrange}] {7} (B);
\draw[edge] (A)-- node[below,sloped,font=\scriptsize\color{ADSOrange}] {3} (C);
\draw[edge] (B)-- node[right,font=\scriptsize\color{ADSOrange}] {4} (C);
\node[right=1.0cm of B,font=\small\color{ADSMuted},align=left] {The same topology can be\\weighted by distance, cost, time, etc.};
\end{tikzpicture}
\end{mlfigure}

\begin{keyidea}
Direction and weight are separate properties. A graph can be directed and weighted, directed and unweighted, undirected and weighted, or undirected and unweighted.
\end{keyidea}

\section{Graph representations}

The graph deck teaches two storage strategies:

\begin{itemize}
  \item sequential or \term{adjacency matrix} representation;
  \item linked or \term{adjacency list} representation.
\end{itemize}

\subsection{Adjacency matrix}

An adjacency matrix uses a matrix entry $A_{ij}$ to represent the connection or associated edge weight from node $i$ to node $j$.

For the undirected example above, a simple unweighted 0/1 matrix is

\begin{mltable}
\begin{tabular}{@{}cccccc@{}}
\toprule
 & A & B & C & D & E \\
\midrule
A & 0 & 1 & 1 & 1 & 0 \\
B & 1 & 0 & 0 & 1 & 1 \\
C & 1 & 0 & 0 & 1 & 0 \\
D & 1 & 1 & 1 & 0 & 1 \\
E & 0 & 1 & 0 & 1 & 0 \\
\bottomrule
\end{tabular}
\end{mltable}

\begin{clarification}
The slide image appears to show a 1 on the $D,D$ diagonal entry, while the edge set printed on the preceding slide does not list a self-loop $(D,D)$. The matrix above follows the printed edge set. This discrepancy is made explicit rather than silently copied.
\end{clarification}

\subsection{Adjacency list}

An adjacency list stores, for each vertex, the vertices connected to it. The slides emphasize that it is storage-efficient because values are stored for edges rather than allocating a full matrix.

\begin{lstlisting}
vector<vector<int>> adj(5);

// A=0, B=1, C=2, D=3, E=4
adj[0] = {1, 2, 3};
adj[1] = {0, 3, 4};
adj[2] = {0, 3};
adj[3] = {0, 1, 2, 4};
adj[4] = {1, 3};
\end{lstlisting}

\section{Breadth-first search (BFS)}

The slide deck states that BFS traverses breadth-wise and uses a queue to move to the next node.

\begin{lstlisting}
void bfs(const vector<vector<int>>& adj, int start) {
    vector<bool> visited(adj.size(), false);
    queue<int> q;

    visited[start] = true;
    q.push(start);

    while (!q.empty()) {
        int u = q.front();
        q.pop();
        cout << u << ' ';

        for (int v : adj[u]) {
            if (!visited[v]) {
                visited[v] = true;
                q.push(v);
            }
        }
    }
}
\end{lstlisting}

For the slide example, one shown BFS order is $A,B,C,D,E$.

\section{Depth-first search (DFS)}

The slides state that DFS traverses depth-wise and uses a stack to reach the next nodes. Recursion is another way to obtain stack behavior.

\begin{lstlisting}
void dfs(const vector<vector<int>>& adj,
         int u, vector<bool>& visited) {
    visited[u] = true;
    cout << u << ' ';

    for (int v : adj[u]) {
        if (!visited[v]) dfs(adj, v, visited);
    }
}
\end{lstlisting}

One DFS order shown by the deck is $A,B,E,D,C$.

\begin{clarification}
BFS and DFS traversal orders are not unique unless the neighbor-visiting order is fixed. The sequences in the slides are valid for a particular ordering of adjacent vertices; different adjacency-list orderings can produce different, equally valid traversals.
\end{clarification}

\section{Why a visited set matters}

Graphs may contain cycles. Without a visited marker, a traversal could keep moving around a cycle indefinitely.

\begin{keyidea}
The queue or stack determines \emph{which discovered vertex to process next}. The visited structure determines \emph{whether a vertex should be processed again}.
\end{keyidea}

\section{Spanning trees}

The slides define a spanning tree as a subset of a graph that covers all vertices using the minimum possible number of edges, has no cycles, and is connected.

For a connected graph with $|V|$ vertices, a spanning tree has $|V|-1$ edges.

The lab then asks students to find a \term{minimum spanning tree} of an undirected graph using a greedy approach.

\begin{clarification}
The deck requests a greedy minimum-spanning-tree implementation but does not name a specific algorithm. Common textbook choices are Prim's algorithm and Kruskal's algorithm; naming them here is additional context, not material explicitly taught in the supplied slides.
\end{clarification}

\begin{quickcheck}
Why is an adjacency matrix often more wasteful for a sparse graph than an adjacency list? Which earlier ADT is directly reused by BFS, and which is reused by DFS?
\end{quickcheck}

\begin{chapterrecap}
\begin{itemize}
  \item A graph is represented as $G(V,E)$ with vertices and edges.
  \item Directed edges have orientation; undirected edges do not.
  \item Graphs can be stored with adjacency matrices or adjacency lists.
  \item BFS uses queue behavior; DFS uses stack behavior.
  \item A spanning tree is connected, acyclic, covers every vertex, and uses $|V|-1$ edges.
\end{itemize}
\end{chapterrecap}

\input{labs/09-graphs}
