\section{Kaggle implementation: KNN versus Gaussian naive Bayes}
\label{sec:kaggle-breast-knn-nb}

\begin{kaggleproject}{Breast Cancer Wisconsin --- local geometry versus probabilistic independence}
\kagglesource{https://www.kaggle.com/datasets/uciml/breast-cancer-wisconsin-data}{Breast Cancer Wisconsin (Diagnostic) Data Set}
\textbf{Question.} How do two very different inductive biases behave on the same standardized diagnostic feature table?
\end{kaggleproject}

KNN bases a prediction on nearby observations, so scaling is essential. Gaussian naive Bayes instead models each feature distribution conditionally and combines those terms under its independence assumption.

\textbf{Before running.} Predict the qualitative pattern you expect from the experiment before you look at the output or plot.

\begin{labmode}
Stage 3B: assemble both pipelines and the training-only cross-validated $k$ comparison yourself before checking the reference implementation.
\end{labmode}

\textbf{Part A --- create the development split.}

\lstinputlisting[style=mlpython,firstline=1,lastline=16]{all_experiments/lab10-breast_cancer_knn_nb.py}

\textbf{Part B --- select \(k\) with cross-validation inside the training partition.}

\lstinputlisting[style=mlpython,firstline=18,lastline=34]{all_experiments/lab10-breast_cancer_knn_nb.py}

\textbf{Part C --- compare the locked KNN with Gaussian naive Bayes on validation data.}

\lstinputlisting[style=mlpython,firstline=36,lastline=56]{all_experiments/lab10-breast_cancer_knn_nb.py}


\begin{tryit}
For KNN, sweep \texttt{n\_neighbors} over $1,3,5,7,11,21$. Use cross-validation inside the training partition to select $k$, then use the development validation set to compare the locked KNN with Gaussian naive Bayes. Do not tune $k$ from the validation result itself.
\end{tryit}

\begin{labdeliverable}
Submit one development-validation comparison table for KNN and Gaussian naive Bayes, a cross-validated plot or table of KNN performance over $k\in\{1,3,5,7,11,21\}$ using training data only, the selected $k$, and a short explanation of why scaling matters to KNN but not for the same reason to Gaussian naive Bayes.
\end{labdeliverable}
