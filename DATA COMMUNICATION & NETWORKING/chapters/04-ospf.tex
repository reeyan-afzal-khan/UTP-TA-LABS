\chapter{Dynamic Routing with OSPF}\label{ch:ospf}
\chapterguide{Static routing, interface addressing, routing-table interpretation, and ping verification.}{Configure router IDs, advertise networks into OSPF area 0, inspect OSPF-learned routes and neighbors, and explain why dynamic routing reduces manual route maintenance.}

\section{Where OSPF fits among routing approaches}
OSPF is a dynamic, interior, link-state routing protocol. Instead of configuring every destination and next hop, routers form neighbor relationships and exchange topology information.

\sourceimage[0.88\linewidth]{Routing-protocol classification shown in the supplied Lab 4 slide}{routing-protocols-map.png}

\section{Why dynamic routing is useful}
Dynamic routing reduces manual maintenance and adapts to topology changes. OSPF uses a hierarchy centered on \term{area 0}; this exercise places every participating interface in that single area.

\subsection{What changes compared with Chapter~\ref{ch:static-routing}}
In the static lab you wrote a route on R1 and a mirror route on R3, then a backup pair on top, and a ping only worked once every one of them was present. Here you tell each router which of \emph{its own} interfaces take part, and reachability follows.

\begin{netfigure}{Every router holds the same map, then computes its own routes}
\begin{tikzpicture}[node distance=1.0cm and 1.5cm,
  db/.style={rectangle,rounded corners=1mm,draw=MLTeal,fill=MLPaleTeal,
             minimum width=2.5cm,minimum height=0.95cm,align=center,
             font=\scriptsize\color{MLNavy}},
  ml/.style={font=\scriptsize\color{MLMuted},align=center}]

\node[router] (r1) {R1};
\node[router,right=2.2cm of r1] (r2) {R2};
\node[router,right=2.2cm of r2] (r3) {R3};
\draw[link] (r1) -- (r2);
\draw[link] (r2) -- (r3);
\draw[link] (r1) to[bend right=32] (r3);

\node[db,below=1.15cm of r1] (d1) {identical\\ link-state DB};
\node[db,below=1.15cm of r2] (d2) {identical\\ link-state DB};
\node[db,below=1.15cm of r3] (d3) {identical\\ link-state DB};
\draw[<->,thick,draw=MLTeal] (d1) -- node[ml,above]{flood} (d2);
\draw[<->,thick,draw=MLTeal] (d2) -- node[ml,above]{flood} (d3);

\node[ml,below=0.45cm of d2,text width=9.5cm]
  {Same database everywhere. Each router then runs Dijkstra \textbf{from itself},
   so the routing tables all differ --- and all agree.};
\end{tikzpicture}
\end{netfigure}

\begin{keyidea}
A static route states a \emph{conclusion} (``send this network that way''). OSPF distributes the \emph{evidence} --- which links exist and what they cost --- and lets each router reach its own conclusion. That is why a failed link needs no new configuration: the evidence changes, and every router recomputes.
\end{keyidea}

\section{Single-area OSPF topology}
R1 connects to R2 over \cmd{10.0.12.0/24} and R3 over \cmd{10.0.13.0/24}. Loopbacks provide stable networks at \cmd{10.0.1.1/24}, \cmd{10.0.2.2/24}, and \cmd{10.0.3.3/24}; the source also shows \cmd{172.16.0.1/24} on R3's Loopback2.

\sourceimage[0.79\linewidth]{OSPF single-area topology from the supplied Lab 4 slide}{ospf-topology.png}

\section{Router IDs and OSPF process 1}
Each OSPF process receives a unique router ID matching the router's loopback address.

\begin{lstlisting}[style=dcnterminal]
[R1] ospf 1 router-id 10.0.1.1
[R1-ospf-1] area 0
[R1-ospf-1-area-0.0.0.0] network 10.0.1.0 0.0.0.255
[R1-ospf-1-area-0.0.0.0] network 10.0.13.0 0.0.0.255
[R1-ospf-1-area-0.0.0.0] network 10.0.12.0 0.0.0.255

[R2] ospf 1 router-id 10.0.2.2
[R2-ospf-1] area 0
[R2-ospf-1-area-0.0.0.0] network 10.0.2.0 0.0.0.255
[R2-ospf-1-area-0.0.0.0] network 10.0.12.0 0.0.0.255
\end{lstlisting}
R3 follows the same pattern for the networks connected to it.

\begin{keyidea}
The \cmd{network} statements select local interfaces for OSPF. An unmatched interface does not participate or advertise its connected network through that process.
\end{keyidea}

\section{Understand the wildcard mask used in the slides}
The wildcard \cmd{0.0.0.255} matches addresses in the corresponding \cmd{x.x.x.0/24} network.

\section{Verify convergence}
After configuration, do not jump directly to ping. First verify the control plane.
\begin{lstlisting}[style=dcnterminal]
<R1> display ip routing-table
<R1> display ospf peer
<R1> display ospf peer brief
\end{lstlisting}
The detailed output includes neighbor ID, address, state, priority, DR/BDR data, timers, and uptime; the brief form shows only the essentials.

\sourceimage[0.74\linewidth]{Condensed OSPF peer information from the supplied Lab 4 material}{ospf-peer-brief.png}

\begin{verification}
Require three checks: the expected neighbor is \textbf{Full}, remote networks appear as OSPF routes, and ping succeeds.
\end{verification}

\section{Connectivity test}
The lab pings from R2 to R1 and R3's loopback \cmd{10.0.3.3}, confirming that OSPF learned the remote paths.

\section{Troubleshooting OSPF in the order the evidence suggests}
\begin{netsteps}
\item Confirm interface IP addresses and physical/protocol state.
\item Confirm each required network is matched by the OSPF configuration in area 0.
\item Use \cmd{display ospf peer brief}. If the expected neighbor is absent or not Full, solve the adjacency problem before studying remote routes.
\item Use \cmd{display ip routing-table} and locate the OSPF-learned destination.
\item Only then use ping to verify the data path.
\end{netsteps}

\begin{pitfall}
A missing route may reflect a failed adjacency. Do not mask the fault with a static route; inspect the interface and OSPF configuration.
\end{pitfall}

\begin{quickcheck}
\begin{enumerate}
\item Why does the lab explicitly configure a router ID?
\item What is the role of area 0 in the source material?
\item Which command gives the compact neighbor summary?
\item What two pieces of evidence should be present before you trust a successful OSPF configuration?
\end{enumerate}
\end{quickcheck}

\begin{chapterrecap}
OSPF exchanges topology information through neighbor relationships. Configure unique router IDs and area networks, verify Full peers and learned routes, then test connectivity.
\end{chapterrecap}

\input{labs/04-ospf}
