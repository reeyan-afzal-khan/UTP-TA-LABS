\chapter{VLAN Configuration: Access, Trunk, and Hybrid Ports}\label{ch:vlan}
\chapterguide{Ethernet switch configuration and Eth-Trunk/LACP concepts from Lab 7.}{Create VLANs, bind access ports, turn the inter-switch Eth-Trunk into a VLAN trunk, configure hybrid ports and PVID behavior, and verify same-VLAN and controlled cross-VLAN communication.}

\section{Purpose of VLANs}
VLANs create separate Layer-2 broadcast domains on shared switches. Hosts in different VLANs remain isolated unless the design deliberately connects them.

\sourceimage[0.88\linewidth]{VLAN topology from the supplied Lab 8 slide}{vlan-topology.png}

\section{Three port behaviors used in the exercise}
\begin{tabularx}{\linewidth}{>{\bfseries}p{0.23\linewidth}X}
\toprule
Port type & Role in the supplied lab\\\midrule
Access & Connects an untagged host to one VLAN.\\
Trunk & Carries multiple VLANs between switches.\\
Hybrid & Controls tagging per VLAN and classifies untagged ingress by PVID.\\\bottomrule
\end{tabularx}

\begin{netfigure}{Where the VLAN tag is added, carried, and removed}
\begin{tikzpicture}[node distance=0.9cm and 1.5cm]
\node[host] (pc1) {PC1};
\node[switch,right=1.5cm of pc1] (sw1) {SW1};
\node[switch,right=2.9cm of sw1] (sw2) {SW2};
\node[host,right=1.5cm of sw2] (pc3) {PC3};

\draw[flowarrow] (pc1) -- node[netlabel,above]{access} (sw1);
\draw[flowarrow] (sw1) -- node[netlabel,above]{trunk} (sw2);
\draw[flowarrow] (sw2) -- node[netlabel,above]{access} (pc3);

\node[frame,below=0.8cm of pc1,text width=1.9cm,align=center]
  {\textbf{no tag}\\ \tiny the host has no idea VLANs exist};
\node[frame,below=0.8cm of $(sw1)!0.5!(sw2)$,text width=2.5cm,align=center,
      draw=MLBlue,fill=MLPaleBlue]
  {\textbf{tag = VLAN 10}\\ \tiny added on entry, so the far switch knows};
\node[frame,below=0.8cm of pc3,text width=1.9cm,align=center]
  {\textbf{no tag}\\ \tiny stripped again on the way out};

\draw[->,draw=MLMuted,dotted] ($(pc1)+(0,-0.35)$) -- ($(pc1)+(0,-0.75)$);
\draw[->,draw=MLMuted,dotted] ($(sw1)!0.5!(sw2)+(0,-0.2)$) -- ($(sw1)!0.5!(sw2)+(0,-0.68)$);
\draw[->,draw=MLMuted,dotted] ($(pc3)+(0,-0.35)$) -- ($(pc3)+(0,-0.75)$);
\end{tikzpicture}

\smallskip
{\scriptsize The tag exists only \emph{between} the switches. Hosts never see it,
which is why a VLAN needs no configuration on the PC --- and why the switch,
not the host, decides which VLAN a frame belongs to.}
\end{netfigure}

\begin{keyidea}
A VLAN is enforced by the switch, not by the host's IP address. Two PCs can share a subnet and still be unable to reach each other if they sit in different VLANs --- changing the PC's address cannot bypass the separation.
\end{keyidea}

\section{Step 1: build the inter-switch Eth-Trunk}
Build static-LACP Eth-Trunk 1 with G0/0/9 and G0/0/10. The source shows both physical-interface membership and \cmd{trunkport} from the Eth-Trunk view.
\begin{lstlisting}[style=dcnterminal]
[S1] interface Eth-Trunk 1
[S1-Eth-Trunk1] mode lacp
[S1] interface GigabitEthernet 0/0/9
[S1-GigabitEthernet0/0/9] eth-trunk 1
[S1] interface GigabitEthernet 0/0/10
[S1-GigabitEthernet0/0/10] eth-trunk 1

[S2] interface Eth-Trunk 1
[S2-Eth-Trunk1] mode lacp
[S2-Eth-Trunk1] trunkport GigabitEthernet 0/0/9
[S2-Eth-Trunk1] trunkport GigabitEthernet 0/0/10
\end{lstlisting}

\section{Step 2: make Eth-Trunk 1 a VLAN trunk}
\begin{lstlisting}[style=dcnterminal]
[S1] interface Eth-Trunk 1
[S1-Eth-Trunk1] port link-type trunk
[S1-Eth-Trunk1] port trunk allow-pass vlan all

[S2] interface Eth-Trunk 1
[S2-Eth-Trunk1] port link-type trunk
[S2-Eth-Trunk1] port trunk allow-pass vlan all
\end{lstlisting}

\begin{keyidea}
Eth-Trunk aggregates links; trunk mode carries VLANs. These are separate functions on the same logical interface.
\end{keyidea}

\section{Step 3: create VLANs and bind access interfaces}
The lab demonstrates two Huawei VRP configuration styles.

On S1, G0/0/13 is associated with VLAN 3 and G0/0/1 with VLAN 4:
\begin{lstlisting}[style=dcnterminal]
[S1] interface GigabitEthernet 0/0/13
[S1-GigabitEthernet0/0/13] port link-type access
[S1] interface GigabitEthernet 0/0/1
[S1-GigabitEthernet0/0/1] port link-type access
[S1] vlan 2
[S1-vlan2] vlan 3
[S1-vlan3] port GigabitEthernet 0/0/13
[S1-vlan3] vlan 4
[S1-vlan4] port GigabitEthernet 0/0/1
\end{lstlisting}

On S2, the source uses \cmd{vlan batch} and \cmd{port default vlan}:
\begin{lstlisting}[style=dcnterminal]
[S2] vlan batch 2 to 4
[S2] interface GigabitEthernet 0/0/3
[S2-GigabitEthernet0/0/3] port link-type access
[S2-GigabitEthernet0/0/3] port default vlan 4
[S2] interface GigabitEthernet 0/0/6
[S2-GigabitEthernet0/0/6] port link-type access
[S2-GigabitEthernet0/0/6] port default vlan 2
\end{lstlisting}

\section{Step 4: address the hosts and verify VLAN isolation}
Hosts in the same VLAN use a common subnet. Initially, R1 and R3 in VLAN 4 should communicate; hosts in different VLANs should not.

\sourceimage[0.62\linewidth]{Ping evidence used in the supplied VLAN verification sequence}{vlan-connectivity-check.png}

\begin{verification}
Test both outcomes: same-VLAN connectivity and different-VLAN isolation before adding hybrid behavior.
\end{verification}

\section{Step 5: change selected ports to hybrid mode}
Hybrid ports then deliver VLAN 2 and VLAN 4 traffic untagged to selected devices.
\begin{lstlisting}[style=dcnterminal]
[S1] interface GigabitEthernet 0/0/1
[S1-GigabitEthernet0/0/1] port link-type hybrid
[S1-GigabitEthernet0/0/1] port hybrid untagged vlan 2 4
[S1-GigabitEthernet0/0/1] port hybrid pvid vlan 4

[S2] interface GigabitEthernet 0/0/3
[S2-GigabitEthernet0/0/3] port link-type hybrid
[S2-GigabitEthernet0/0/3] port hybrid untagged vlan 2 4
[S2-GigabitEthernet0/0/3] port hybrid pvid vlan 4

[S2] interface GigabitEthernet 0/0/6
[S2-GigabitEthernet0/0/6] port link-type hybrid
[S2-GigabitEthernet0/0/6] port hybrid untagged vlan 2 4
[S2-GigabitEthernet0/0/6] port hybrid pvid vlan 2
\end{lstlisting}

\section{PVID and untagged behavior}
The commands are complementary:
\begin{itemize}
\item \cmd{port hybrid pvid vlan <id>} assigns a VLAN classification to untagged frames received from the host.
\item \cmd{port hybrid untagged vlan 2 4} permits frames from the listed VLANs to leave that host-facing port without a VLAN tag.
\end{itemize}
Retest reachability: the selected VLAN-2 and VLAN-4 hosts should communicate, while the VLAN-3 host remains isolated.

\begin{conceptmap}
\textbf{Ingress from host, no tag} $\rightarrow$ classify using the port PVID.\\
\textbf{Frame inside the switch} $\rightarrow$ VLAN membership is tracked logically.\\
\textbf{Egress toward selected host} $\rightarrow$ listed hybrid VLANs can be sent untagged.
\end{conceptmap}

\section{The native-VLAN mismatch: a leak that reports no error}
A trunk sends exactly one VLAN \emph{untagged} --- the native VLAN, taken from the port's PVID. If the two ends disagree about which VLAN that is, each switch files the untagged frames into a different VLAN, and two supposedly isolated VLANs are joined.

\begin{netfigure}{Mismatched native VLAN silently merges two VLANs}
\begin{tikzpicture}[node distance=1.1cm and 2.6cm]
\node[switch] (s1) {SW1};
\node[switch,right=3.4cm of s1] (s2) {SW2};
\node[vlanA,left=1.3cm of s1] (h1) {VLAN 10};
\node[vlanB,right=1.3cm of s2] (h2) {VLAN 20};

\draw[flowarrow] (h1) -- (s1);
\draw[very thick,draw=MLRed,-{Stealth[length=2.2mm]}]
   (s1) -- node[netlabel,above,text width=3.2cm,align=center]
   {frame crosses with \textbf{no tag}} (s2);
\draw[flowarrow] (s2) -- (h2);

\node[netlabel,below=2mm of s1,text width=3.0cm,align=center]
  {PVID 10\\ \tiny ``VLAN 10 goes untagged''};
\node[netlabel,below=2mm of s2,text width=3.0cm,align=center]
  {PVID 20\\ \tiny ``untagged means VLAN 20''};
\node[below=1.35cm of $(s1)!0.5!(s2)$,font=\scriptsize\bfseries\color{MLRed},
      text width=6.4cm,align=center]
  {Result: VLAN 10 traffic is delivered into VLAN 20.\\
   Both switches log nothing. Both configurations look correct.};
\end{tikzpicture}
\end{netfigure}

\begin{pitfall}
If a cross-VLAN ping unexpectedly \emph{succeeds}, that is not a success --- it is this bug. Set the native VLAN explicitly, to an unused VLAN carrying no user data (999 in the lab files), on \emph{both} ends. Confirm with \cmd{display port vlan} rather than assuming, because nothing else will tell you.
\end{pitfall}

\section{Troubleshooting VLAN reachability}
\begin{netsteps}
\item Check the host IP/subnet first; VLAN experiments still require correct Layer-3 addressing on the endpoints.
\item Confirm the host-facing port link type and VLAN/PVID assignment.
\item Confirm the VLAN exists on both switches where required.
\item Confirm Eth-Trunk 1 is up and is configured as a trunk carrying the relevant VLANs.
\item For hybrid ports, inspect both the PVID and the tagged/untagged VLAN lists.
\item Use ping results as evidence only after the switch configuration matches the intended VLAN path.
\end{netsteps}

\begin{tryit}
Complete the source assignment: build a topology with two switches, two routers, and two PCs. Put the routers in VLAN 2 and the PCs in VLAN 3. Verify same-VLAN and different-VLAN connectivity and capture the command-prompt evidence.
\end{tryit}

\begin{quickcheck}
\begin{enumerate}
\item Why is the inter-switch Eth-Trunk configured as a trunk rather than an access port?
\item What does an access port simplify for a non-VLAN-aware host?
\item In the source exercise, what does the hybrid PVID control?
\item Why should the lab include both successful and failed ping tests?
\end{enumerate}
\end{quickcheck}

\begin{chapterrecap}
Access ports serve one VLAN, trunks carry several, and hybrid ports control ingress classification and egress tagging per VLAN. Verify the design with both successful and failed connectivity tests.
\end{chapterrecap}

\input{labs/08-vlan}
